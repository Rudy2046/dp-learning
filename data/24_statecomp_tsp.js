// data/24_statecomp_tsp.js
// 章节：24.集合类状压DP·TSP · 旅行商问题

export const LESSON = {
  id:    'statecomp_tsp',
  title: '24.集合类状压DP·TSP',
  sub:   '旅行商问题',

  battles: {
    new: {
      rank: '新兵', coins: 50, xp: 120,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 旅行商问题（TSP）',
          body:
`有 n 座城市，已知任意两城之间的距离 dist[i][j]。
从城市 0 出发，经过每座城市恰好一次，最终返回城市 0，求最短总路程。

这是经典的 NP-Hard 问题，但当 n ≤ 20 时，可以用状压 DP 在 O(2^n · n^2) 时间内求解。`,
          concept:
`状态定义：
  dp[S][i] = 已经访问过的城市集合为 S，当前停在城市 i，此时的最短路程。
  S 是一个整数，其二进制第 k 位为 1 表示城市 k 已被访问。

转移方程：
  从集合 S 中某个前驱城市 j 转移到当前城市 i：
  dp[S][i] = min(dp[S ^ (1 << i)][j] + dist[j][i])
  其中 j 是 S 中除 i 之外任意已访问城市。

初始化：
  dp[1][0] = 0  （只访问了城市 0，停在城市 0，路程为 0）
  其余初始化为正无穷。

答案：
  min(dp[(1<<n)-1][i] + dist[i][0])，枚举最后停在哪座城市 i 再回到起点。`,
          tip: '📌 S 必须包含 i（第 i 位为 1），否则状态无意义，别忘记这个前提检查。',
        },
        {
          type:  'choice',
          lbl:   '状态',
          title: '❓ 选择：状态 dp[S][i] 中 S 的含义',
          q:
`dp[S][i] 表示"已访问城市集合为 S，当前在城市 i"。
假设共 4 座城市（编号 0~3），此时 S = 0b0111 = 7，i = 2，
请问已访问了哪些城市？当前在哪里？`,
          opts: [
            '已访问城市 0、1、2，当前在城市 2',
            '已访问城市 1、2、3，当前在城市 2',
            '已访问城市 0、1、2，当前在城市 1',
            '已访问城市 0、2、3，当前在城市 2',
          ],
          ans:     0,
          fb_ok:  '✓ S=0b0111，第 0、1、2 位为 1，即已访问城市 0、1、2；i=2 表示当前停在城市 2。',
          fb_err: '✗ S 的二进制第 k 位为 1 表示城市 k 已被访问。0b0111 第 0、1、2 位为 1，对应城市 0、1、2；i=2 是当前位置。',
          hint:   '把 S=7 写成二进制，从低位到高位数哪几位是 1。',
        },
        {
          type:  'choice',
          lbl:   '转移',
          title: '❓ 选择：状态转移方向',
          q:
`计算 dp[S][i] 时，需要枚举上一个城市 j（j != i，且 j 在 S 中）。
转移式为 dp[S][i] = min(dp[S ^ (1<<i)][j] + dist[j][i])。
请问 S ^ (1<<i) 这个操作的作用是什么？`,
          opts: [
            '把集合 S 中第 i 位清零，得到"到达 i 之前"的城市集合',
            '把集合 S 中第 i 位置 1，加入城市 i',
            '把集合 S 中第 j 位清零，去掉城市 j',
            '把集合 S 的所有位取反',
          ],
          ans:     0,
          fb_ok:  '✓ 因为 i 已经在 S 中（第 i 位为 1），异或 1<<i 正好把第 i 位清零，还原出"还没到 i 时"的集合。',
          fb_err: '✗ S 中第 i 位已经是 1，XOR 1<<i = 清零该位，得到不含 i 的集合，也就是到达城市 i 之前的状态。',
          hint:   '当某一位已经是 1 时，XOR 1 的结果是什么？',
        },
        {
          type:  'fill',
          lbl:   '代码',
          title: '✏️ 填空：TSP 核心转移',
          q:     '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`const int INF = 1e9;
int n;
int dist[20][20];
int dp[1 << 20][20];

void solve() {
    // 初始化全为 INF
    for (int s = 0; s < (1 << n); s++)
        for (int i = 0; i < n; i++) dp[s][i] = INF;

    // [A] 起点：只访问了城市 0，停在城市 0
    dp[1][0] = 0;

    for (int s = 1; s < (1 << n); s++) {
        for (int i = 0; i < n; i++) {
            if (!(s & (1 << i))) continue; // i 不在集合中跳过
            if (dp[s][i] == INF) continue;
            for (int j = 0; j < n; j++) {
                if (s & (1 << j)) continue; // j 已访问跳过
                // [B] 从城市 i 扩展到未访问的城市 j
                dp[s | (1 << j)][j] = min(dp[s | (1 << j)][j], dp[s][i] + dist[i][j]);
            }
        }
    }

    int ans = INF;
    int full = (1 << n) - 1;
    for (int i = 1; i < n; i++)
        // [C] 所有城市都访问后，从 i 回到起点 0
        ans = min(ans, dp[full][i] + dist[i][0]);
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'dp[1][0] = 0;',
            'dp[0][0] = 0;',
            'dp[s | (1 << j)][j] = min(dp[s | (1 << j)][j], dp[s][i] + dist[i][j]);',
            'dp[s | (1 << j)][j] = min(dp[s | (1 << j)][j], dp[s][i] + dist[j][i]);',
            'ans = min(ans, dp[full][i] + dist[i][0]);',
            'ans = min(ans, dp[full][i] + dist[0][i]);',
          ],
          answers: ['dp[1][0] = 0;', 'dp[s | (1 << j)][j] = min(dp[s | (1 << j)][j], dp[s][i] + dist[i][j]);', 'ans = min(ans, dp[full][i] + dist[i][0]);'],
          fb_ok:  '✓ A：出发时只有城市 0 被访问，集合=1，位置=0，费用=0。B：扩展到 j，集合加入 j，距离用 dist[i][j]（从 i 到 j）。C：最后从 i 回到 0，用 dist[i][0]。',
          fb_err: '✗ A：集合应为 1（二进制 01）不是 0；B：dist[i][j] 是从 i 出发到 j，不是反过来；C：回到起点 0 的距离是 dist[i][0] 不是 dist[0][i]（有向图中方向很重要）。',
          hint:   '注意起点状态集合是 1 而不是 0；扩展时是从 i 走到 j，方向决定 dist 下标顺序。',
        },
      ],
    },

    pro: {
      rank: '锐士', coins: 80, xp: 200,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 TSP 的两种写法对比',
          body:
`TSP 状压 DP 有两种等价写法，理解两者的区别有助于灵活应对变形题。

写法一（推送式 / 枚举下一个城市）：
  枚举当前状态 dp[S][i]，再枚举下一个未访问城市 j，更新 dp[S|(1<<j)][j]。

写法二（拉取式 / 枚举上一个城市）：
  枚举当前状态 dp[S][i]，再枚举 S 中上一个城市 j（j != i），由 dp[S^(1<<i)][j] 更新 dp[S][i]。

两种写法复杂度相同，但枚举对象不同。`,
          concept:
`写法一（推送式）：
  外层枚举 S 和 i（i 在 S 中），内层枚举下一城市 j（j 不在 S 中）
  dp[S|(1<<j)][j] = min(dp[S|(1<<j)][j], dp[S][i] + dist[i][j])

写法二（拉取式）：
  外层枚举 S 和 i（i 在 S 中），内层枚举前驱城市 j（j 在 S 中，j != i）
  dp[S][i] = min(dp[S][i], dp[S^(1<<i)][j] + dist[j][i])

常见陷阱：
  写法二中枚举 j 时，j 必须在 S^(1<<i) 中，即 (S^(1<<i)) & (1<<j) != 0，
  确保 j 在"到 i 之前"的集合里。
  写法一则要确认 j 不在当前 S 中。`,
          tip: '📌 两种写法中，初始化和答案汇总完全相同，只有内层循环的枚举对象和转移方向不同。',
        },
        {
          type:  'choice',
          lbl:   '陷阱',
          title: '❓ 选择：写法二的合法前驱',
          q:
`用拉取式写法计算 dp[S][i]，枚举前驱城市 j，转移式为：
  dp[S][i] = min(dp[S][i], dp[S ^ (1<<i)][j] + dist[j][i])

以下哪个条件能正确筛选出合法的 j？`,
          opts: [
            '(S ^ (1<<i)) & (1<<j)，即 j 在去掉 i 后的集合中',
            'S & (1<<j)，即 j 在当前集合 S 中（含 i）',
            '!(S & (1<<j))，即 j 不在集合中',
            'j == i - 1，即 j 是 i 的前一个编号',
          ],
          ans:     0,
          fb_ok:  '✓ 拉取式需要 j 出现在"去掉 i 之前"的集合 S^(1<<i) 里，用 (S^(1<<i)) & (1<<j) != 0 判断。',
          fb_err: '✗ 仅检查 S & (1<<j) 会包含 j==i 的情况（自己转移到自己）；!(S & (1<<j)) 会枚举未访问的城市，方向完全反了。',
          hint:   '拉取式的前驱 j 必须在"还没到 i 时"的集合里，那个集合是 S 去掉 i 之后得到的。',
        },
        {
          type:  'choice',
          lbl:   '复杂度',
          title: '❓ 选择：TSP 状压 DP 的时间复杂度',
          q:
`n 座城市的 TSP 状压 DP，外层枚举集合 S（2^n 个），内层双重枚举城市 i 和 j（各 n 次），总时间复杂度是多少？`,
          opts: [
            'O(2^n · n^2)',
            'O(n! )',
            'O(2^n · n)',
            'O(n^2 · 2^(n/2))',
          ],
          ans:     0,
          fb_ok:  '✓ 外层 2^n 个集合，每个集合内双重枚举 i、j 各 n 次，共 O(2^n · n^2)。比暴力枚举全排列的 O(n!) 快得多。',
          fb_err: '✗ 枚举 i 和 j 是两层 n 的循环，乘在一起是 n^2，不是 n。n=20 时 2^20 * 400 ≈ 4 亿，刚好卡在可行范围。',
          hint:   '2^n 个集合，每个集合内两重循环各跑 n 次，把三项乘起来。',
        },
        {
          type:  'fill',
          lbl:   '代码',
          title: '✏️ 填空：拉取式 TSP 转移',
          q:     '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 拉取式写法
for (int s = 1; s < (1 << n); s++) {
    for (int i = 0; i < n; i++) {
        // [A] 城市 i 必须在集合 s 中
        if (!(s & (1 << i))) continue;
        int prev = s ^ (1 << i); // 去掉 i 之前的集合
        for (int j = 0; j < n; j++) {
            // [B] j 必须在 prev 集合中
            if (!(prev & (1 << j))) continue;
            if (dp[prev][j] == INF) continue;
            // [C] 拉取转移：由 j->i 更新当前状态
            dp[s][i] = min(dp[s][i], dp[prev][j] + dist[j][i]);
        }
    }
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'if (!(s & (1 << i))) continue;',
            'if (s & (1 << i)) continue;',
            'if (!(prev & (1 << j))) continue;',
            'if (!(s & (1 << j))) continue;',
            'dp[s][i] = min(dp[s][i], dp[prev][j] + dist[j][i]);',
            'dp[s][i] = min(dp[s][i], dp[prev][j] + dist[i][j]);',
          ],
          answers: [
            'if (!(s & (1 << i))) continue;',
            'if (!(prev & (1 << j))) continue;',
            'dp[s][i] = min(dp[s][i], dp[prev][j] + dist[j][i]);',
          ],
          fb_ok:  '✓ A：i 必须在 s 中才有意义；B：j 必须在去掉 i 之后的集合 prev 中；C：拉取式从 j 到 i，距离是 dist[j][i]。',
          fb_err: '✗ A：条件取反会跳过合法状态；B：应检查 prev 而非 s，否则 j==i 的情况不会被排除；C：dist 下标顺序是"从 j 出发到 i"，写反会在有向图中得到错误答案。',
          hint:   '拉取式：j 是上一个城市，i 是当前城市，所以 dist[j][i]；j 要在 prev（不含 i 的集合）里。',
        },
      ],
    },

    hero: {
      rank: '英雄', coins: 120, xp: 280,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 集合类 vs 棋盘类状压：本质区别',
          body:
`棋盘类状压（第 23 章）和集合类状压（本章 TSP）都用整数的二进制位表示状态，
但两者的状态语义和转移方式有根本区别。

理解这个区别，能帮你在新题面前快速判断该用哪种框架。`,
          concept:
`棋盘类状压（以互不侵犯为例）：
  状态 S 表示"某一行的棋子放置方案"，是一个 n 位二进制数。
  转移：按行推进，dp[row][S] -> dp[row+1][S']。
  状态中的每一位对应棋盘的一列，位为 1 表示该列放了棋子。
  状态空间：O(行数 * 2^n)，转移在相邻行之间进行。

集合类状压（TSP）：
  状态 S 表示"已经访问的城市集合"，是一个 n 位二进制数。
  转移：按集合大小推进（集合从小到大），dp[S][i] -> dp[S|(1<<j)][j]。
  状态中的每一位对应一个城市，位为 1 表示该城市已被访问。
  状态空间：O(2^n * n)，转移在不同集合之间进行。

核心区别：
  棋盘类：二进制位 = 空间位置（列），状态沿"行"这个物理维度推进。
  集合类：二进制位 = 集合成员，状态沿"集合大小"这个逻辑维度推进。
  棋盘类的 S 在每一行都被"刷新"；集合类的 S 单调增长，已访问的位不会清零。`,
          tip: '📌 看到"每个元素只用一次、需要记住用过哪些"就联想集合类状压；看到"按行/列递推、同一位置可复用"就联想棋盘类状压。',
        },
        {
          type:  'choice',
          lbl:   '对比',
          title: '❓ 选择：判断题型归属',
          q:
`有一道题：给定 n 个任务，每个任务有执行时间，任意两个任务有"切换代价"，
问完成所有任务的最少总时间（每个任务恰好做一次）。
这道题应该用哪种状压框架？`,
          opts: [
            '集合类状压：用 S 记录"已完成的任务集合"，类似 TSP',
            '棋盘类状压：按任务编号逐行递推',
            '两种都可以，本质相同',
            '不需要状压，用普通线性 DP 即可',
          ],
          ans:     0,
          fb_ok:  '✓ 每个任务恰好做一次、需要记录"已完成哪些"，和 TSP 记录"已访问哪些城市"完全同构，用集合类状压。',
          fb_err: '✗ 棋盘类状压是针对"某行/列的放置模式"，不适合"每个元素恰好用一次"的排列类问题。这道题和 TSP 同构。',
          hint:   '关键词是"每个元素恰好用一次"+"需要知道用过哪些"，这是集合类状压的标志。',
        },
        {
          type:  'choice',
          lbl:   '推广',
          title: '❓ 选择：TSP 变形——不需要回到起点',
          q:
`把 TSP 改成"从城市 0 出发，经过所有城市一次，停在任意城市，不需要回到 0"，
只需改动原 TSP 代码的哪个部分？`,
          opts: [
            '只改答案汇总部分：去掉 +dist[i][0]，直接取 min(dp[full][i])',
            '改初始化：dp[0][0] = 0 而不是 dp[1][0] = 0',
            '改转移方程：枚举 j 时允许 j 已在集合中',
            '改循环顺序：集合从大到小枚举',
          ],
          ans:     0,
          fb_ok:  '✓ 不需要回到起点，只需在所有城市都被访问后取 min(dp[full][i]) 即可，不加回起点的距离。其余部分完全不变。',
          fb_err: '✗ 初始化、转移、循环顺序都不需要改变；唯一的区别在于最后"是否加上回到起点的距离"。',
          hint:   'TSP 和"哈密顿路径"的区别只在最后要不要回到起点，找到对应的代码位置。',
        },
        {
          type:  'fill',
          lbl:   '代码',
          title: '✏️ 填空：区分两类状压的关键代码特征',
          q:     '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 集合类状压 TSP 框架特征

// 特征1：状态含义
// dp[S][i]: S = 已访问城市集合，i = 当前城市
// [A] 初始化起点状态（集合只含城市 0）

// 特征2：转移方向——集合单调扩大
for (int s = 1; s < (1 << n); s++) {
    for (int i = 0; i < n; i++) {
        if (!(s & (1 << i))) continue;
        for (int j = 0; j < n; j++) {
            if (s & (1 << j)) continue;
            // [B] 扩展到未访问城市 j，集合加入 j
            dp[s | (1 << j)][j] = min(dp[s | (1 << j)][j], dp[s][i] + dist[i][j]);
        }
    }
}

// 特征3：答案汇总
int ans = INF, full = (1 << n) - 1;
for (int i = 0; i < n; i++)
    // [C] 所有城市已访问，当前在 i，回到起点
    ans = min(ans, dp[full][i] + dist[i][0]);`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'dp[1][0] = 0;',
            'dp[0][0] = 0;',
            'dp[s | (1 << j)][j] = min(dp[s | (1 << j)][j], dp[s][i] + dist[i][j]);',
            'dp[s & ~(1 << j)][j] = min(dp[s & ~(1 << j)][j], dp[s][i] + dist[i][j]);',
            'ans = min(ans, dp[full][i] + dist[i][0]);',
            'ans = min(ans, dp[full][i]);',
          ],
          answers: [
            'dp[1][0] = 0;',
            'dp[s | (1 << j)][j] = min(dp[s | (1 << j)][j], dp[s][i] + dist[i][j]);',
            'ans = min(ans, dp[full][i] + dist[i][0]);',
          ],
          fb_ok:  '✓ A：起点集合 = 1（只含城市 0），不是 0；B：OR 操作把 j 加入集合，集合单调扩大；C：full = 2^n-1 表示全集，最后加 dist[i][0] 回到起点。',
          fb_err: '✗ A：dp[0][0]=0 的集合为空，城市 0 还没被"访问"，状态语义错误；B：AND 操作是从集合中去掉元素，方向反了；C：不加 dist[i][0] 就变成了"不需要回到起点"的变形版。',
          hint:   '集合类状压三个特征：初始化 dp[1][0]=0，转移用 OR 扩大集合，答案在 full 集合下汇总。',
        },
      ],
    },
  },
};
