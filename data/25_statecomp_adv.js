// data/25_statecomp_adv.js
// 章节：25.状压DP综合 · 轮廓线DP入门

export const LESSON = {
  id:    'statecomp_adv',
  title: '25.状压DP综合',
  sub:   '轮廓线DP入门',

  battles: {
    new: {
      rank: '新兵', coins: 50, xp: 120,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 轮廓线DP——逐格推进',
          body:
`经典问题：用 1×2 的多米诺骨牌铺满 n 行 m 列的棋盘，求铺法总数。

按行转移的棋盘状压每次处理一整行，状态表示一行的放置方案。
轮廓线 DP 则更细致——逐格推进，一次处理一个格子，
状态表示"当前处理格子及其之前的一条波浪形分界线"。`,
          concept:
`轮廓线的定义：
  把棋盘格子按行优先顺序编号（第 r 行第 c 列 = 位置 r*m+c）。
  "轮廓线"是已填区域和未填区域之间的分界线，
  用一个 m 位二进制数 S 表示：
    第 k 位为 1：轮廓线在第 k 列"凸出"，即该列已被上一个骨牌的一半占据。
    第 k 位为 0：该列在轮廓线后方，当前格子可以自由放置。

状态定义：
  dp[c][S] = 处理到当前行第 c 列时，轮廓线状态为 S 的方案数。
  每处理一个格子，轮廓线状态更新一次，最终处理完整行后进入下一行。

与按行转移的区别：
  按行状压：状态 = 整行放置方案，一次转移跨越一整行。
  轮廓线：状态 = 局部分界线，一次转移只移动一格，更通用、可处理不规则棋盘。`,
          tip: '📌 轮廓线的二进制位含义是"该列是否有骨牌的一半伸出来等待配对"，不是整行方案。',
        },
        {
          type:  'choice',
          lbl:   '状态',
          title: '❓ 选择：轮廓线状态的含义',
          q:
`3 列棋盘，轮廓线状态 S = 0b101 = 5（从低位到高位对应第 0、1、2 列）。
请问这个状态表示什么？`,
          opts: [
            '第 0 列和第 2 列有骨牌的一半伸出来，第 1 列没有',
            '第 0 列和第 2 列整列已经全部铺好，第 1 列空着',
            '第 1 列有骨牌的一半伸出来，第 0、2 列没有',
            '第 0、1、2 列都有骨牌伸出来',
          ],
          ans:     0,
          fb_ok:  '✓ S=0b101，第 0 位和第 2 位为 1，表示第 0 列和第 2 列各有一个竖向骨牌的上半格占据，需要等待下一行配对；第 1 位为 0，第 1 列无伸出。',
          fb_err: '✗ 轮廓线的每一位不是"整列是否铺好"，而是"该列是否有竖向骨牌的一半正在等待配对"。S=0b101 的第 0 位和第 2 位为 1。',
          hint:   '把 5 写成三位二进制，从低位（第 0 列）到高位（第 2 列）看哪几位是 1。',
        },
        {
          type:  'choice',
          lbl:   '推进',
          title: '❓ 选择：处理当前格子时的两种选择',
          q:
`轮廓线 DP 处理到格子 (r, c)，当前该格对应轮廓线第 c 位。
如果第 c 位为 0（该格空闲），可以做哪些操作？`,
          opts: [
            '放竖向骨牌（占用下方格，轮廓线第 c 位置 1）或放横向骨牌（前提是第 c+1 位也为 0）',
            '只能放竖向骨牌，轮廓线第 c 位置 1',
            '只能空着，等下一行再处理',
            '放横向骨牌或不放，竖向骨牌要求第 c 位为 1',
          ],
          ans:     0,
          fb_ok:  '✓ 第 c 位为 0 表示空闲，可以：1) 放一个竖向骨牌占住本格和下方格，轮廓线第 c 位置 1；2) 放横向骨牌（本格和右边格），前提是第 c+1 位也为 0（右边也空闲）。',
          fb_err: '✗ 空闲格有两种放置选择，不局限于竖向；横向骨牌要求当前格和右邻格都空闲（两个位都为 0）。',
          hint:   '骨牌有两个方向：竖向占用本格+下方格；横向占用本格+右侧格。分别对轮廓线有什么影响？',
        },
        {
          type:  'fill',
          lbl:   '代码',
          title: '✏️ 填空：轮廓线DP逐格推进',
          q:     '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 用轮廓线DP求 n×m 棋盘的多米诺铺法数
long long dp[2][1 << 12]; // dp[cur][S]
int n, m;

long long solve() {
    int cur = 0;
    memset(dp, 0, sizeof(dp));
    // [A] 初始状态：第一行第 0 列处理前，轮廓线全 0（无伸出）
    dp[0][0] = 1;

    for (int r = 0; r < n; r++) {
        for (int c = 0; c < m; c++) {
            int nxt = cur ^ 1;
            memset(dp[nxt], 0, sizeof(dp[nxt]));
            for (int s = 0; s < (1 << m); s++) {
                if (!dp[cur][s]) continue;
                if (s & (1 << c)) {
                    // 第 c 位为 1：该格被竖向骨牌上半格占据，直接清掉这一位继续
                    // [B] 清除第 c 位，轮廓线向右推进
                    dp[nxt][s ^ (1 << c)] += dp[cur][s];
                } else {
                    // 第 c 位为 0：该格空闲，可以放竖向或横向骨牌
                    // 选择1：放竖向骨牌，第 c 位置 1（下方格将被占）
                    dp[nxt][s | (1 << c)] += dp[cur][s];
                    // 选择2：放横向骨牌，需要 c+1 < m 且第 c+1 位也为 0
                    // [C] 横向骨牌：同时占用第 c 列和第 c+1 列
                    if (c + 1 < m && !(s & (1 << (c + 1))))
                        dp[nxt][s] += dp[cur][s];
                }
            }
            cur = nxt;
        }
        // 每处理完一行，轮廓线"归位"：只保留第 c 位为 1（竖向伸入下一行）的状态合法
    }
    // [A2] 最终状态：所有格处理完，轮廓线全 0（无骨牌伸出边界）
    return dp[cur][0];
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'dp[0][0] = 1;',
            'dp[0][(1<<m)-1] = 1;',
            'dp[nxt][s ^ (1 << c)] += dp[cur][s];',
            'dp[nxt][s & ~(1 << c)] += dp[cur][s];',
            'if (c + 1 < m && !(s & (1 << (c + 1)))) dp[nxt][s] += dp[cur][s];',
            'if (c + 1 < m && (s & (1 << (c + 1)))) dp[nxt][s] += dp[cur][s];',
          ],
          answers: [
            'dp[0][0] = 1;',
            'dp[nxt][s ^ (1 << c)] += dp[cur][s];',
            'if (c + 1 < m && !(s & (1 << (c + 1)))) dp[nxt][s] += dp[cur][s];',
          ],
          fb_ok:  '✓ A：初始轮廓线全 0，方案数为 1。B：XOR 清除第 c 位和 AND 取反效果相同，但这里第 c 位一定是 1，XOR 更简洁。C：横向骨牌要求右邻（第 c+1 位）也为 0，用 ! 取反判断。',
          fb_err: '✗ A：初始轮廓线应全为 0（无伸出），不是全 1；B：两种写法效果一样，但要确保只操作第 c 位；C：横向骨牌要求右邻为 0，条件是 !(s & (1<<(c+1)))，若改成有 1 则逻辑反了。',
          hint:   '初始方案只有一种（空棋盘，轮廓线全 0）；清除一位用 XOR；横向骨牌要求右边也空闲，即那一位为 0。',
        },
      ],
    },

    pro: {
      rank: '锐士', coins: 80, xp: 200,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 轮廓线DP的常见变形',
          body:
`理解了基础轮廓线DP之后，可以处理几类常见变形：
1. 棋盘有障碍格（某些格子不能放骨牌）
2. 骨牌类型不止一种（如 1×1 和 1×2 混合）
3. 求最大权重而非方案数

这些变形只需修改局部逻辑，整体框架不变。`,
          concept:
`变形1：带障碍的棋盘
  处理到障碍格时，该格只能"跳过"（既不能被放置，也不能被竖向骨牌占用）。
  若当前轮廓线第 c 位为 1（有骨牌占据），且该格是障碍，则此状态非法，直接丢弃。
  若第 c 位为 0 且该格是障碍，轮廓线第 c 位保持 0 继续推进（不做任何放置）。

变形2：带权骨牌（求最大权重）
  dp 数组改成存最大值而非方案数，转移时用 max 代替加法。
  初始化 dp[0][0] = 0，其余为 -INF。

变形3：1×1 和 1×2 骨牌混合
  空闲格多了一个选择：放 1×1 骨牌，轮廓线第 c 位保持 0 继续推进。
  原来两个分支变成三个分支：1×1、1×2横向、1×2竖向。`,
          tip: '📌 处理障碍格时，轮廓线第 c 位为 1 说明有骨牌伸入障碍格，这是非法的，直接 continue 丢弃该状态。',
        },
        {
          type:  'choice',
          lbl:   '障碍',
          title: '❓ 选择：障碍格的处理',
          q:
`处理到障碍格 (r, c) 时，当前轮廓线状态 s 的第 c 位为 1（有骨牌的一半伸入该格）。
应该怎么处理？`,
          opts: [
            '直接 continue，丢弃该状态（骨牌侵入障碍格，非法）',
            '和普通格一样，清零第 c 位继续推进',
            '把第 c 位置 0，同时方案数减半',
            '允许伸入，障碍格只是不能主动放置骨牌',
          ],
          ans:     0,
          fb_ok:  '✓ 障碍格不能被任何骨牌覆盖，包括被竖向骨牌的一半占据。若第 c 位为 1 说明已经有骨牌伸入，此状态非法，直接丢弃。',
          fb_err: '✗ 障碍格不允许任何骨牌覆盖，不论主动放置还是被动延伸。第 c 位为 1 就是非法状态，不能继续推进。',
          hint:   '骨牌的上半格（竖向）同样"覆盖"了那个格子，障碍格禁止被覆盖。',
        },
        {
          type:  'choice',
          lbl:   '对比',
          title: '❓ 选择：轮廓线 vs 按行状压的适用场景',
          q:
`以下哪个场景，轮廓线DP比按行状压更有优势？`,
          opts: [
            '棋盘有随机分布的障碍格，不能整行枚举合法状态',
            '棋盘无障碍，只用 1×2 的骨牌，n 和 m 都较小',
            '需要求方案数对质数取模',
            '骨牌只能横向放置',
          ],
          ans:     0,
          fb_ok:  '✓ 有障碍格时，按行状压需要预处理每行的合法状态（障碍使状态集合复杂），而轮廓线逐格处理，遇障碍直接丢弃状态，更简洁直接。',
          fb_err: '✗ 无障碍的纯骨牌铺设两种方法都可以做，对模运算也没影响；只能横向放置反而让按行状压更简单。带障碍才是轮廓线的主要优势场景。',
          hint:   '轮廓线逐格推进的灵活性在哪种情况下最能体现？想想按行状压处理障碍格时有哪些麻烦。',
        },
        {
          type:  'fill',
          lbl:   '代码',
          title: '✏️ 填空：带障碍的轮廓线DP',
          q:     '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`bool block[105][105]; // block[r][c] = true 表示障碍
long long dp[2][1 << 12];

// 处理格子 (r, c) 时的障碍判断
for (int s = 0; s < (1 << m); s++) {
    if (!dp[cur][s]) continue;

    if (block[r][c]) {
        // [A] 障碍格：若有骨牌伸入（第 c 位为 1），非法；否则跳过该格继续
        if (s & (1 << c)) continue; // 骨牌侵入障碍，非法
        dp[nxt][s] += dp[cur][s];   // 第 c 位为 0，跳过障碍格
    } else if (s & (1 << c)) {
        // 第 c 位为 1：竖向骨牌上半在此，清除继续
        // [B] 清除第 c 位
        dp[nxt][s ^ (1 << c)] += dp[cur][s];
    } else {
        // 第 c 位为 0：可放竖向或横向骨牌
        dp[nxt][s | (1 << c)] += dp[cur][s]; // 竖向
        // [C] 横向骨牌条件：右邻不越界且右邻也无伸出
        if (c + 1 < m && !block[r][c+1] && !(s & (1 << (c + 1))))
            dp[nxt][s] += dp[cur][s];
    }
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'if (s & (1 << c)) continue; dp[nxt][s] += dp[cur][s];',
            'if (!(s & (1 << c))) continue; dp[nxt][s] += dp[cur][s];',
            'dp[nxt][s ^ (1 << c)] += dp[cur][s];',
            'dp[nxt][s | (1 << c)] += dp[cur][s];',
            'if (c + 1 < m && !block[r][c+1] && !(s & (1 << (c + 1)))) dp[nxt][s] += dp[cur][s];',
            'if (c + 1 < m && !(s & (1 << (c + 1)))) dp[nxt][s] += dp[cur][s];',
          ],
          answers: [
            'if (s & (1 << c)) continue; dp[nxt][s] += dp[cur][s];',
            'dp[nxt][s ^ (1 << c)] += dp[cur][s];',
            'if (c + 1 < m && !block[r][c+1] && !(s & (1 << (c + 1)))) dp[nxt][s] += dp[cur][s];',
          ],
          fb_ok:  '✓ A：障碍格两步处理，先判非法（第 c 位为 1 则 continue），再转移（第 c 位为 0 则跳过）。B：清除第 c 位用 XOR。C：横向骨牌还要额外检查右邻格是否是障碍。',
          fb_err: '✗ A：条件判断取反会把合法状态丢弃、把非法状态保留；B：OR 会把 0 变 1，不是清除；C：带障碍时横向骨牌还必须额外检查右邻格 block[r][c+1]，否则骨牌可能覆盖障碍。',
          hint:   '障碍格处理分两步：先排除非法（有骨牌伸入），再处理合法（跳过）。横向骨牌要同时检查右邻是否也是障碍。',
        },
      ],
    },

    hero: {
      rank: '英雄', coins: 120, xp: 280,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 棋盘状压 vs 轮廓线：两类状压的本质区别',
          body:
`学完三种状压框架（棋盘类、集合类TSP、轮廓线），现在做一个系统对比。
英雄关的目标是：能从题目描述中快速判断应该用哪种框架，并说出理由。`,
          concept:
`三种状压框架对比：

棋盘类状压（第 23 章）：
  状态语义：S = 某一行的"放置模式"（哪列放了棋子）
  推进单位：一整行
  转移：dp[row][S] -> dp[row+1][S']，相邻行之间的合法性检查
  适用：棋子只影响相邻行、规则整齐的棋盘类问题

集合类状压/TSP（第 24 章）：
  状态语义：S = "已访问的元素集合"，j = 当前所在元素
  推进单位：集合大小（单调增）
  转移：dp[S][j] -> dp[S|(1<<k)][k]，每次把一个新元素加入集合
  适用：每个元素恰好用一次、排列顺序影响代价的问题

轮廓线DP（本章）：
  状态语义：S = "当前处理格旁边的波浪形分界线状态"
  推进单位：一个格子
  转移：每格只有有限种选择（放竖/横/跳过），逐格更新轮廓线
  适用：覆盖类铺设问题，尤其有障碍或不规则形状时更灵活

判断方法：
  看"状态更新的最小单位"：整行 → 棋盘类；单个元素加入集合 → 集合类；单个格子 → 轮廓线。`,
          tip: '📌 三种框架都用整数二进制表示状态，但"每一位的含义"完全不同，这是区分它们的关键。',
        },
        {
          type:  'choice',
          lbl:   '判断',
          title: '❓ 选择：框架匹配',
          q:
`题目：n 名工人，m 项任务（n=m≤15），每人完成某项任务有一个代价 cost[i][j]，
每项任务恰好分配给一名工人，每名工人最多做一项任务，求总代价最小的分配方案。
（这是经典的"最优分配/指派问题"）
应该用哪种状压框架？`,
          opts: [
            '集合类状压：S 表示"已分配任务的集合"，逐工人枚举任务',
            '棋盘类状压：按工人编号逐行递推',
            '轮廓线DP：把工人和任务排成格子逐格处理',
            '不需要状压，用匈牙利算法',
          ],
          ans:     0,
          fb_ok:  '✓ 每项任务恰好用一次，需要记录"已分配了哪些任务"，和 TSP 记录"已访问哪些城市"完全同构。用集合类状压：dp[S][i] = 前 i 名工人处理完毕、已分配任务集合为 S 时的最小代价。',
          fb_err: '✗ 棋盘类用于"行列式放置"，不适合"每元素恰好用一次"的分配；轮廓线用于覆盖铺设；匈牙利算法可以解这道题，但状压 DP 更直接且 n≤15 时完全可行。',
          hint:   '关键词：每项任务恰好用一次 + 需要记录哪些已用 + n≤15。和 TSP 有什么相似之处？',
        },
        {
          type:  'choice',
          lbl:   '对比',
          title: '❓ 选择：状态"推进单位"的区别',
          q:
`棋盘类状压、集合类状压（TSP）、轮廓线DP 三种框架，"状态推进的最小单位"分别是什么？`,
          opts: [
            '棋盘类：一整行；集合类：一个元素（加入集合）；轮廓线：一个格子',
            '三种都是一整行',
            '棋盘类：一个格子；集合类：一整行；轮廓线：一个元素',
            '棋盘类：一个元素；集合类：一个格子；轮廓线：一整行',
          ],
          ans:     0,
          fb_ok:  '✓ 棋盘类按行转移（dp[row][S]->dp[row+1][S\']），集合类每次把一个元素加入集合（S->S|(1<<k)），轮廓线逐格推进（处理完 (r,c) 再到 (r,c+1)）。',
          fb_err: '✗ 三种框架的推进粒度不同，正是这种区别决定了它们各自的适用场景。棋盘类最粗（一行），轮廓线最细（一格），集合类介于两者之间（一个元素加入集合）。',
          hint:   '看每种框架的外层循环变量：棋盘类是 row，集合类是集合大小，轮廓线是 (r, c)。',
        },
        {
          type:  'fill',
          lbl:   '代码',
          title: '✏️ 填空：最优指派（集合类状压）',
          q:     '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// n 名工人分配 n 项任务，cost[i][j] = 工人 i 做任务 j 的代价
// dp[S] = 恰好分配了任务集合 S（|S|个任务）给前 |S| 名工人的最小代价
int dp[1 << 16];
int cost[16][16], n;

void solve() {
    // [A] 初始化：空集代价为 0，其余为正无穷
    fill(dp, dp + (1 << n), 1e9);
    dp[0] = 0;

    for (int s = 0; s < (1 << n); s++) {
        if (dp[s] == (int)1e9) continue;
        // [B] 当前已分配 popcount(s) 个任务，下一个工人编号为 popcount(s)
        int worker = __builtin_popcount(s);
        if (worker == n) continue;
        for (int j = 0; j < n; j++) {
            if (s & (1 << j)) continue; // 任务 j 已被分配
            // [C] 把任务 j 分配给 worker，更新集合 s|(1<<j) 的代价
            dp[s | (1 << j)] = min(dp[s | (1 << j)], dp[s] + cost[worker][j]);
        }
    }
    // 答案为 dp[(1<<n)-1]
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'fill(dp, dp + (1 << n), 1e9); dp[0] = 0;',
            'fill(dp, dp + (1 << n), 0); dp[0] = 1e9;',
            'int worker = __builtin_popcount(s);',
            'int worker = s;',
            'dp[s | (1 << j)] = min(dp[s | (1 << j)], dp[s] + cost[worker][j]);',
            'dp[s | (1 << j)] = min(dp[s | (1 << j)], dp[s] + cost[j][worker]);',
          ],
          answers: [
            'fill(dp, dp + (1 << n), 1e9); dp[0] = 0;',
            'int worker = __builtin_popcount(s);',
            'dp[s | (1 << j)] = min(dp[s | (1 << j)], dp[s] + cost[worker][j]);',
          ],
          fb_ok:  '✓ A：求最小值初始化为正无穷，dp[0]=0 表示空分配代价为 0。B：已分配了 popcount(s) 个任务，下一个工人编号就是 popcount(s)（从 0 开始）。C：cost[worker][j] 是工人 worker 做任务 j 的代价，下标顺序不能反。',
          fb_err: '✗ A：求最小要初始化为大值，dp[0] 应为 0；B：工人编号等于已分配任务数，用 __builtin_popcount 统计 s 中 1 的个数；C：cost 下标是[工人][任务]，写反会得到错误代价。',
          hint:   '关键洞察：当集合 S 中已有 k 个任务被分配时，对应的是前 k 名工人做了这些任务，下一个工人编号是 k = popcount(S)。',
        },
      ],
    },
  },
};
