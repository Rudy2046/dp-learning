// data/11_multi_pack.js
// 章节：11.多重背包与二进制优化 · 有限件数的背包

export const LESSON = {
  id:    'multi_pack',
  title: '11.多重背包与二进制优化',
  sub:   '有限件数的背包',

  battles: {

    // ─────────────────────────────────────────────
    // 新兵：多重背包核心理解
    // ─────────────────────────────────────────────
    new: {
      rank: '新兵',
      coins: 50,
      xp: 120,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 多重背包问题',
          body:
`有 n 种物品和一个容量为 W 的背包。
第 i 种物品：重量 w[i]，价值 v[i]，最多可取 c[i] 件。
问：在不超过背包容量的前提下，能装入的最大总价值是多少？

与 0/1 背包的区别：
  0/1 背包：每种物品只有 0 件或 1 件
  完全背包：每种物品可取无限件
  多重背包：每种物品最多取 c[i] 件（有明确上限）

例：W=10，物品如下：
  物品1：w=3，v=4，c=2（最多2件）
  物品2：w=4，v=5，c=3（最多3件）
  物品3：w=2，v=3，c=4（最多4件）`,
          concept:
`最朴素的做法：对每种物品枚举取 0、1、2、...、c[i] 件。

状态定义：dp[j] 表示背包容量为 j 时的最大价值（滚动数组）。

转移方程（对每种物品 i，逆序枚举容量 j，再枚举件数 k）：
  for k in 1..c[i]:
    dp[j] = max(dp[j], dp[j - k*w[i]] + k*v[i])

时间复杂度：O(n * W * max_c)
当 c[i] 很大时，内层枚举件数成为瓶颈。

边界：dp[0..W] 初始化为 0。`,
          table: [
            ['物品', 'w', 'v', 'c', '可选件数'],
            ['1', '3', '4', '2', '0件、1件、2件'],
            ['2', '4', '5', '3', '0件、1件、2件、3件'],
            ['3', '2', '3', '4', '0件、1件、2件、3件、4件'],
          ],
          tip: '📌 多重背包不能直接套完全背包的正序循环——那样会让某件物品被无限次选取，超出 c[i] 的限制。',
        },

        {
          type:  'choice',
          lbl:   '区别',
          title: '❓ 选择：多重背包和完全背包的核心区别',
          q:     '以下哪句话最准确地描述了多重背包与完全背包的核心区别？',
          opts: [
            '多重背包每种物品只能选1件，完全背包每种物品可以无限选',
            '多重背包的容量比完全背包大，所以需要额外限制件数',
            '多重背包每种物品有明确的件数上限 c[i]，完全背包件数无上限',
            '多重背包和完全背包的转移方程完全相同，只是初始化不同',
          ],
          ans:    2,
          fb_ok:  '正确。多重背包的关键约束就是件数上限 c[i]：每种物品最多取 c[i] 件，而完全背包可以无限取。件数上限决定了两种背包的转移逻辑完全不同。',
          fb_err: '多重背包不是"只能选1件"（那是0/1背包）。容量与件数限制无关。转移方程也不同：完全背包正序枚举容量，多重背包需额外枚举件数。核心区别是：多重背包每种物品有明确上限 c[i]，完全背包没有。',
          hint:   '想想三种背包（0/1、完全、多重）对"每种物品最多取几件"的限制分别是什么。',
        },

        {
          type:  'choice',
          lbl:   '推导',
          title: '❓ 选择：朴素多重背包的时间复杂度',
          q:
`有 n=100 种物品，背包容量 W=1000，每种物品最多 c[i]=100 件。
朴素做法（三层循环：枚举物品、容量、件数）的时间复杂度是多少？`,
          opts: [
            'O(n * W)，约 10^5 次操作',
            'O(n * W * max_c)，约 10^7 次操作',
            'O(n^2 * W)，约 10^7 次操作',
            'O(n * W^2)，约 10^8 次操作',
          ],
          ans:    1,
          fb_ok:  '正确。三层循环：n=100 种物品 × W=1000 个容量 × max_c=100 件数 = 10^7 次操作。当 c[i] 更大时（如10000），就会达到 10^9，超出时限。',
          // 【修正】原版fb_err只重申了正确答案，补充了对三个干扰项的针对性说明
          fb_err: '朴素做法是三层循环：物品 n × 容量 W × 件数 max_c，复杂度为 O(n*W*max_c) = 100×1000×100 = 10^7。选项A漏掉了件数枚举层，只有两层循环；选项C误把复杂度写成 n^2*W，多了一个n因子；选项D误把W写成W^2，与实际三层循环不符。',
          hint:   '数一数循环层数：最外层是物品，中间是容量，最内层是件数——三层各自的规模是多少？',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：朴素多重背包核心代码',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 朴素多重背包，dp[j]表示容量j时的最大价值
void multiPackNaive(int n, int W,
    vector<int>& w, vector<int>& v, vector<int>& c) {
    vector<int> dp(W + 1, 0);
    for (int i = 0; i < n; i++) {
        // 逆序枚举容量，防止同一物品被重复计入
        for (int j = W; j >= w[i]; j--) {
            // 枚举取第i种物品 k 件（k从1到c[i]，且不超容量）
            for (int k = 1; [A]; k++) {
                dp[j] = max(dp[j], [B]);
            }
        }
    }
}`,
          blanks:  ['A', 'B'],
          opts: [
            'k <= c[i] && k * w[i] <= j', 'k <= c[i]',
            'dp[j - k*w[i]] + k*v[i]',    'dp[j - w[i]] + k*v[i]',
          ],
          answers: ['k <= c[i] && k * w[i] <= j', 'dp[j - k*w[i]] + k*v[i]'],
          fb_ok:   '正确。A 处需要双重条件：k 不超过件数上限 c[i]，且 k 件物品的总重量不超过当前容量 j。B 处取走 k 件后，剩余容量 j-k*w[i] 的最优价值加上 k 件的总价值 k*v[i]。',
          fb_err:  'A 处：只写 k<=c[i] 会在 k*w[i]>j 时访问负下标，必须同时加 k*w[i]<=j 的容量约束。B 处：取 k 件，剩余容量是 j-k*w[i]（不是 j-w[i]），总价值是 k*v[i]。',
          hint:    'A 想想 k 同时受两个约束：件数上限和容量上限；B 想想取 k 件后容量减少了多少、价值增加了多少。',
        },
      ],
    },

    // ─────────────────────────────────────────────
    // 锐士：二进制拆分原理
    // ─────────────────────────────────────────────
    pro: {
      rank: '锐士',
      coins: 80,
      xp: 200,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 二进制拆分加速多重背包',
          body:
`核心思想：把"最多取 c[i] 件"的一种物品，
拆分成若干"只能取 0 件或 1 件"的新物品组，
然后对这些新物品跑 0/1 背包。

朴素做法枚举 0~c[i]，共 c[i]+1 个选项。
二进制拆分后，用 O(log c[i]) 个新物品就能表示全部 0~c[i] 的组合。

拆分方式（以 c=13 为例）：
  13 = 1 + 2 + 4 + 6
  拆成重量分别为 1、2、4、6 倍的新物品（组）
  取任意子集，可以凑出 0~13 中的任意整数。

为什么是 1、2、4、6？
  先取 2 的幂次：1、2、4（累计 7，还剩 13-7=6）
  剩余 6 < 下一个幂次 8，直接把 6 作为最后一组。`,
          concept:
`二进制拆分的通用步骤（c[i] 件物品）：

  令 rem = c[i]，k = 1
  while rem > 0:
    取 t = min(k, rem)
    生成新物品：重量 t*w[i]，价值 t*v[i]（0/1物品）
    rem -= t
    k *= 2

新物品数量为 O(log c[i])，对全部新物品跑 0/1 背包。

整体复杂度：O(n * W * log(max_c))
  朴素：O(n * W * max_c)
  优化后：O(n * W * log(max_c))
  当 max_c=1000 时，约快 100 倍（log2(1000)≈10）。`,
          table: [
            ['c[i]', '拆分组', '可表示范围', '组数'],
            ['7',  '1, 2, 4',      '0~7',  '3（=log2(8)）'],
            ['13', '1, 2, 4, 6',   '0~13', '4'],
            ['15', '1, 2, 4, 8',   '0~15', '4（=log2(16)）'],
            ['20', '1, 2, 4, 8, 5','0~20', '5'],
          ],
          tip: '📌 最后一组的大小是"余数"，不一定是2的幂——这很正常，余数组保证覆盖到精确的上限 c[i]，不会超出。',
        },

        {
          type:  'choice',
          lbl:   '原理',
          title: '❓ 选择：二进制拆分的核心原因',
          q:     '二进制拆分能把 c[i] 件物品压缩到 O(log c[i]) 个新物品的根本原因是什么？',
          opts: [
            '二进制每一位只有0和1，所以枚举位数比枚举件数更快',
            '1,2,4,8,...这些2的幂次的任意子集相加，可以表示0到它们之和的所有整数',
            '将物品拆成更多份后，每份重量更小，背包更容易装满',
            '0/1背包的时间复杂度比多重背包低，所以转化后一定更快',
          ],
          ans:    1,
          fb_ok:  '正确。2的幂次（1,2,4,8,...）具有完备表示性：它们的任意子集（每个要或不要）恰好能覆盖 0 到它们之和的全部整数，因此用 O(log c) 个0/1选择就能模拟 c+1 种件数选择。',
          fb_err: '核心不是"位数更少"，也不是"重量变小"，更不是"0/1背包天生更快"（转化后物品数增多了）。关键在于：1,2,4,...的幂次子集能无遗漏地表示所有 0~c[i] 的整数，这是二进制表示的完备性。',
          hint:   '想想 1+2+4=7，用这三个数的子集（选或不选）能凑出 0~7 的哪些数？能凑全吗？',
        },

        {
          type:  'choice',
          lbl:   '拆分',
          title: '❓ 选择：c=11 的二进制拆分结果',
          q:
`对 c[i]=11 的物品进行二进制拆分（原始重量 w，价值 v）。
按照"先取2的幂次，最后取余数"的规则，拆出的组是哪些？`,
          opts: [
            '1,2,4,3（四组，1+2+4+3=10，总和不足11）',
            '1,2,4,4（四组，1+2+4+4=11，覆盖0~11）',
            '1,2,8（三组，1+2+8=11，但跳过了k=4这步）',
            '1,2,4,8（四组，总和15，超出上限）',
          ],
          ans:    1,
          fb_ok:  '正确。步骤：rem=11，k=1，取1（rem=10）；k=2，取2（rem=8）；k=4，取4（rem=4）；k=8，但 rem=4 < 8，直接取余数4（rem=0）。拆成 1,2,4,4，共4组。子集可以表示 0~11 的所有整数。',
          // 【修正】原版把选项C说成"1+2+4+3=10"，实际选项C是"1,2,8"。已修正各选项的错误说明。
          fb_err: '拆分步骤：rem=11，依次取 k=1（rem变10），k=2（rem变8），k=4（rem变4），此时 rem=4 < 下一个 k=8，直接把余数4作为最后一组。结果是 1,2,4,4，总和=11。选项A的1+2+4+3=10总和不足11，无法覆盖11件。选项C的1,2,8跳过了k=4这步，虽总和=11，但会漏掉5件等中间组合（5=1+4，而4这组缺失）。选项D总和=15已超出上限c[i]=11。',
          hint:   '按步骤走：k从1开始每次翻倍，每次取 min(k, rem)，直到 rem=0。c=11 需要几步？',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：二进制拆分预处理代码',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 将多重背包物品二进制拆分，转化为0/1背包物品列表
void binaryExpand(int n,
    vector<int>& w, vector<int>& v, vector<int>& c,
    vector<int>& nw, vector<int>& nv) { // 新物品列表
    for (int i = 0; i < n; i++) {
        int rem = c[i], k = 1;
        while (rem > 0) {
            int t = [A]; // 本轮拆出的件数：取k和余量的较小值
            nw.push_back(t * w[i]); // 新物品重量
            nv.push_back(t * v[i]); // 新物品价值
            rem -= t;
            [B]; // 下一轮拆分量翻倍
        }
    }
}`,
          blanks:  ['A', 'B'],
          opts: [
            'min(k, rem)', 'max(k, rem)',
            'k *= 2',      'k += 1',
          ],
          answers: ['min(k, rem)', 'k *= 2'],
          fb_ok:   '正确。A 处取 min(k, rem)：优先取当前幂次 k，但若剩余量 rem 不足 k 则取余量，防止超出总件数。B 处 k*=2 让每轮拆分量翻倍，保证覆盖所有二进制位。',
          fb_err:  'A 处：取 max(k, rem) 会在 rem 很小时多拆，导致总和超过 c[i]；应取 min(k, rem) 保证不超。B 处：k+=1 是线性增长，拆出的组数是 O(c[i]) 而非 O(log c[i])，达不到加速效果；应 k*=2 翻倍。',
          hint:    'A 想想当 rem 剩余量比 k 小时，应该取哪个更安全；B 想想"二进制"的特点——相邻两组的大小关系是翻倍还是递增1？',
        },
      ],
    },

    // ─────────────────────────────────────────────
    // 英雄：13件拆分演示 & 完整流程对比
    // ─────────────────────────────────────────────
    hero: {
      rank: '英雄',
      coins: 120,
      xp: 280,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 把13件物品拆成1/2/4/6组：完整演示',
          body:
`物品原始参数：w=5，v=8，c=13（最多13件）

二进制拆分过程（rem=13，k从1开始翻倍）：

  第1轮：k=1，rem=13，t=min(1,13)=1
    新物品A：重量 1×5=5，价值 1×8=8
    rem = 13-1 = 12，k = 1×2 = 2

  第2轮：k=2，rem=12，t=min(2,12)=2
    新物品B：重量 2×5=10，价值 2×8=16
    rem = 12-2 = 10，k = 2×2 = 4

  第3轮：k=4，rem=10，t=min(4,10)=4
    新物品C：重量 4×5=20，价值 4×8=32
    rem = 10-4 = 6，k = 4×2 = 8

  第4轮：k=8，rem=6，t=min(8,6)=6（余量不足，取余量）
    新物品D：重量 6×5=30，价值 6×8=48
    rem = 6-6 = 0，结束。

拆分结果：A(5,8)、B(10,16)、C(20,32)、D(30,48)，共4个0/1物品。`,
          concept:
`为什么 1+2+4+6 能表示 0~13 的所有整数？

  1+2+4 = 7，可以表示 0~7（三个物品的 2^3=8 种子集）
  再加一个"6"，可以在 0~7 的基础上全部偏移 +6，得到 6~13
  两个范围合并：0~7 并 6~13 = 0~13（6和7有重叠，覆盖完整）

一般规律：
  若已覆盖 [0, S]，新加入一个大小为 t（t <= S+1）的组，
  则可以覆盖 [0, S] 并 [t, S+t] = [0, S+t]（因为 t<=S+1，两段无断口）

正是这个"无断口"性质，保证了二进制拆分的每一步都不会在覆盖范围内留下空洞。`,
          // 【修正】表头改为"累计可选件数"，与列内容对应
          table: [
            ['拆分组', '大小 t', '新物品重量', '新物品价值', '累计可选件数'],
            ['A', '1', '5',  '8',  '可选 0~1 件'],
            ['B', '2', '10', '16', '可选 0~3 件'],
            ['C', '4', '20', '32', '可选 0~7 件'],
            ['D', '6', '30', '48', '可选 0~13 件'],
          ],
          tip: '📌 最后一组大小 t=6 不是2的幂，但它满足 t=6 <= 已覆盖的 7+1=8，所以覆盖范围从0~7无断口地扩展到0~13。这是余数组合法的数学保证。',
        },

        {
          type:  'choice',
          lbl:   '覆盖',
          title: '❓ 选择：为什么余数组不会留下覆盖空洞',
          q:
`二进制拆分中，每次取 t=min(k, rem)。
在取余数组之前，已经覆盖了 [0, S] 范围内的所有整数。
余数 rem 满足 rem < k（否则就不是余数了）。

请问：加入大小为 rem 的最后一组后，新的覆盖范围是什么？
（提示：rem <= S+1 是关键条件）`,
          opts: [
            '[0, S] 和 [rem, S+rem]，两段之间可能有断口',
            '[0, S+rem]，两段首尾衔接，无断口',
            '[rem, S+rem]，之前的 [0, rem-1] 被覆盖范围替换掉',
            '[0, 2*S]，覆盖范围直接翻倍',
          ],
          ans:    1,
          fb_ok:  '正确。加入大小为 rem 的组后，新覆盖范围是 [0,S] 和 [rem, S+rem] 的并集。因为 rem <= S+1，所以 [rem, S+rem] 的起点 rem 不超过 S+1，两段无断口，合并为完整的 [0, S+rem]。',
          fb_err: '新增一个大小为 rem 的0/1物品，相当于在原有 [0,S] 基础上叠加一段 [rem, S+rem]。关键在于 rem <= S+1（余数小于下一个幂次，而前面的幂次之和恰好是 S=k-1），所以两段衔接无断口，合并成 [0, S+rem]。',
          hint:   '两段 [0,S] 和 [rem, S+rem] 要无断口衔接，rem 需要满足什么条件？rem 和 S 的大小关系是什么？',
        },

        {
          type:  'choice',
          lbl:   '对比',
          title: '❓ 选择：c=13 时朴素法和二进制优化的操作次数对比',
          q:
`W=100，c[i]=13，只考虑单个物品对容量的枚举次数：

  朴素法：对每个容量 j（共W个），枚举取 0~13 件（共14种）
  二进制法：转化为4个0/1物品后，对每个物品逆序枚举容量（共W个）

两种方法的操作次数分别是多少？`,
          opts: [
            '朴素：100×14=1400，二进制：4×100=400',
            '朴素：100×13=1300，二进制：4×100=400',
            '朴素：100×14=1400，二进制：100×log(13)≈365',
            '朴素：13×14=182，二进制：4×13=52',
          ],
          ans:    0,
          fb_ok:  '正确。朴素法：W个容量 × (c[i]+1)个件数选项 = 100×14=1400；二进制法：4个新物品 × W个容量 = 4×100=400。操作次数降低到约 2/7，c[i] 越大节省越多。',
          fb_err: '朴素法枚举件数是 0~c[i]，共 c[i]+1=14 种，而非13种；操作次数 = 100×14=1400。二进制拆分后有4个0/1物品，每个对全部容量枚举一次：4×100=400。答案是1400 vs 400。',
          hint:   '朴素法枚举件数是0件到c[i]件，一共几种？二进制拆出几个新物品？',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：二进制优化多重背包完整代码',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`int multiPackBinary(int n, int W,
    vector<int>& w, vector<int>& v, vector<int>& c) {
    vector<int> dp(W + 1, 0);
    // 第一步：二进制拆分，生成0/1物品列表
    vector<int> nw, nv;
    for (int i = 0; i < n; i++) {
        int rem = c[i], k = 1;
        while (rem > 0) {
            int t = min(k, rem);
            nw.push_back(t * w[i]);
            nv.push_back(t * v[i]);
            rem -= t;
            k *= 2;
        }
    }
    // 第二步：对新物品列表跑0/1背包
    for (int i = 0; i < (int)nw.size(); i++) {
        for (int j = W; [A]; j--) { // 逆序保证每件只选一次
            dp[j] = max(dp[j], [B]); // 选或不选当前新物品
        }
    }
    return dp[W];
}`,
          blanks:  ['A', 'B'],
          opts: [
            'j >= nw[i]',             'j >= 0',
            'dp[j - nw[i]] + nv[i]',  'dp[j - nw[i]] + nv[i] * 2',
          ],
          answers: ['j >= nw[i]', 'dp[j - nw[i]] + nv[i]'],
          fb_ok:   '正确。A 处：j 的下界是 nw[i]，容量不足时无法装入当前新物品，不需要更新；下界写0会访问 dp[j-nw[i]] 时出现负下标。B 处：选当前新物品，剩余容量 j-nw[i] 的最优价值加上新物品价值 nv[i]，这是标准0/1背包转移。',
          fb_err:  'A 处：j >= 0 会在 j < nw[i] 时尝试访问 dp[j-nw[i]]，出现负下标越界；下界应为 j >= nw[i]。B 处：价值是 nv[i] 而不是 nv[i]*2，nv[i] 已经是"t 件原物品的总价值"，不需要再乘2。',
          hint:    'A 这里和普通0/1背包的内层循环下界一样——装不下时没必要继续；B 想想 nv[i] 里已经包含了几件物品的价值。',
        },
      ],
    },
  },
};
