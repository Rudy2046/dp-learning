// data/02_triangle.js
// 章节：02.数字三角形 · 自底向上递推入门

export const LESSON = {
  id: 'triangle',
  title: '02.数字三角形',
  sub: '自底向上递推入门',

  battles: {
    new: {
      rank: '新兵', coins: 50, xp: 120, steps: [
        {
          type: 'concept',
          lbl: '读题',
          title: '📖 数字三角形：从塔顶走到塔底',
          body: `给定一个 n 行的数字三角形，从顶端出发，每一步只能走到下一行**相邻**的两个数字之一（正下方或右下方），求走到底部时路径上数字之和的最大值。

例如下面这个三角形：

\`\`\`
        7
      3   8
    8   1   0
  2   7   4   4
4   5   2   6   5
\`\`\`

最优路径是 7→3→8→7→5，总和为 **30**。`,
          concept: `**状态定义**：设 \`dp[i][j]\` 表示**从位置 (i,j) 出发，走到塔底**所能获得的最大路径和。

**为什么从底往上想？** 因为塔底每个位置就是它自己（无路可走），状态最简单；越往上选择越多。从最简单的子问题出发，正是 DP 的精髓。

**转移方程**：
\`\`\`
dp[i][j] = a[i][j] + max(dp[i+1][j], dp[i+1][j+1])
\`\`\`

**边界**：最后一行 \`dp[n-1][j] = a[n-1][j]\`

**答案**：\`dp[0][0]\``,
          table: [
            ['位置 (i,j)', '含义'],
            ['a[i][j]', '三角形中该位置的数值'],
            ['dp[i][j]', '从 (i,j) 出发到塔底的最大和'],
            ['dp[i+1][j]', '走到正下方继续累加的最大和'],
            ['dp[i+1][j+1]', '走到右下方继续累加的最大和'],
          ],
          tip: '📌 三角形是"左对齐"存储的：第 i 行有 i+1 个数，下标 j 范围是 0~i。',
        },
        {
          type: 'choice',
          lbl: '概念',
          title: '❓ 选择：dp[i][j] 究竟表示什么',
          q: `在自底向上的写法中，我们定义 \`dp[i][j]\` 为「从 (i,j) 出发到塔底的最大路径和」。

下列哪种理解是**正确**的？`,
          opts: [
            'dp[i][j] 表示从塔顶 (0,0) 走到 (i,j) 的最大和',
            'dp[i][j] 表示从 (i,j) 走到塔底某个位置的最大和',
            'dp[i][j] 表示 (i,j) 这个格子本身的值',
            'dp[i][j] 表示从 (i,j) 走到塔底的路径条数',
          ],
          ans: 1,
          fb_ok: '✓ 正确。dp[i][j] 是一个"未来值"——站在 (i,j) 往下看的最大收益。所以最终答案是 dp[0][0]，即从塔顶出发的最大收益。',
          fb_err: '✗ 注意"自底向上"的状态方向：dp[i][j] 描述的是**从该位置出发**还能拿到多少，而不是"走到这里已经拿了多少"。后者是自顶向下的另一种定义（也对，但和当前转移方程不匹配）。',
          hint: '看转移方程 dp[i][j] = a[i][j] + max(dp[i+1][...], ...)，它依赖的是"下方"的 dp 值，说明 dp 描述的是"往下走"的结果。',
        },
        {
          type: 'choice',
          lbl: '推导',
          title: '❓ 选择：手动算一格',
          q: `对于下面这个三角形：

\`\`\`
        7
      3   8
    8   1   0
  2   7   4   4
4   5   2   6   5
\`\`\`

最后一行的 dp 直接等于 a 本身：dp[4] = [4, 5, 2, 6, 5]。

请问 **dp[3][1]** 等于多少？(也就是第4行中数字 7 那个位置)`,
          opts: [
            '7 + max(5, 2) = 12',
            '7 + max(4, 5) = 12',
            '7 + 5 + 2 = 14',
            '7 + max(2, 6) = 13',
          ],
          ans: 0,
          fb_ok: '✓ 正确。dp[3][1] = a[3][1] + max(dp[4][1], dp[4][2]) = 7 + max(5, 2) = 12。注意 (3,1) 的下方是 (4,1) 和 (4,2)。',
          fb_err: '✗ 关键是搞清下标关系：从 (i,j) 出发，正下方是 (i+1, j)，右下方是 (i+1, j+1)。所以 (3,1) 看的是 dp[4][1]=5 和 dp[4][2]=2。',
          hint: '从 (i,j) 出发，下一行能到的两个位置下标分别是 j 和 j+1，不是 j-1 和 j+1。',
        },
        {
          type: 'fill',
          lbl: '代码',
          title: '✏️ 填空：基础递推',
          q: '点击代码中的 [?] 方框选中，再点候选项填入：',
          code: `int n;
int a[N][N], dp[N][N];

int solve() {
    // 第一步：初始化最后一行
    for (int j = 0; j < n; j++)
        dp[n-1][j] = [A];   // 边界：塔底位置的 dp 值

    // 第二步：自底向上推
    for (int i = n - 2; i >= 0; i--) {
        for (int j = 0; j <= i; j++) {
            dp[i][j] = a[i][j] + [B];   // 选下方两个中较大的
        }
    }

    return dp[0][0];   // 答案：塔顶出发的最大和
}`,
          blanks: ['A', 'B'],
          opts: [
            'a[n-1][j]', '0',
            'max(dp[i+1][j], dp[i+1][j+1])', 'max(dp[i-1][j], dp[i-1][j+1])',
          ],
          answers: ['a[n-1][j]', 'max(dp[i+1][j], dp[i+1][j+1])'],
          fb_ok: '✓ 完美！A 处：塔底位置无路可走，dp 值就是它自己 a[n-1][j]；B 处：从 (i,j) 往下，正下方是 (i+1,j)、右下方是 (i+1,j+1)，取较大者再加上当前格子。',
          fb_err: '✗ A 处填 0 是错的——dp 表示"含本格的最大和"，塔底位置的 dp 必须包含它自己。B 处用 i-1 方向错了：自底向上推时，新的 dp[i] 依赖的是**已经算好的** dp[i+1]，不是上方。',
          hint: '想清楚两件事：① 边界 dp[n-1][j] 该是多少？② 转移时 dp[i][j] 依赖的是上一行还是下一行的数据？',
        },
      ]
    },

    pro: {
      rank: '锐士', coins: 80, xp: 200, steps: [
        {
          type: 'concept',
          lbl: '优化',
          title: '📖 空间优化：滚动数组与一维 dp',
          body: `新兵关用了二维 dp[N][N]，但仔细想想——

计算 dp[i][j] 时，只用到了 dp[i+1][...] 这一行。**上一行算完后，更早的行就再也用不到了。**

这意味着：我们其实只需要一个**一维数组**就够了！

\`\`\`
原始：dp[i][j] = a[i][j] + max(dp[i+1][j], dp[i+1][j+1])
优化：dp[j]    = a[i][j] + max(dp[j],     dp[j+1])
\`\`\`

把二维的 i 这一维"滚掉"，用同一个一维数组反复覆盖。`,
          concept: `**关键：遍历顺序**

一维 dp[j] 复用空间时，更新 dp[j] 会**覆盖掉**原来的值。如果后面的转移还需要"旧的 dp[j]"，就会出错。

观察转移 \`dp[j] = a[i][j] + max(dp[j], dp[j+1])\`：
- 它要用到 **dp[j]** (旧值, 代表下方正下方) 和 **dp[j+1]** (旧值, 代表下方右下方)
- 我们要写入的位置是 **dp[j]**
- 只要保证写 dp[j] 时，dp[j+1] 还没被这一轮覆盖，就是安全的

**结论**：j 从小到大遍历就行，因为 dp[j+1] 是这一轮**还没轮到的**位置。

初始化：直接令 dp[j] = a[n-1][j] (最后一行)。`,
          table: [
            ['写法', '空间', '关键点'],
            ['二维 dp[N][N]', 'O(n²)', '直观，易理解'],
            ['一维 dp[N]', 'O(n)', 'j 从小到大遍历'],
            ['直接复用 a', 'O(1) 额外', '在 a 上原地修改'],
          ],
          tip: '📌 滚动数组的核心是「分析依赖方向」：依赖的位置不能在当前轮次中被提前覆盖。',
        },
        {
          type: 'choice',
          lbl: '陷阱',
          title: '❓ 选择：一维 dp 的遍历方向',
          q: `把二维 dp 压成一维时，转移是：

\`\`\`
dp[j] = a[i][j] + max(dp[j], dp[j+1]);
\`\`\`

请问 **内层循环 j 应该如何遍历**？`,
          opts: [
            'j 从大到小：for (j = i; j >= 0; j--)',
            'j 从小到大：for (j = 0; j <= i; j++)',
            '从大到小或从小到大都正确',
            '必须双向各扫一遍才正确',
          ],
          ans: 1,
          fb_ok: '✓ 正确！转移要用到 dp[j] 和 dp[j+1] 两个旧值，写入位置是 dp[j]。j 从小到大遍历时，dp[j+1] 还没被本轮触碰，旧值得以保留。',
          fb_err: '✗ 看依赖方向：写 dp[j] 时需要"旧 dp[j+1]"还在。如果 j 从大到小，dp[j+1] 会先被写入新值，等算 dp[j] 时拿到的就是已经污染的新值了。注意这和 0/1 背包"j 从大到小"的方向是**反过来**的，因为背包依赖的是 dp[j-w]（左边），三角形依赖的是 dp[j+1]（右边）。',
          hint: '画一张表：j=0,1,2,3 依次更新时，新 dp[j] 依赖谁？谁会被先污染？',
        },
        {
          type: 'choice',
          lbl: '对比',
          title: '❓ 选择：两种写法对比',
          q: `下面两种写法都试图解决数字三角形：

**写法甲**（自底向上递推）：
\`\`\`cpp
for (int j = 0; j < n; j++) dp[n-1][j] = a[n-1][j];
for (int i = n-2; i >= 0; i--)
    for (int j = 0; j <= i; j++)
        dp[i][j] = a[i][j] + max(dp[i+1][j], dp[i+1][j+1]);
return dp[0][0];
\`\`\`

**写法乙**（自顶向下递推）：
\`\`\`cpp
dp[0][0] = a[0][0];
for (int i = 1; i < n; i++)
    for (int j = 0; j <= i; j++)
        dp[i][j] = a[i][j] + max(dp[i-1][j-1], dp[i-1][j]);
// 最后扫最后一行取最大值
int ans = 0;
for (int j = 0; j < n; j++) ans = max(ans, dp[n-1][j]);
\`\`\`

关于二者的**差异**，下列哪项描述**最准确**？`,
          opts: [
            '两种写法答案不一样，甲对乙错',
            '两种写法答案相同，但乙需要处理 j=0 和 j=i 的边界',
            '乙的状态定义错了，根本无法做数字三角形',
            '甲更慢，因为是从下往上的"反向"思考',
          ],
          ans: 1,
          fb_ok: '✓ 正确。两种写法都对，答案一致。但写法乙中，第 i 行的最左 (j=0) 只能从 (i-1, 0) 来，最右 (j=i) 只能从 (i-1, i-1) 来——dp[i-1][j-1] 和 dp[i-1][j] 其中一个可能越界，需要特殊处理；写法甲不需要，因为 (i+1, j) 和 (i+1, j+1) 在下一行总是存在的。',
          fb_err: '✗ 两种写法都是合法的 DP 思路，时间复杂度都是 O(n²)，答案也相同。差别在于**边界处理的便利性**：甲是"自然边界"，乙需要小心 j=0 和 j=i 时的越界。这正是数字三角形常用自底向上写法的原因。',
          hint: '动手画一下乙的写法在第 2 行 j=0 和 j=2 时分别从哪些位置转移过来。',
        },
        {
          type: 'fill',
          lbl: '代码',
          title: '✏️ 填空：一维空间优化',
          q: '点击代码中的 [?] 方框选中，再点候选项填入：',
          code: `int n;
int a[N][N];
int dp[N];   // 只用一维数组

int solve() {
    // 初始化：把最后一行复制进来
    for (int j = 0; j < n; j++)
        dp[j] = a[n-1][j];

    // 自底向上，一维滚动
    for (int i = n - 2; i >= 0; i--) {
        for ([A]) {   // 内层 j 的遍历方向
            dp[j] = a[i][j] + [B];   // 用旧 dp[j] 和旧 dp[j+1]
        }
    }

    return dp[0];
}`,
          blanks: ['A', 'B'],
          opts: [
            'int j = 0; j <= i; j++', 'int j = i; j >= 0; j--',
            'max(dp[j], dp[j+1])', 'max(dp[j], dp[j-1])',
          ],
          answers: ['int j = 0; j <= i; j++', 'max(dp[j], dp[j+1])'],
          fb_ok: '✓ 准确！A 处 j 从小到大走，使得 dp[j+1] 在被读取时仍是上一轮的旧值；B 处依赖正下方 dp[j] 和右下方 dp[j+1]，二者都是"右边"的旧值，与遍历方向匹配。',
          fb_err: '✗ A 错了会导致 dp[j+1] 被先覆盖成新值，结果错乱；B 用 dp[j-1] 方向错了——三角形向下走只能到 j 和 j+1（同列或右下），不是 j-1（左下）。',
          hint: '两个空互相关联：B 决定了我们要保留谁的旧值，A 决定了遍历顺序能否保住那个旧值。',
        },
      ]
    },

    hero: {
      rank: '英雄', coins: 120, xp: 280, steps: [
        {
          type: 'concept',
          lbl: '换角度',
          title: '📖 自顶向下递归 vs 自底向上递推',
          body: `同一个数字三角形问题，可以用两种"语法"表达 DP：

**方式一：自顶向下递归 + 记忆化**（像第01章的斐波那契）
\`\`\`
solve(i, j) 表示从 (i,j) 走到塔底的最大和
  if 已算过: 直接返回
  if 到塔底: 返回 a[i][j]
  否则: a[i][j] + max(solve(i+1, j), solve(i+1, j+1))
\`\`\`

**方式二：自底向上递推**（新兵/锐士关写过）
\`\`\`
for i = n-1 down to 0:
  for j = 0 to i:
    dp[i][j] = a[i][j] + max(dp[i+1][j], dp[i+1][j+1])
\`\`\`

两者**计算的是完全相同的状态**，但"驱动方式"不同。`,
          concept: `**核心对比**

| 维度 | 自顶向下（递归+记忆化） | 自底向上（递推） |
|------|------------------------|-----------------|
| 思维方向 | 从问题反推子问题 | 从子问题搭建大问题 |
| 触发计算 | 用到才算（懒求值） | 全部都算（饿求值） |
| 代码形态 | 递归函数 | 嵌套循环 |
| 状态访问顺序 | 由递归调用栈决定 | 由循环显式指定 |

**为什么要学两种？**

- **自顶向下**：状态定义直接、思路自然，适合初学和"难想出循环顺序"的题。
- **自底向上**：常数小、易做空间优化（滚动数组）、不会爆栈。竞赛中的主流写法。

**等价性**：两种写法计算出的 dp 值**完全一样**。差别只在"先算谁后算谁"。`,
          tip: '📌 自顶向下不需要思考循环顺序，但必须开记忆化数组，否则退化为 O(2^n) 指数爆炸。',
        },
        {
          type: 'choice',
          lbl: '理解',
          title: '❓ 选择：递归方向 vs 递推方向',
          q: `小明在自顶向下的版本里写了下面的递归函数：

\`\`\`cpp
int solve(int i, int j) {
    if (memo[i][j] != -1) return memo[i][j];
    if (i == n - 1) return memo[i][j] = a[i][j];
    return memo[i][j] = a[i][j] + max(solve(i+1, j), solve(i+1, j+1));
}
\`\`\`

而自底向上写法是 \`for (int i = n-2; i >= 0; i--)\`，i 从大到小。

**两种"方向"看起来相反**，为什么结果一样？下列解释**最准确**？`,
          opts: [
            '其实方向不相反：递归 solve(0,0) 实际先冲到底，再从底层一路返回填表',
            '方向确实相反，所以两个版本算出的 dp 值是镜像对称的',
            '递归版本会算出不同的结果，只是恰好题目允许多解',
            '递归实际上是 i 从 0 到 n-1 顺序计算，和递推方向不同',
          ],
          ans: 0,
          fb_ok: '✓ 透彻！递归调用 solve(0,0)，会一路下钻直到 i=n-1 才命中边界返回，然后回溯时从底层向上填 memo。**实际填表顺序仍是从底到顶**——和自底向上完全一致。代码"看起来"从顶往下，运行时其实从底往上。',
          fb_err: '✗ 关键洞察：递归的"调用方向"和"赋值方向"是相反的。调用是 (0,0)→(1,0)→(2,0)→...→(n-1,0) 一路下钻，但 memo 数组的赋值发生在**回溯时**，所以是 (n-1,0) 先被算出来，(0,0) 最后被算出来。这和递推循环 i 从大到小是一致的。',
          hint: '想想递归调用栈：solve(0,0) 什么时候才会返回？它需要 solve(1,0) 和 solve(1,1) 都先返回，而这俩又需要更下层先返回……',
        },
        {
          type: 'choice',
          lbl: '陷阱',
          title: '❓ 选择：记忆化的"门票"',
          q: `下面是一份"自顶向下"的代码，但小红觉得能再优化一下，于是改成了这样：

\`\`\`cpp
int solve(int i, int j) {
    if (i == n - 1) return a[i][j];
    return a[i][j] + max(solve(i+1, j), solve(i+1, j+1));
}
\`\`\`

她**删掉了 memo 数组**，理由是"反正算的都是一样的状态，没必要存"。

这样改之后，对于 n=30 的输入，会发生什么？`,
          opts: [
            '答案依然正确，运行速度和原版一样快',
            '答案依然正确，但时间复杂度从 O(n²) 退化为 O(2^n)',
            '答案会出错，因为没有 memo 数组无法记录状态',
            '答案正确且变得更快，因为省去了查表的开销',
          ],
          ans: 1,
          fb_ok: '✓ 一针见血。答案是对的（递归逻辑没变），但每个 solve(i,j) 会被反复重新计算——solve(i+1, j) 在算 solve(i, j) 时被叫一次，在算 solve(i, j-1) 时又被叫一次……整个调用树退化为二叉树，时间复杂度 O(2^n)。n=30 时大约 10亿次调用，会超时。',
          fb_err: '✗ 没有 memo 仍然能算出正确答案，但**重叠子问题**会被反复计算。这正是第01章斐波那契强调的核心思想——记忆化把指数级变成多项式级。这是"DP=最优子结构+重叠子问题+记忆化"中最关键的一环。',
          hint: '回想第01章 fib：如果递归 fib(n) 不加记忆化会是什么复杂度？数字三角形也是同样的道理。',
        },
        {
          type: 'fill',
          lbl: '代码',
          title: '✏️ 填空：自顶向下记忆化版',
          q: '点击代码中的 [?] 方框选中，再点候选项填入：',
          code: `int n;
int a[N][N];
int memo[N][N];   // 初始化为 -1 表示"未算过"

int solve(int i, int j) {
    if ([A]) return memo[i][j];   // 命中缓存直接返回
    if (i == n - 1) return memo[i][j] = a[i][j];   // 边界：塔底
    return memo[i][j] = a[i][j] + [B];   // 递归求两个子问题并取较大者
}

int main() {
    // 输入 n 和三角形 a[][]
    memset(memo, -1, sizeof(memo));
    cout << solve(0, 0);   // 从塔顶 (0,0) 出发
    return 0;
}`,
          blanks: ['A', 'B'],
          opts: [
            'memo[i][j] != -1', 'memo[i][j] == -1',
            'max(solve(i+1, j), solve(i+1, j+1))', 'max(solve(i-1, j), solve(i-1, j+1))',
          ],
          answers: ['memo[i][j] != -1', 'max(solve(i+1, j), solve(i+1, j+1))'],
          fb_ok: '✓ 完美！A 处：memo 初始化为 -1，"不等于 -1"意味着已经算过了，直接复用结果；B 处：从 (i,j) 向下走只能到 (i+1, j) 或 (i+1, j+1)，递归求解两者再取较大。整体和自底向上写法等价，只是改用了递归驱动。',
          fb_err: '✗ A 写成 == -1 含义就反了——"等于 -1"代表**没算过**，这时候直接 return 就把"未初始化值"当成结果返回了。B 处方向写成 i-1 错了：从 (i,j) 向下应该到 i+1 行，不是 i-1。',
          hint: 'A 处的判断条件是"已经算过吗？"，memo[i][j] 在什么情况下证明已经算过？B 处和新兵关的转移方程方向应当完全一致。',
        },
      ]
    },
  }
};