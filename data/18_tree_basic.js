// data/18_tree_basic.js
// 章节：18.树形DP基础 · 没有上司的舞会

export const LESSON = {
  id:    'tree_basic',
  title: '18.树形DP基础',
  sub:   '没有上司的舞会',

  battles: {

    // ══════════════════════════════════════════════
    // 新兵关：理解核心——树形DP的状态与DFS
    // ══════════════════════════════════════════════
    new: {
      rank: '新兵',
      coins: 50,
      xp: 120,
      steps: [

        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 没有上司的舞会——树上的独立集问题',
          body:
`题目：公司有 n 名员工，关系是一棵以 1 号为根的有根树（每人最多一个直属上司）。
每名员工有一个"快乐值" happy[i]。
规则：若某员工参加舞会，则他的直属上司不能参加（上下级不能同时出席）。
求所有合法方案中，参加舞会的员工快乐值总和的最大值。

建模：
  这是树上的"最大权独立集"问题——选取一个点集，使得集合中没有父子关系的两个点，
  并最大化所选点的权值之和。

关键思路：
  对每个节点 u，只需知道两件事：
    f[u][1] = 以 u 为根的子树中，u 参加时的最大快乐值
    f[u][0] = 以 u 为根的子树中，u 不参加时的最大快乐值
  
  转移（v 是 u 的所有子节点）：
    f[u][1] = happy[u] + sum{ f[v][0] }  （u参加，所有子节点必须不参加）
    f[u][0] = sum{ max(f[v][0], f[v][1]) }（u不参加，每个子节点自由选择）`,
          concept:
`状态定义：
  f[u][0] = 以 u 为根的子树，u 不参加时的最大快乐值总和
  f[u][1] = 以 u 为根的子树，u 参加时的最大快乐值总和

转移方程（对 u 的每个子节点 v）：
  f[u][1] += f[v][0]    （u参加 → 子节点v不能参加）
  f[u][0] += max(f[v][0], f[v][1])  （u不参加 → 子节点v可参加可不参加，取最优）

初始化（叶节点 u）：
  f[u][0] = 0    （叶节点不参加，快乐值0）
  f[u][1] = happy[u]  （叶节点参加，贡献自身快乐值）

实现：用 DFS，后序遍历（先处理子节点，再处理当前节点）。
  f[u] 初始化为 f[u][1]=happy[u], f[u][0]=0，
  然后对每个子节点 v，DFS(v) 后把 v 的结果合并到 u。

答案：max(f[root][0], f[root][1])`,
          table: [
            ['节点u', 'f[u][0]（不参加）', 'f[u][1]（参加）'],
            ['叶节点', '0', 'happy[u]'],
            ['有子节点', 'Σmax(f[v][0],f[v][1])', 'happy[u]+Σf[v][0]'],
          ],
          tip: '📌 树形DP的核心：状态以"子树"为单位，DFS 后序处理（子节点先于父节点完成）。每个子节点独立对父节点贡献，不需要枚举断点，这是树形DP与区间DP的根本区别。',
        },

        {
          type:  'choice',
          lbl:   '状态',
          title: '❓ 选择：f[u][1] 的转移方程',
          q:
`树形DP中，f[u][1] 表示节点 u 参加舞会时，以 u 为根的子树中所有参加者的最大快乐值总和。

当 u 参加时，其子节点 v1, v2, ... 的状态应该怎么转移？`,
          opts: [
            'f[u][1] = happy[u] + sum{ max(f[v][0], f[v][1]) }（子节点自由选择）',
            'f[u][1] = happy[u] + sum{ f[v][0] }（子节点强制不参加）',
            'f[u][1] = happy[u] + sum{ f[v][1] }（子节点强制参加）',
            'f[u][1] = happy[u]（子节点不计入，只算自身）',
          ],
          ans:    1,
          fb_ok:  '✓ 正确。u 参加时，规则是"直属上司不能同时参加"，即 v 的上司 u 参加了，所以 v 不能参加。因此每个子节点 v 只能用 f[v][0]（v 不参加时子树的最优值），全部累加到 f[u][1]。',
          fb_err: '✗ 选项A：子节点自由选择是 f[u][0] 的转移逻辑（u不参加时子节点才自由）。选项C：子节点强制参加意味着上下级都参加，违反规则。选项D：只算自身忽略了子树的贡献，一棵非叶节点的子树中可能有其他可参加的员工。',
          hint:   '题目规定"上级参加则直接下级不能参加"。u 是 v 的直属上司，u 参加了，所以 v 只能取 f[v][0]。',
        },

        {
          type:  'choice',
          lbl:   '顺序',
          title: '❓ 选择：DFS 中何时更新 f[u]',
          q:
`树形DP用 DFS 实现，对节点 u 和其子节点 v，以下哪种更新顺序是正确的？`,
          opts: [
            '先更新 f[u]（用 happy[u] 初始化），再 DFS(v)，最后把 f[v] 合并到 f[u]',
            '先 DFS(v) 完成所有子节点，再根据子节点结果更新 f[u]',
            'DFS(v) 之前先读取 f[v] 的值合并到 f[u]，DFS(v) 用于继续向下递归',
            '先设 f[u][0]=f[u][1]=INF，DFS(v) 后再取最小值',
          ],
          ans:    1,
          fb_ok:  '✓ 正确。后序遍历：先递归处理所有子节点（DFS(v)），子节点的 f[v] 值计算完成后，再把它们合并到父节点 f[u]。这是 DP "子问题先于当前问题被解决"的树上体现。',
          fb_err: '✗ 选项A：在 DFS(v) 之前就合并 f[v] 是错误的，此时 v 的子树尚未处理，f[v] 还是初始值（0），不是正确的子树最优值。选项C：DFS(v) 之前读取 f[v] 同样是未计算完的值。选项D：初始化为 INF 且用最小值没有意义，f 表示最大快乐值，应初始化为确定值（f[u][0]=0, f[u][1]=happy[u]）再做最大值累加。',
          hint:   '想想为什么叫"后序遍历"——后序就是"左子树→右子树→根节点"的访问顺序，根节点的处理在所有子节点之后。',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：树形DP核心DFS',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`#include <bits/stdc++.h>
using namespace std;
int happy[6005], f[6005][2];
vector<int> children[6005]; // children[u] = u的所有子节点

void dfs(int u) {
    // [A] 初始化当前节点
    f[u][0] = 0;
    f[u][1] = happy[u];

    for (int v : children[u]) {
        // [B] 先递归处理子节点
        dfs(v);
        // 合并子节点的结果
        // [C] u不参加时：子节点v可以自由选择
        f[u][0] += max(f[v][0], f[v][1]);
        // u参加时：子节点v强制不参加
        f[u][1] += f[v][0];
    }
}

int main() {
    int n; cin >> n;
    for (int i = 1; i <= n; i++) cin >> happy[i];
    // 读边并建树（略）
    dfs(1); // 从根节点开始
    cout << max(f[1][0], f[1][1]) << endl;
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'f[u][0] = 0; f[u][1] = happy[u]',
            'f[u][0] = happy[u]; f[u][1] = 0',
            'dfs(v)',
            'dfs(u)',
            'f[u][0] += max(f[v][0], f[v][1])',
            'f[u][0] += f[v][1]',
          ],
          answers: [
            'f[u][0] = 0; f[u][1] = happy[u]',
            'dfs(v)',
            'f[u][0] += max(f[v][0], f[v][1])',
          ],
          fb_ok:  '✓ A：f[u][0]=0（u不参加，子树初始贡献0，等待子节点合并）；f[u][1]=happy[u]（u参加，先算上自身快乐值）。B：dfs(v) 先递归处理子节点，保证 f[v] 已经是子树的正确最优值。C：f[u][0] 合并时每个子节点自由选择，取 max(f[v][0],f[v][1])。',
          fb_err: '✗ A 若初始化反过来（f[u][0]=happy[u]）：语义错误，"不参加"状态不应包含自身快乐值。B 若写 dfs(u)：无限递归，应递归子节点 v 而不是当前节点 u。C 若写 f[u][0]+=f[v][1]：u 不参加时子节点只能参加（f[v][1]），漏掉了"子节点也不参加"的可能，答案偏小。',
          hint:   '初始化时把 u 自身的贡献放在 f[u][1] 里（参加才有快乐值），f[u][0]=0 等待子节点累加。DFS 必须先走子节点。',
        },
      ]
    },

    // ══════════════════════════════════════════════
    // 锐士关：理解陷阱——建树方式与根节点确定
    // ══════════════════════════════════════════════
    pro: {
      rank: '锐士',
      coins: 80,
      xp: 200,
      steps: [

        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 树形DP的两个实现细节',
          body:
`细节一：如何确定根节点（题目给的不一定是有根树）

题目给出 n-1 条"上下级关系"边，可以按如下方式找根：
  方法：记录每个节点是否有父节点（即是否出现在"下级"的位置）。
  没有父节点的那个节点就是根。

  具体做法：
  读入每条边 (a, b)，表示 a 是 b 的上司，将 b 的父节点设为 a，建立 children[a].push_back(b)。
  最后找没有父节点的节点作为根（is_child[b]=true，根是 is_child[root]=false 的那个）。

细节二：邻接表建树 vs 建有向树

  如果题目只给无向边（不告诉谁是父谁是子），可以任选一个根，用无向邻接表建图，
  DFS 时传入 parent 参数避免走回头路：

  void dfs(int u, int parent) {
      for (int v : adj[u]) {
          if (v == parent) continue; // 不走回头路
          dfs(v, u);
          // 合并 f[v] 到 f[u]
      }
  }`,
          concept:
`完整建树代码（"没有上司的舞会"原题格式）：

  int par[6005]; // par[i] = i的父节点，0表示根
  vector<int> children[6005];

  // 读入关系
  for (int i = 0; i < n-1; i++) {
      int l, k; cin >> l >> k; // l是k的下属
      children[k].push_back(l);
      par[l] = k;
  }

  // 找根节点
  int root = 1;
  for (int i = 1; i <= n; i++)
      if (par[i] == 0) { root = i; break; }

  dfs(root);
  cout << max(f[root][0], f[root][1]) << endl;

注意：某些题目的输入格式是"子节点 父节点"而不是"父节点 子节点"，读题时要仔细区分，否则建树方向反了，整个DP都是错的。`,
          tip: '📌 树形DP的常见实现bug：①建树方向反了（父子搞反）；②DFS忘记传parent导致走回头路（无向图）；③在DFS(v)之前就读取f[v]（子树未计算完）。这三个bug几乎覆盖了90%的树形DP运行时错误。',
        },

        {
          type:  'choice',
          lbl:   '建树',
          title: '❓ 选择：如何找到树的根节点',
          q:
`输入 n-1 条有向边，每条边格式为"a b"表示 a 是 b 的直属上司（即 a→b 的父子关系，b 的父节点是 a）。以下哪种方式能正确找到根节点？`,
          opts: [
            '读入所有边后，出度最大的节点是根',
            '读入边 (a,b) 时记录 has_parent[b]=true，最后找 has_parent[i]==false 的节点',
            '节点编号最小的是根节点',
            '第一条边的第一个数字是根节点',
          ],
          ans:    1,
          fb_ok:  '✓ 正确。根节点没有父节点，在所有"a是b的上司"的输入中，b 都有父节点（has_parent[b]=true），只有根节点从未作为 b 出现，has_parent[root] 仍为 false。',
          fb_err: '✗ 选项A（出度最大）：出度最大是子节点最多的节点，不一定是根（如链状树中所有中间节点出度为1，根也是1）。选项C（编号最小）：题目没有保证根编号最小，这只是某些特殊题目的约定。选项D（第一条边第一个数）：输入顺序不保证根最先出现。',
          hint:   '根节点的定义：没有父节点的节点。"a 是 b 的上司"表示 b 有父节点 a，所以记录谁出现在 b 的位置，剩下没出现过的就是根。',
        },

        {
          type:  'choice',
          lbl:   '合并',
          title: '❓ 选择：f[u][0] 和 f[u][1] 的含义辨析',
          q:
`树形DP中，对于以下这棵树：
  节点1（快乐值5）是根，子节点为2（快乐值3）和3（快乐值4）。
  节点2和节点3都是叶节点。

手动计算 f[1][0] 和 f[1][1]，结果是？`,
          opts: [
            'f[1][0]=7, f[1][1]=5（不参加时选最优子节点，参加时只算自身）',
            'f[1][0]=7, f[1][1]=8（不参加时两子节点都参加取最大，参加时加两子节点不参加值）',
            'f[1][0]=7, f[1][1]=5（参加时子节点不能参加，f[v][0]=0）',
            'f[1][0]=3+4=7, f[1][1]=5+0+0=5',
          ],
          ans:    2,
          fb_ok:  '✓ 正确。叶节点：f[2][0]=0,f[2][1]=3；f[3][0]=0,f[3][1]=4。f[1][0]=max(f[2][0],f[2][1])+max(f[3][0],f[3][1])=max(0,3)+max(0,4)=3+4=7；f[1][1]=happy[1]+f[2][0]+f[3][0]=5+0+0=5。答案=max(7,5)=7（选择节点2和3参加，不选根节点）。',
          fb_err: '✗ 选项A和D：f[1][1]=5 是对的（u参加时子节点不能参加，f[v][0]=0），f[1][0]=7 也是对的，但选项B说 f[1][1]=8 是错的——u=1参加了，子节点v2和v3不能参加，只能用f[v][0]=0，1的快乐值5+0+0=5。选项C和本正确选项内容一致但表述上 f[1][1]=5 的来源说法有误——答案选项C虽然数值对，但选项B数值错误(8)，正确答案是C即f[1][0]=7,f[1][1]=5。注意选项标记：正确选项是第三个(ans:2,0-indexed)，即f[1][0]=7, f[1][1]=5（参加时子节点不能参加）。',
          hint:   '一步步算：先算叶节点（f[leaf][0]=0,f[leaf][1]=happy[leaf]），再合并到根。根参加时，每个子节点强制不参加（f[v][0]=0）。',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：完整树形DP（含建树和找根）',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`#include <bits/stdc++.h>
using namespace std;
int happy[6005], f[6005][2];
bool has_parent[6005];
vector<int> children[6005];

void dfs(int u) {
    f[u][0] = 0; f[u][1] = happy[u];
    for (int v : children[u]) {
        dfs(v);
        // [A] u不参加时，子节点自由选择
        f[u][0] += max(f[v][0], f[v][1]);
        // [B] u参加时，子节点不能参加
        f[u][1] += f[v][0];
    }
}

int main() {
    int n; cin >> n;
    for (int i = 1; i <= n; i++) cin >> happy[i];
    for (int i = 0; i < n - 1; i++) {
        int a, b; cin >> a >> b; // a是b的上司
        children[a].push_back(b);
        // [C] 记录b有父节点
        has_parent[b] = true;
    }
    int root = 1;
    for (int i = 1; i <= n; i++)
        if (!has_parent[i]) { root = i; break; }
    dfs(root);
    cout << max(f[root][0], f[root][1]) << endl;
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'f[u][0] += max(f[v][0], f[v][1])',
            'f[u][0] += f[v][1]',
            'f[u][1] += f[v][0]',
            'f[u][1] += max(f[v][0], f[v][1])',
            'has_parent[b] = true',
            'has_parent[a] = true',
          ],
          answers: [
            'f[u][0] += max(f[v][0], f[v][1])',
            'f[u][1] += f[v][0]',
            'has_parent[b] = true',
          ],
          fb_ok:  '✓ A：u不参加，子节点v自由选择，取max(f[v][0],f[v][1])累加。B：u参加，子节点v强制不参加，取f[v][0]累加。C：边(a,b)表示a是b的上司，b是下级，b有父节点，记录has_parent[b]=true。',
          fb_err: '✗ A 若写 f[v][1]：u不参加时子节点只能参加，漏掉了子节点也不参加的情况。B 若写 max(f[v][0],f[v][1])：u参加时子节点可以自由选择，违反题目规则。C 若写 has_parent[a]=true：a 是上司（父节点），记录 a 有父节点是错误的，应记录下级 b 有父节点。',
          hint:   'A 和 B 的区别完全对应转移方程：f[u][0] 子节点自由；f[u][1] 子节点强制不参加。C 的方向：谁是"下级"谁才有父节点，b 是下级。',
        },
      ]
    },

    // ══════════════════════════════════════════════
    // 英雄关：f[u][0]和f[u][1]的含义及DFS回溯过程
    // ══════════════════════════════════════════════
    hero: {
      rank: '英雄',
      coins: 120,
      xp: 280,
      steps: [

        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 DFS回溯过程：子节点如何向父节点汇总',
          body:
`英雄关把 DFS 回溯时"子节点信息逐步汇总到父节点"的过程讲清楚。

以这棵树为例（快乐值在括号内）：
       1(5)
      /    \\
    2(3)   3(4)
   /   \\
  4(2)  5(1)

DFS(1) 调用过程：
  1. DFS(1) 初始化：f[1][0]=0, f[1][1]=5，然后遍历子节点
  2. 调用 DFS(2)
     2.1. DFS(2) 初始化：f[2][0]=0, f[2][1]=3，遍历子节点
     2.2. 调用 DFS(4)：叶节点，f[4][0]=0, f[4][1]=2，返回
     2.3. 合并4到2：f[2][0]+=max(0,2)=2；f[2][1]+=f[4][0]=0+0=0（f[2][1]=3+0=3）
     2.4. 调用 DFS(5)：叶节点，f[5][0]=0, f[5][1]=1，返回
     2.5. 合并5到2：f[2][0]+=max(0,1)=1（f[2][0]=2+1=3）；f[2][1]+=0（f[2][1]=3）
     2.6. DFS(2) 完成：f[2][0]=3, f[2][1]=3
  3. 合并2到1：f[1][0]+=max(3,3)=3；f[1][1]+=f[2][0]=0+3=3（f[1][1]=5+3=8）
  4. 调用 DFS(3)：叶节点，f[3][0]=0, f[3][1]=4，返回
  5. 合并3到1：f[1][0]+=max(0,4)=4（f[1][0]=3+4=7）；f[1][1]+=0（f[1][1]=8+0=8）
  6. 最终：f[1][0]=7, f[1][1]=8，答案=max(7,8)=8（选1、4、5参加）`,
          concept:
`DFS 回溯的本质：自底向上的信息传递

  · 叶节点：直接从初始值出发，f[leaf][0]=0, f[leaf][1]=happy[leaf]
  · 每次 DFS(v) 返回后，v 的子树已经处理完，f[v][0] 和 f[v][1] 是子树的最优值
  · 把 f[v] 合并到 f[u]：实际上是"把子树 v 的最优决策加入到以 u 为根的子树答案中"
  · 所有子节点合并完毕后，f[u][0] 和 f[u][1] 就是以 u 为根的整棵子树的最优值

时间复杂度：
  每条边被访问一次（父→子），每个节点被处理一次。
  总时间 O(n)，空间 O(n)（递归栈深度等于树的高度，最坏 O(n)）。

树形DP的通用模式：
  1. 初始化当前节点的 dp 值（用自身属性）
  2. DFS 每个子节点（保证子树处理完）
  3. 把子节点的 dp 值合并到当前节点（"汇总"步骤）
  4. 当前节点的 dp 值完成`,
          tip: '📌 树形DP中每个节点只被处理一次，总复杂度 O(n)。相比之下，区间DP每个状态 O(n) 转移，总复杂度 O(n^3)。树形结构天然避免了重叠，这是树形DP高效的根本原因。',
        },

        {
          type:  'choice',
          lbl:   '回溯',
          title: '❓ 选择：合并子节点时 f[u][1] 的累加顺序',
          q:
`节点 u 的快乐值为 10，有三个子节点 v1(快乐值3)、v2(快乐值5)、v3(快乐值2)，都是叶节点。
执行 DFS(u) 后，f[u][1] 的最终值是多少？`,
          opts: [
            '10（只算 u 自身快乐值，子节点不参加所以不贡献）',
            '20（u 参加 + 三个子节点都参加）',
            '10 + 0 + 0 + 0 = 10（u 参加，子节点强制不参加，f[vi][0]=0）',
            '10 + max(0,3) + max(0,5) + max(0,2) = 20',
          ],
          ans:    2,
          fb_ok:  '✓ 正确。f[u][1] = happy[u] + f[v1][0] + f[v2][0] + f[v3][0] = 10 + 0 + 0 + 0 = 10。三个子节点都是叶节点，f[vi][0]=0（叶节点不参加快乐值为0）。选项A和C说的是同一件事——f[u][1]=10，两者等价。',
          fb_err: '✗ 选项B（20）：u 参加时子节点不能参加，不能把子节点快乐值加进来。选项D（20）：max(f[v][0],f[v][1]) 是 f[u][0] 的转移逻辑，不是 f[u][1]。u 参加时子节点固定用 f[vi][0]=0。',
          hint:   'f[u][1] 的转移：u参加后，对每个子节点v，只能用 f[v][0]（v不参加的最优值）。叶节点的 f[leaf][0]=0，所以三个子节点贡献都是0。',
        },

        {
          type:  'choice',
          lbl:   '扩展',
          title: '❓ 选择：树形DP的状态可以扩展到几维',
          q:
`"没有上司的舞会"用了 f[u][0/1] 两个状态。若题目改成：
  · 员工有三种状态：不参加(0)、普通参加(1)、VIP参加(2)
  · 约束变为：若父节点是VIP，则子节点不能VIP；若父节点普通参加，子节点不能参加
则状态应该怎么扩展？`,
          opts: [
            '无法扩展，树形DP只支持0/1两种状态',
            '把第二维改成三状态：f[u][0]、f[u][1]、f[u][2]，重新设计转移方程',
            '用 f[u][0..2] 和 f[v][0..2] 枚举所有父子状态组合（共9种），选满足约束的最优',
            '新增一个额外的布尔数组记录是否是VIP',
          ],
          ans:    1,
          fb_ok:  '✓ 正确。树形DP的状态维度可以根据题意自由扩展。f[u][0]=不参加的最优值，f[u][1]=普通参加，f[u][2]=VIP参加。对每个子节点v，根据u的状态确定v的允许状态集合，在允许范围内取最优合并。这是树形DP状态扩展的通用思路。',
          fb_err: '✗ 选项A：树形DP可以有任意多的状态维度，只受内存和时间限制，不限于0/1。选项C：描述了一种枚举方式，但这不是"状态扩展"的准确表述——实际上就是对每种父节点状态，枚举子节点的合法状态取最优，本质与选项B相同。选项D：额外数组不改变DP结构，没有解决如何设计转移方程的问题。',
          hint:   '树形DP的状态第二维可以是任意离散的"当前节点的状态"。题目中有几种状态，第二维就有几个值，转移方程根据约束重新设计。',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：验证DFS回溯——手动追踪一棵小树',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 树结构：1(5)->2(3)->4(2), 1(5)->2(3)->5(1), 1(5)->3(4)
// 预期结果：f[4]={0,2}, f[5]={0,1}, f[2]={3,3}, f[3]={0,4}, f[1]={7,8}
// 答案 = max(f[1][0], f[1][1]) = max(7, 8) = 8

#include <bits/stdc++.h>
using namespace std;
int happy[] = {0, 5, 3, 4, 2, 1}; // 1-indexed
int f[6][2];
vector<int> ch[] = {{},{2,3},{4,5},{},{},{}}; // 预设树结构

void dfs(int u) {
    f[u][0] = 0;
    // [A] 初始化f[u][1]为自身快乐值
    f[u][1] = happy[u];
    for (int v : ch[u]) {
        dfs(v);
        // [B] u不参加：子节点自由选择
        f[u][0] += max(f[v][0], f[v][1]);
        // [C] u参加：子节点强制不参加
        f[u][1] += f[v][0];
    }
}

int main() {
    dfs(1);
    cout << max(f[1][0], f[1][1]) << endl; // 输出8
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'f[u][1] = happy[u]',
            'f[u][1] = 0',
            'f[u][0] += max(f[v][0], f[v][1])',
            'f[u][0] += f[v][0]',
            'f[u][1] += f[v][0]',
            'f[u][1] += max(f[v][0], f[v][1])',
          ],
          answers: [
            'f[u][1] = happy[u]',
            'f[u][0] += max(f[v][0], f[v][1])',
            'f[u][1] += f[v][0]',
          ],
          fb_ok:  '✓ 三处填空与新兵关完全一致，英雄关用这棵具体的树验证理解。追踪一遍：dfs(4)→f[4]={0,2}；dfs(5)→f[5]={0,1}；dfs(2)合并4和5→f[2][0]=max(0,2)+max(0,1)=3，f[2][1]=3+f[4][0]+f[5][0]=3+0+0=3；dfs(3)→f[3]={0,4}；dfs(1)合并2和3→f[1][0]=max(3,3)+max(0,4)=3+4=7，f[1][1]=5+f[2][0]+f[3][0]=5+3+0=8。答案max(7,8)=8。',
          fb_err: '✗ 若A写0：f[u][1]初始就丢失了u自身的快乐值，最终答案偏小。若B写f[v][0]：u不参加时子节点只能不参加，漏掉了子节点参加的可能，f[1][0]会算成0+0=0而非7。若C写max：u参加时子节点自由选择，违反约束，f[2][1]会算成3+max(0,2)+max(0,1)=6而非3，f[1][1]会变成11而非8。',
          hint:   '按照DFS调用顺序逐步追踪每个节点的f值，验证最终f[1][0]=7,f[1][1]=8，答案8。',
        },
      ]
    },
  }
};
