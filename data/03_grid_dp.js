// data/02_triangle.js
// 章节：02.数字三角形 · 自底向上递推入门

export const LESSON = {
  id: 'triangle',
  title: '02.数字三角形',
  sub: '自底向上递推入门',

  battles: {

    // ── 新兵关 ────────────────────────────────────────────────
    new: {
      rank: '新兵', coins: 50, xp: 120,
      steps: [
        {
          type: 'concept',
          lbl: '读题',
          title: '📖 数字三角形：从塔顶走到塔底',
          body: `给定一个 n 行的数字三角形，从顶端出发，每步只能走到下一行相邻的两个数字之一（正下方或右下方），求路径上数字之和的最大值。

示例三角形（n=5）：
    7
   3 8
  8 1 0
 2 7 4 4
4 5 2 6 5

最优路径：7→3→8→7→5，总和 = 30。`,
          concept: `状态定义：
  dp[i][j] = 从位置 (i,j) 出发，走到塔底的最大路径和

为什么从底往上推？
  塔底每个格子的 dp 值就是它自身，最简单。
  越往上依赖越少算好的下层结果，正是 DP 的思路。

状态转移方程：
  dp[i][j] = a[i][j] + max(dp[i+1][j], dp[i+1][j+1])

边界：最后一行 dp[n-1][j] = a[n-1][j]

答案：dp[0][0]`,
          table: [
            ['n', '1', '2', '3', '4', '5'],
            ['该行格子数', '1', '2', '3', '4', '5'],
            ['j 的范围', '0', '0~1', '0~2', '0~3', '0~4'],
          ],
          tip: '📌 从 (i,j) 向下只能到 (i+1,j) 或 (i+1,j+1)，不是 (i+1,j-1) 和 (i+1,j)。',
        },
        {
          type: 'choice',
          lbl: '概念',
          title: '❓ 选择：dp[i][j] 的含义',
          q: `在自底向上的写法中，dp[i][j] 表示什么？`,
          opts: [
            '从塔顶 (0,0) 走到 (i,j) 的最大路径和',
            '从 (i,j) 出发走到塔底的最大路径和',
            '位置 (i,j) 这个格子本身的数值',
            '从 (i,j) 走到塔底的路径总条数',
          ],
          ans: 1,
          fb_ok: '✓ 正确！dp[i][j] 是一个"往下看"的值：站在 (i,j) 往下走能拿到的最大和，包含 a[i][j] 本身。所以答案是 dp[0][0]，即从塔顶出发的最大收益。',
          fb_err: '✗ 注意递推方向：dp[i][j] 依赖 dp[i+1][...]，说明它描述的是"从该位置出发往下"的结果，不是"从塔顶走到这里"的累计值。',
          hint: '看转移方程 dp[i][j] = a[i][j] + max(dp[i+1][j], dp[i+1][j+1])，它依赖下方的 dp 值，说明 dp 描述的是"往下走"的结果。',
        },
        {
          type: 'choice',
          lbl: '推导',
          title: '❓ 选择：手动推导一格',
          q: `三角形如下，最后一行 dp[4] = [4, 5, 2, 6, 5]。

    7
   3 8
  8 1 0
 2 7 4 4
4 5 2 6 5

请问 dp[3][1]（第4行数字7那个位置）等于多少？`,
          opts: [
            '7 + max(dp[4][1], dp[4][2]) = 7 + max(5,2) = 12',
            '7 + max(dp[4][0], dp[4][1]) = 7 + max(4,5) = 12',
            '7 + dp[4][1] + dp[4][2] = 7+5+2 = 14',
            '7 + max(dp[4][2], dp[4][3]) = 7 + max(2,6) = 13',
          ],
          ans: 0,
          fb_ok: '✓ 正确！dp[3][1] = a[3][1] + max(dp[4][1], dp[4][2]) = 7 + max(5,2) = 12。从 (3,1) 向下：正下方是 (4,1)=5，右下方是 (4,2)=2，取较大的5。',
          fb_err: '✗ 从 (i,j) 向下走，正下方是 (i+1, j)，右下方是 (i+1, j+1)。所以 (3,1) 对应的是 dp[4][1] 和 dp[4][2]，不是 dp[4][0] 和 dp[4][1]。',
          hint: '从 (i,j) 出发，下一行能到的两个位置下标是 j 和 j+1，不是 j-1 和 j。',
        },
        {
          type: 'fill',
          lbl: '代码',
          title: '✏️ 填空：自底向上基础版',
          q: '点击代码中的 [?] 方框选中，再点候选项填入：',
          code: `int n;
int a[N][N], dp[N][N];

int solve() {
    for (int j = 0; j < n; j++)
        dp[n-1][j] = [A];        // 边界：塔底 dp 值就是自身

    for (int i = n-2; i >= 0; i--)
        for (int j = 0; j <= i; j++)
            dp[i][j] = a[i][j] + [B];   // 向下两格取较大值

    return dp[0][0];
}`,
          blanks: ['A', 'B'],
          opts: [
            'a[n-1][j]',
            '0',
            'max(dp[i+1][j], dp[i+1][j+1])',
            'max(dp[i-1][j], dp[i-1][j+1])',
          ],
          answers: ['a[n-1][j]', 'max(dp[i+1][j], dp[i+1][j+1])'],
          fb_ok: '✓ 满分！A：塔底位置无路可走，dp 值就是格子本身 a[n-1][j]。B：从 (i,j) 向下，正下方 (i+1,j) 和右下方 (i+1,j+1) 取较大者。',
          fb_err: '✗ A 填 0 是错的，dp 表示含本格的路径和，塔底必须包含自身数值。B 用 i-1 方向错了，自底向上推时新的 dp[i] 依赖已算好的 dp[i+1]，是下方而非上方。',
          hint: 'A：边界 dp[n-1][j] 该存什么？B：转移时依赖下一行还是上一行？',
        },
      ]
    },

    // ── 锐士关 ────────────────────────────────────────────────
    pro: {
      rank: '锐士', coins: 80, xp: 200,
      steps: [
        {
          type: 'concept',
          lbl: '优化',
          title: '📖 空间优化：一维滚动数组',
          body: `新兵关用了二维 dp[N][N]。

观察：计算 dp[i][j] 时，只用到了 dp[i+1][...] 这一行。
更早的行算完后就再也用不到了。

结论：只需要一个一维数组，反复覆盖即可。

原始：dp[i][j] = a[i][j] + max(dp[i+1][j], dp[i+1][j+1])
优化：dp[j]    = a[i][j] + max(dp[j],       dp[j+1])

把二维的 i 这一维"滚掉"。`,
          concept: `遍历方向分析：

更新 dp[j] 时，需要读取旧的 dp[j] 和旧的 dp[j+1]。
写入位置是 dp[j]，读取位置包含 dp[j+1]（右边）。

只要保证写 dp[j] 时，dp[j+1] 还没被本轮覆盖，就是安全的。

结论：j 从小到大遍历。
  处理 j 时，j+1 还没被本轮更新，旧值仍然有效。

注意：这和 0/1 背包的"j 从大到小"方向相反！
  背包依赖 dp[j-w]（左边），三角形依赖 dp[j+1]（右边）。`,
          table: [
            ['写法', '空间复杂度', '关键点'],
            ['二维 dp[N][N]', 'O(n²)', '直观，易理解'],
            ['一维 dp[N]（j从小到大）', 'O(n)', '保护右边旧值'],
            ['0/1背包一维（j从大到小）', 'O(W)', '保护左边旧值'],
          ],
          tip: '📌 滚动数组的核心：分析依赖方向，确保被依赖的值在本轮不被提前覆盖。',
        },
        {
          type: 'choice',
          lbl: '陷阱',
          title: '❓ 选择：一维 dp 的遍历方向',
          q: `把二维 dp 压成一维后，转移是：
dp[j] = a[i][j] + max(dp[j], dp[j+1]);

内层循环 j 应该如何遍历？`,
          opts: [
            'j 从大到小：for (j = i; j >= 0; j--)',
            'j 从小到大：for (j = 0; j <= i; j++)',
            '从大到小或从小到大都正确',
            '必须双向各扫一遍才正确',
          ],
          ans: 1,
          fb_ok: '✓ 正确！写 dp[j] 时需要旧的 dp[j+1]（右边）还在。j 从小到大时，dp[j+1] 还没被本轮触碰，旧值得以保留，结果正确。',
          fb_err: '✗ j 从大到小时，dp[j+1] 会先被写入新值，等算 dp[j] 时拿到的是已经污染的新值。注意这和 0/1 背包方向相反：背包依赖左边 dp[j-w]，三角形依赖右边 dp[j+1]。',
          hint: '写 dp[j] 时需要保护谁的旧值？dp[j+1] 在左边还是右边？j 从哪个方向走能保住它？',
        },
        {
          type: 'choice',
          lbl: '对比',
          title: '❓ 选择：自顶向下 vs 自底向上',
          q: `自底向上写法循环是 for (i = n-2; i >= 0; i--)，i 从大到小。
自顶向下递归写法是 solve(0, 0)，从 (0,0) 开始调用。

两种写法的答案一样吗？差别在哪里？`,
          opts: [
            '答案不同，自顶向下会算出错误结果',
            '答案相同，但自顶向下不加记忆化会重复计算导致超时',
            '答案相同，自顶向下更快因为省去了很多不必要的格子',
            '答案相同，且两者时间复杂度完全一致不需要记忆化',
          ],
          ans: 1,
          fb_ok: '✓ 正确！两种写法计算的是完全相同的状态，答案一样。但自顶向下如果不加记忆化数组，solve(i,j) 会被反复调用，时间复杂度从 O(n²) 退化为 O(2^n)，n=30 时就会超时。',
          fb_err: '✗ 两种写法等价，答案相同。关键差别是：自底向上天然不会重复计算（每个格子只算一次）；自顶向下必须加记忆化才能保证每个状态只算一次。',
          hint: '想想 solve(2,1) 会被 solve(1,0) 和 solve(1,1) 各调用一次，如果不记忆化会怎样？',
        },
        {
          type: 'fill',
          lbl: '代码',
          title: '✏️ 填空：一维空间优化版',
          q: '点击代码中的 [?] 方框选中，再点候选项填入：',
          code: `int n;
int a[N][N];
int dp[N];

int solve() {
    for (int j = 0; j < n; j++)
        dp[j] = a[n-1][j];          // 初始化最后一行

    for (int i = n-2; i >= 0; i--)
        for ([A])                    // j 从小到大保护右边旧值
            dp[j] = a[i][j] + [B];  // 依赖旧 dp[j] 和旧 dp[j+1]

    return dp[0];
}`,
          blanks: ['A', 'B'],
          opts: [
            'int j = 0; j <= i; j++',
            'int j = i; j >= 0; j--',
            'max(dp[j], dp[j+1])',
            'max(dp[j], dp[j-1])',
          ],
          answers: ['int j = 0; j <= i; j++', 'max(dp[j], dp[j+1])'],
          fb_ok: '✓ 完美！A：j 从小到大，确保 dp[j+1] 在被读取时仍是上一轮旧值。B：依赖正下方 dp[j] 和右下方 dp[j+1]，方向正确。',
          fb_err: '✗ A 若从大到小，dp[j+1] 会被先覆盖成新值，结果出错。B 若用 dp[j-1] 方向错了，三角形向下只能到 j 和 j+1（同列或右下），不存在 j-1（左下）。',
          hint: 'A 和 B 互相关联：B 决定了要保留谁的旧值，A 决定了遍历顺序能否保住那个旧值。',
        },
      ]
    },

    // ── 英雄关 ────────────────────────────────────────────────
    hero: {
      rank: '英雄', coins: 120, xp: 280,
      steps: [
        {
          type: 'concept',
          lbl: '换角度',
          title: '📖 自顶向下记忆化搜索',
          body: `同一个问题，可以用递归+记忆化来写：

solve(i, j) = 从 (i,j) 走到塔底的最大路径和

  如果 memo[i][j] 已算过，直接返回
  如果 i == n-1，返回 a[i][j]（塔底边界）
  否则返回 a[i][j] + max(solve(i+1,j), solve(i+1,j+1))

和自底向上递推计算的是完全相同的状态，答案一样。
只是驱动方式不同：递归是"用到才算"，递推是"全部都算"。`,
          concept: `两种写法对比：

自顶向下（递归+记忆化）：
  优点：状态定义直接，思路自然
  缺点：有函数调用开销，必须开记忆化数组，否则指数爆炸
  适合：状态难以枚举顺序、初学理解 DP

自底向上（循环递推）：
  优点：常数小，易做空间优化（滚动数组），不会爆栈
  缺点：需要手动想清楚循环顺序
  适合：竞赛主流写法

等价性：两种写法的 dp 值完全一样，差别只在计算顺序。
  递归实际上也是从底往上填 memo，只是由调用栈驱动。`,
          table: [
            ['维度', '自顶向下', '自底向上'],
            ['思维方向', '从问题反推子问题', '从子问题搭建大问题'],
            ['触发计算', '用到才算', '全部都算'],
            ['代码形态', '递归函数', '嵌套循环'],
            ['空间优化', '不易做', '可做滚动数组'],
          ],
          tip: '📌 自顶向下不需要思考循环顺序，但必须加记忆化，否则退化为 O(2^n)。',
        },
        {
          type: 'choice',
          lbl: '理解',
          title: '❓ 选择：递归实际填表的顺序',
          q: `自底向上循环是 i 从 n-2 到 0（从下往上）。
自顶向下调用是 solve(0,0)（从顶开始）。

递归版本实际填写 memo 数组的顺序是什么？`,
          opts: [
            '从 (0,0) 开始往下填，和调用顺序一致',
            '从塔底往上填，因为递归要等子问题返回才能赋值',
            '随机顺序，取决于运行时的调用栈',
            '从左到右，每行依次填完再到下一行',
          ],
          ans: 1,
          fb_ok: '✓ 正确！solve(0,0) 调用 solve(1,0) 和 solve(1,1)，而它们又调用更深层，一直到塔底才返回赋值。memo 数组的赋值发生在回溯时，所以实际是从底层往上填，和自底向上循环一致。',
          fb_err: '✗ 递归的"调用方向"和"赋值方向"相反。调用是从 (0,0) 往下钻，但 memo[i][j] = ... 这行代码只有在 solve(i,j) 准备返回时才执行，所以赋值是从塔底往上完成的。',
          hint: '递归函数什么时候才会 return？它需要等所有子调用都返回之后，return 语句才能执行。',
        },
        {
          type: 'choice',
          lbl: '陷阱',
          title: '❓ 选择：去掉记忆化会怎样',
          q: `下面代码删掉了 memo，理由是"答案不变没必要存"：

int solve(int i, int j) {
    if (i == n-1) return a[i][j];
    return a[i][j] + max(solve(i+1,j), solve(i+1,j+1));
}

对于 n=30 的输入，结果如何？`,
          opts: [
            '答案正确，速度和加 memo 版本一样',
            '答案正确，但时间复杂度从 O(n²) 退化为 O(2^n)，会超时',
            '答案会出错，因为缺少 memo 导致状态被错误覆盖',
            '答案正确且更快，省去了查表开销',
          ],
          ans: 1,
          fb_ok: '✓ 正确！答案不变（递归逻辑正确），但每个 solve(i,j) 会被反复重新计算。整个调用树是二叉树形状，时间复杂度 O(2^n)。n=30 时约 10 亿次调用，必然超时。',
          fb_err: '✗ 没有 memo 仍能算出正确答案，但重叠子问题会被反复计算。这正是第01章强调的核心：记忆化把指数级变成多项式级。没有它，DP 就退化回了暴力递归。',
          hint: '回想 fib(n) 不加记忆化是 O(2^n)，数字三角形的 solve(i,j) 有几个子问题？会不会被重复调用？',
        },
        {
          type: 'fill',
          lbl: '代码',
          title: '✏️ 填空：自顶向下记忆化版',
          q: '点击代码中的 [?] 方框选中，再点候选项填入：',
          code: `int n;
int a[N][N];
int memo[N][N];   // -1 表示未算过

int solve(int i, int j) {
    if ([A]) return memo[i][j];          // 命中缓存直接返回
    if (i == n-1) return memo[i][j] = a[i][j];  // 塔底边界
    return memo[i][j] = a[i][j] + [B];  // 递归求两子问题取较大
}

int main() {
    memset(memo, -1, sizeof(memo));
    cout << solve(0, 0);
}`,
          blanks: ['A', 'B'],
          opts: [
            'memo[i][j] != -1',
            'memo[i][j] == -1',
            'max(solve(i+1, j), solve(i+1, j+1))',
            'max(solve(i-1, j), solve(i-1, j+1))',
          ],
          answers: ['memo[i][j] != -1', 'max(solve(i+1, j), solve(i+1, j+1))'],
          fb_ok: '✓ 完美！A：memo 初始为 -1，不等于 -1 说明已算过，直接复用。B：从 (i,j) 向下到 (i+1,j) 和 (i+1,j+1)，与自底向上转移方向完全一致。',
          fb_err: '✗ A 写成 == -1 含义反了，等于 -1 是没算过，这时不能直接 return。B 方向写成 i-1 错了，递归和递推的转移方向必须一致，都是向下（i+1）。',
          hint: 'A：memo[i][j] != -1 意味着已经算过了。B：和新兵关转移方程方向完全一致。',
        },
      ]
    },

  }
};