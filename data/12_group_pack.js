// data/12_group_pack.js
// 章节：12.分组背包 · 组内只能选一件

export const LESSON = {
  id:    'group_pack',
  title: '12.分组背包',
  sub:   '组内只能选一件',

  battles: {

    // ─────────────────────────────────────────────
    // 新兵：分组背包核心理解
    // ─────────────────────────────────────────────
    new: {
      rank: '新兵',
      coins: 50,
      xp: 120,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 分组背包问题',
          body:
`有 n 组物品和一个容量为 W 的背包。
第 i 组有 m[i] 件物品，组内第 j 件物品：重量 w[i][j]，价值 v[i][j]。
约束：每组最多选一件（也可以一件都不选）。
问：在不超过背包容量的前提下，能装入的最大总价值是多少？

与 0/1 背包的区别：
  0/1 背包：每件物品独立决策，选或不选
  分组背包：物品按组划分，每组内只能选一件（或不选）

例：W=10，共3组：
  第1组：(w=3,v=4)、(w=5,v=7)
  第2组：(w=4,v=6)、(w=2,v=3)、(w=6,v=9)
  第3组：(w=3,v=5)`,
          concept:
`状态定义：dp[j] 表示背包容量为 j 时的最大价值（滚动数组）。

转移方程：对每一组 i，逆序枚举容量 j，再枚举组内每件物品 k：
  dp[j] = max(dp[j], dp[j - w[i][k]] + v[i][k])

注意：dp[j] 的初始值已经包含"不选第 i 组"的情况（保留上一轮结果）。

循环结构（三层）：
  外层：枚举组 i（共 n 组）
  中层：逆序枚举容量 j（从 W 到 0）
  内层：枚举组 i 内每件物品 k

时间复杂度：O(n * W * max_m)，其中 max_m 是每组最大物品数。
边界：dp[0..W] 初始化为 0。`,
          table: [
            ['组', '物品', 'w', 'v', '说明'],
            ['第1组', '物品A', '3', '4', '二选一或不选'],
            ['第1组', '物品B', '5', '7', '二选一或不选'],
            ['第2组', '物品C', '4', '6', '三选一或不选'],
            ['第2组', '物品D', '2', '3', '三选一或不选'],
            ['第2组', '物品E', '6', '9', '三选一或不选'],
          ],
          tip: '📌 组内枚举必须放在容量逆序的内层，不能把容量枚举放在最内层——否则同一组的多件物品可能都被选入。',
        },

        {
          type:  'choice',
          lbl:   '约束',
          title: '❓ 选择：分组背包的核心约束是什么',
          q:     '以下哪句话最准确地描述了分组背包与0/1背包的核心区别？',
          opts: [
            '分组背包中每件物品只能选0次或1次，和0/1背包完全相同',
            '分组背包把物品分成若干组，每组内最多选一件（或不选），组间独立',
            '分组背包中每组物品必须恰好选一件，不能一件都不选',
            '分组背包是完全背包的特例，每组的物品可以重复选取',
          ],
          ans:    1,
          fb_ok:  '正确。分组背包的关键约束是"组内互斥"：每组最多选一件或不选，不同组之间独立决策。这与0/1背包（每件独立）、完全背包（可重复选）都不同。',
          fb_err: '选项A忽略了组的约束，单件独立选就是0/1背包。选项C"必须恰好选一件"过强，题目允许一组都不选。选项D"重复选取"是完全背包的特征。分组背包的核心是：每组内至多选一件，组间独立。',
          hint:   '想想"分组"这两个字的含义——组内的物品之间是什么关系？',
        },

        {
          type:  'choice',
          lbl:   '推导',
          title: '❓ 选择：分组背包手动推导',
          q:
`W=6，共2组：
  第1组：(w=2, v=3)、(w=4, v=6)
  第2组：(w=3, v=5)、(w=1, v=2)

初始 dp = [0,0,0,0,0,0,0]（下标0~6）
处理完第1组后，dp[6] 的值是多少？`,
          opts: [
            '6（选第1组的第一件，重量2价值3，再加一件凑满）',
            '6（选第1组的第二件，重量4价值6）',
            '9（两件都选，3+6=9）',
            '0（第1组没有物品能装进去）',
          ],
          ans:    1,
          fb_ok:  '正确。处理第1组时只能从两件中选一件：选(w=2,v=3)则 dp[6]=max(dp[6], dp[4]+3)=3；选(w=4,v=6)则 dp[6]=max(3, dp[2]+6)=6。最终 dp[6]=6。注意此时第2组还没处理，不能叠加第2组。',
          fb_err: '处理第1组时，dp 从全0出发，对容量6：选(w=2,v=3) → dp[6]=max(0,0+3)=3；选(w=4,v=6) → dp[6]=max(3,0+6)=6。选项C的9是把两件都选了，违反"组内至多一件"的约束。选项D错误，第1组有物品可装。',
          hint:   '逆序枚举容量，对第1组的每件物品更新 dp[6]。注意不能同时选两件。',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：分组背包核心代码',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 分组背包，dp[j]表示容量j时的最大价值
void groupPack(int n, int W,
    vector<vector<int>>& w, vector<vector<int>>& v) {
    vector<int> dp(W + 1, 0);
    for (int i = 0; i < n; i++) {         // 枚举每一组
        [A] {                              // 逆序枚举容量，防止组内多选
            for (int k = 0; k < (int)w[i].size(); k++) { // 枚举组内每件
                if (j >= w[i][k])
                    dp[j] = max(dp[j], [B]); // 选组i的第k件
            }
        }
    }
}`,
          blanks:  ['A', 'B'],
          opts: [
            'for (int j = W; j >= 0; j--)', 'for (int j = 0; j <= W; j++)',
            'dp[j - w[i][k]] + v[i][k]',    'dp[j - w[i][k]] + v[i][k] * 2',
          ],
          answers: ['for (int j = W; j >= 0; j--)', 'dp[j - w[i][k]] + v[i][k]'],
          fb_ok:   '正确。A 处逆序枚举容量：保证每组内最多选一件，不会在同一轮中叠加同组多件物品。B 处转移：从容量 j-w[i][k] 的最优状态加上第 k 件的价值 v[i][k]。',
          fb_err:  'A 处：正序枚举 j 会让同一组内的多件物品都被选入（类似完全背包的错误），必须逆序。B 处：价值就是 v[i][k]，乘以2没有依据。',
          hint:    'A 想想0/1背包为什么要逆序——分组背包同理；B 就是标准的"选这件物品后的价值"。',
        },
      ],
    },

    // ─────────────────────────────────────────────
    // 锐士：循环嵌套陷阱与边界
    // ─────────────────────────────────────────────
    pro: {
      rank: '锐士',
      coins: 80,
      xp: 200,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 分组背包的循环陷阱',
          body:
`分组背包最经典的错误：把三层循环的顺序写错。

正确顺序：组 → 容量（逆序） → 组内物品
错误顺序A：组 → 组内物品 → 容量（逆序）   ← 容量放最内层
错误顺序B：容量（逆序） → 组 → 组内物品   ← 组放中间

错误顺序A 的问题：
  内层是容量逆序，但此时已经把所有物品都处理完了，
  每次更新 dp[j] 时，dp[j-w[i][k]] 可能已经被同一组的
  另一件物品更新过，导致同组多件物品都被计入。

错误顺序B 的问题：
  容量在最外层，每个容量独立更新，但组间的依赖关系
  被打乱，无法保证"对每组完整决策一次"的语义。

正确写法的直觉：
  每次处理完整的一组，组内枚举所有物品候选，
  对同一个容量 j 取最优——这才叫"组内选一"。`,
          concept:
`两种常见错误写法对比：

错误写法（容量放最内层）：
  for i in 0..n:
    for k in 0..m[i]:
      for j = W downto w[i][k]:
        dp[j] = max(dp[j], dp[j-w[i][k]] + v[i][k])
  问题：k循环在外，j循环在内，等于对组i每件物品
  分别跑了一次0/1背包，组内多件可能同时入选。

正确写法（容量在中层）：
  for i in 0..n:
    for j = W downto 0:
      for k in 0..m[i]:
        dp[j] = max(dp[j], dp[j-w[i][k]] + v[i][k])
  保证：对同一个容量 j，组内所有物品候选只更新一次，
  dp[j] 的初始值（不选该组）天然保留。

记忆口诀：组 包 组内（组-背包容量-组内物品）。`,
          table: [
            ['循环顺序', '是否正确', '错误原因'],
            ['组 → 容量逆序 → 组内', '正确', '组内选一，语义清晰'],
            ['组 → 组内 → 容量逆序', '错误', '同组多件物品可能叠加'],
            ['容量逆序 → 组 → 组内', '错误', '组间决策语义被打乱'],
          ],
          tip: '📌 判断循环顺序是否正确的方法：对同一个 dp[j]，检查组内是否只有一件物品的贡献能被选入，不选等于保留上一轮的 dp[j] 值。',
        },

        {
          type:  'choice',
          lbl:   '陷阱',
          title: '❓ 选择：错误循环顺序的后果',
          q:
`下面这段代码把循环顺序写成了"组 → 组内物品 → 容量逆序"：

  for i in 0..n:
    for k in 0..m[i]:
      for j = W downto w[i][k]:
        dp[j] = max(dp[j], dp[j-w[i][k]] + v[i][k])

这段代码的实际效果是什么？`,
          opts: [
            '等价于正确的分组背包，结果相同',
            '对第 i 组的每件物品分别跑了一次0/1背包，组内多件可能同时入选',
            '相当于完全背包，每件物品可以无限选',
            '只会选组内最后一件物品，之前的物品全部被覆盖',
          ],
          ans:    1,
          fb_ok:  '正确。这段代码对第 i 组的每件物品 k 分别执行了一次逆序0/1背包更新。每次更新时，dp 已经包含了同组前面物品的贡献，所以同组多件物品可以叠加入选，违反了"组内至多一件"的约束。',
          fb_err: '这不等价于正确写法（A错）。也不是完全背包（C错），因为容量是逆序的，单件不会无限选。选项D错误，每件物品都独立更新了dp，不是覆盖。正确分析：k在外、j在内，等于对每件物品单独跑0/1背包，组内多件会叠加。',
          hint:   '想想这段代码里，处理组内第2件物品时，dp 已经被第1件物品更新过了——这意味着什么？',
        },

        {
          type:  'choice',
          lbl:   '边界',
          title: '❓ 选择：不选当前组的情况如何保证',
          q:
`在正确的分组背包代码中：

  for j = W downto 0:
    for k in 0..m[i]:
      if j >= w[i][k]:
        dp[j] = max(dp[j], dp[j-w[i][k]] + v[i][k])

"不选第 i 组"这个决策是在哪里体现的？`,
          opts: [
            '需要额外写一行 dp[j] = dp[j]，显式保留不选的情况',
            '内层 for k 循环结束后，dp[j] 自动清零表示不选',
            'dp[j] 在进入 k 循环前已有上一组处理完的值，max 操作天然保留了不选的情况',
            '只有当组内所有物品都装不下时，才等价于不选该组',
          ],
          ans:    2,
          fb_ok:  '正确。进入第 i 组的处理前，dp[j] 保存的是前 i-1 组决策后的最优值。内层 max(dp[j], ...) 中的 dp[j] 本身就是"不选第 i 组"的结果，不需要显式处理。',
          fb_err: '选项A多此一举，dp[j]=dp[j] 什么都没做。选项B错误，dp[j] 不会清零。选项D不全面，即使能装下，dp[j] 也可能因为选了性价比更低的物品而保持原值。正确理解：max 操作的左操作数 dp[j] 本身就是不选该组的价值。',
          hint:   'max(dp[j], dp[j-w[i][k]]+v[i][k]) 这个表达式里，左边的 dp[j] 代表什么决策？',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：分组背包（修正循环顺序版）',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 分组背包，注意三层循环的正确嵌套顺序
int groupPackCorrect(int n, int W,
    vector<vector<int>>& w, vector<vector<int>>& v) {
    vector<int> dp(W + 1, 0);
    for (int i = 0; i < n; i++) {    // 第一层：枚举组
        [A] {                         // 第二层：逆序枚举容量（组外）
            for (int k = 0; k < (int)w[i].size(); k++) { // 第三层：组内枚举
                if ([B])              // 容量足够装第k件
                    dp[j] = max(dp[j], dp[j - w[i][k]] + v[i][k]);
            }
        }
    }
    return dp[W];
}`,
          blanks:  ['A', 'B'],
          opts: [
            'for (int j = W; j >= 0; j--)', 'for (int j = 0; j <= W; j++)',
            'j >= w[i][k]',                  'j > w[i][k]',
          ],
          answers: ['for (int j = W; j >= 0; j--)', 'j >= w[i][k]'],
          fb_ok:   '正确。A 处逆序枚举容量放在中层（组外、组内物品之外），确保每组只贡献一件物品。B 处条件是 j >= w[i][k]，等号必须保留——当 j 恰好等于物品重量时可以装入，漏掉等号会错失这种情况。',
          fb_err:  'A 处：正序枚举会让同组多件叠加，必须逆序。B 处：j > w[i][k] 漏掉了 j == w[i][k] 的情况，当背包容量恰好等于物品重量时本可以装入，写成大于号会错过这个选择。',
          hint:    'A 想想为什么0/1背包要逆序——分组背包同理；B 注意"不超过容量"的边界，等号该不该要？',
        },
      ],
    },

    // ─────────────────────────────────────────────
    // 英雄：与0/1背包循环结构对比，组内枚举的正确嵌套
    // ─────────────────────────────────────────────
    hero: {
      rank: '英雄',
      coins: 120,
      xp: 280,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 分组背包 vs 0/1背包：循环结构对比',
          body:
`把0/1背包和分组背包的循环结构并排对比，看清本质联系。

0/1背包（每件物品独立）：
  for i in 0..n:                   // 枚举物品
    for j = W downto w[i]:         // 逆序枚举容量
      dp[j] = max(dp[j], dp[j-w[i]] + v[i])

分组背包（每组选一件）：
  for i in 0..n:                   // 枚举组
    for j = W downto 0:            // 逆序枚举容量
      for k in 0..m[i]:            // 枚举组内物品
        if j >= w[i][k]:
          dp[j] = max(dp[j], dp[j-w[i][k]] + v[i][k])

关键洞察：
  0/1背包 = 每组只有1件物品的分组背包
  分组背包 = 0/1背包 + 内层多了一层"组内物品枚举"

两者的逆序容量逻辑完全相同，目的都是防止同一决策单元被重复选取。`,
          concept:
`为什么说"0/1背包是分组背包的特例"？

  0/1背包：第 i 件物品单独成组，组内只有1件可选。
    选 → dp[j] = dp[j-w[i]] + v[i]
    不选 → dp[j] = dp[j]（保留）

  分组背包：第 i 组有 m[i] 件，组内选价值最大的一件。
    选第k件 → dp[j] = dp[j-w[i][k]] + v[i][k]
    不选该组 → dp[j] = dp[j]（保留）

两种背包的共同点：
  1. 每个"决策单元"（物品/组）只能贡献一次
  2. 容量逆序保证这一点
  3. dp[j] 初始值天然表示"不选当前决策单元"

分组背包只是把"一件物品"替换成"一组中的最优一件"。`,
          table: [
            ['对比项', '0/1背包', '分组背包'],
            ['决策单元', '单件物品', '一组物品（选其中一件）'],
            ['循环层数', '2层（物品+容量）', '3层（组+容量+组内）'],
            ['容量方向', '逆序', '逆序（相同）'],
            ['不选的体现', 'dp[j]保留上轮值', 'dp[j]保留上轮值（相同）'],
            ['可选数量', '每件0或1次', '每组0或1件'],
          ],
          tip: '📌 理解分组背包的最快方法：把每一组想象成0/1背包里的一件"超级物品"，它的候选重量和价值有多个，取其中最划算的一个。',
        },

        {
          type:  'choice',
          lbl:   '对比',
          title: '❓ 选择：0/1背包与分组背包的本质关系',
          q:     '以下哪个说法最准确地描述了0/1背包与分组背包的关系？',
          opts: [
            '两者没有关系，状态定义和转移方程完全不同',
            '分组背包是0/1背包的特例，每组只有1件时退化为0/1背包',
            '0/1背包是分组背包的特例，每组只有1件时退化为0/1背包',
            '分组背包只是0/1背包把逆序改成正序的变种',
          ],
          ans:    2,
          fb_ok:  '正确。0/1背包可以看作每组恰好只有1件物品的分组背包，分组背包是更通用的模型。反过来说，分组背包在每组 m[i]=1 时，内层 k 循环只执行一次，完全等价于0/1背包。',
          fb_err: '选项A错误，两者有明确的特例关系。选项B方向反了，应该是0/1背包是分组背包的特例，而不是反过来。选项D错误，分组背包容量同样逆序，和0/1背包一样，不需要改成正序。',
          hint:   '想想当分组背包中每组只有1件物品时，三层循环退化成几层？结果和哪种背包一样？',
        },

        {
          type:  'choice',
          lbl:   '嵌套',
          title: '❓ 选择：组内枚举放在容量循环外面会怎样',
          q:
`有人把分组背包写成下面这样（组内枚举在外，容量在内）：

  for i in 0..n:
    for k in 0..m[i]:
      for j = W downto w[i][k]:
        dp[j] = max(dp[j], dp[j-w[i][k]] + v[i][k])

与正确写法（容量在外，组内在内）相比，这段代码等价于什么？`,
          opts: [
            '等价于正确的分组背包，只是写法不同',
            '等价于对第i组的每件物品单独跑一次0/1背包，组内多件可同时入选',
            '等价于完全背包，每件物品可以无限次选取',
            '等价于多重背包，每组内每件物品最多选 m[i] 次',
          ],
          ans:    1,
          fb_ok:  '正确。这段代码对第i组的 m[i] 件物品逐件做了0/1背包更新，每件更新时 dp 已包含同组前面物品的贡献，组内多件物品可以叠加入选。本质上变成了"组内 m[i] 件物品的0/1背包"，而非"选一件"。',
          fb_err: '选项A错误，这两种写法结果不同。选项C错误，容量是逆序的，单件不会无限选。选项D错误，这不是多重背包（多重背包是同种物品多件）。正确分析：k在外j在内 = 对每件物品单独做0/1背包 = 组内多件可叠加。',
          hint:   '这段代码里，处理第 k 件物品时，dp 中已经有第 0~k-1 件物品的贡献了。这会导致什么？',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：分组背包 vs 0/1背包对比填空',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 分组背包完整代码，体现与0/1背包的结构对比
int groupPack(int n, int W,
    vector<vector<int>>& w, vector<vector<int>>& v) {
    vector<int> dp(W + 1, 0);
    for (int i = 0; i < n; i++) {
        // 容量逆序在中层，与0/1背包相同
        for (int j = W; j >= 0; j--) {
            // 组内枚举在最内层，这是分组背包特有的
            for (int k = 0; [A]; k++) { // 枚举组i内所有物品
                if (j >= w[i][k])
                    dp[j] = [B]; // 选组i第k件或保持不选
            }
        }
    }
    return dp[W];
}`,
          blanks:  ['A', 'B'],
          opts: [
            'k < (int)w[i].size()', 'k <= (int)w[i].size()',
            'max(dp[j], dp[j - w[i][k]] + v[i][k])', 'dp[j - w[i][k]] + v[i][k]',
          ],
          answers: ['k < (int)w[i].size()', 'max(dp[j], dp[j - w[i][k]] + v[i][k])'],
          fb_ok:   '正确。A 处 k < size() 用严格小于，防止越界访问（下标从0开始，最后一件是 size()-1）。B 处用 max(dp[j], ...) 而不是直接赋值：dp[j] 是不选该组的价值，max 保证从"选"和"不选"中取更优的结果。',
          fb_err:  'A 处：k <= size() 会越界访问 w[i][size()]，必须用严格小于。B 处：直接赋值 dp[j]=dp[j-w[i][k]]+v[i][k] 丢失了"不选该组"的选项，必须用 max 保留 dp[j] 原有的值。',
          hint:    'A 想想 vector 下标范围是 0 到 size()-1；B 想想分组背包里"不选该组"是怎么体现的。',
        },
      ],
    },
  },
};
