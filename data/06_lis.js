// data/06_lis.js
// 章节：06.LIS专题 · 最长递增子序列

export const LESSON = {
  id:    'lis',
  title: '06.LIS专题',
  sub:   '最长递增子序列',

  battles: {

    // ─────────────────────────────────────────────
    // 新兵：LIS核心理解
    // ─────────────────────────────────────────────
    new: {
      rank: '新兵',
      coins: 50,
      xp: 120,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 最长递增子序列',
          body:
`给定一个整数序列 a[0..n-1]，求其最长严格递增子序列的长度。

子序列：从原序列中选出若干元素，保持原来的相对顺序（不要求连续）。
严格递增：选出的每个元素都比前一个大（不能相等）。

例如 a = [3, 1, 4, 1, 5, 9, 2, 6]：
  子序列 [1, 4, 5, 9] 长度为4，严格递增
  子序列 [1, 4, 5, 6] 长度为4，严格递增
  子序列 [3, 4, 5, 9] 长度为4，严格递增
  最长递增子序列长度 = 4`,
          concept:
`状态定义：dp[i] 表示以 a[i] 结尾的最长递增子序列长度。

转移方程：
  dp[i] = max(dp[j] + 1)，对所有 j < i 且 a[j] < a[i]

含义：枚举 i 之前所有比 a[i] 小的位置 j，
把以 a[j] 结尾的最长子序列再接上 a[i]，取最大值。

边界：dp[i] = 1（至少以自己单独构成长度1的子序列）

最终答案：max(dp[0], dp[1], ..., dp[n-1])
注意：答案不一定是 dp[n-1]，因为最优子序列不一定以最后一个元素结尾。

计算顺序：i 从 0 到 n-1，内层 j 从 0 到 i-1。`,
          table: [
            ['i',    '0', '1', '2', '3', '4', '5', '6', '7'],
            ['a[i]', '3', '1', '4', '1', '5', '9', '2', '6'],
            ['dp[i]','1', '1', '2', '1', '3', '4', '2', '4'],
          ],
          tip: '📌 最终答案是对整个 dp 数组取 max，不是直接返回 dp[n-1]——LIS 可能在中间某个位置就结束了。',
        },

        {
          type:  'choice',
          lbl:   '状态',
          title: '❓ 选择：dp[i] 的准确含义',
          q:     '在LIS问题中，dp[i] 准确表示什么？',
          opts: [
            '整个序列前 i+1 个元素中最长递增子序列的长度',
            '以 a[i] 为结尾的最长递增子序列的长度',
            '以 a[i] 为开头的最长递增子序列的长度',
            '序列中第 i 小的元素在最长递增子序列中的位置',
          ],
          ans:    1,
          fb_ok:  '正确。dp[i] 强调"以 a[i] 为结尾"，这是关键约束——它保证了子序列的最后一个元素恰好是 a[i]，从而可以向后拼接。',
          fb_err: 'dp[i] 的关键词是"以 a[i] 结尾"，而不是"前 i+1 个元素中"（那样的话转移方程就不好写了），也不是"以 a[i] 开头"（那样需要从后往前算，且最终答案更难汇总）。',
          hint:   '想想转移时，为什么要求 a[j] < a[i]？这和 dp[i] 的"结尾"约束有什么关系？',
        },

        {
          type:  'choice',
          lbl:   '推导',
          title: '❓ 选择：手动推导 dp[4]',
          q:
`a = [3, 1, 4, 1, 5, 9, 2, 6]
已知 dp[0]=1, dp[1]=1, dp[2]=2, dp[3]=1。
计算 dp[4]（a[4]=5）时，枚举所有 j<4 且 a[j]<5 的位置：

  j=0: a[0]=3 < 5，dp[0]+1 = 2
  j=1: a[1]=1 < 5，dp[1]+1 = 2
  j=2: a[2]=4 < 5，dp[2]+1 = ?
  j=3: a[3]=1 < 5，dp[3]+1 = 2

dp[4] 等于多少？`,
          opts: [
            '2',
            '3',
            '4',
            '5',
          ],
          ans:    1,
          fb_ok:  '正确。j=2 时 dp[2]+1 = 2+1 = 3，是四个候选中最大的，所以 dp[4] = 3。',
          fb_err: 'j=2：dp[2]+1 = 2+1 = 3；其余 j 得到的都是 2。取最大值，dp[4] = 3。',
          hint:   '四个候选值分别是多少？取其中最大的那个。',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：LIS O(n²) 核心代码',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`int lis(vector<int>& a, int n) {
    vector<int> dp(n, 1); // 每个位置至少长度为1
    int ans = 1;
    for (int i = 1; i < n; i++) {
        for (int j = 0; j < i; j++) {
            if ([A]) { // 严格递增条件
                dp[i] = max(dp[i], [B]); // 尝试从j处接上a[i]
            }
        }
        ans = max(ans, dp[i]); // 随时更新全局最优
    }
    return ans;
}`,
          blanks:  ['A', 'B'],
          opts: [
            'a[j] < a[i]',  'a[j] <= a[i]',
            'dp[j] + 1',    'dp[j - 1] + 1',
          ],
          answers: ['a[j] < a[i]', 'dp[j] + 1'],
          fb_ok:   '正确。A 处用严格小于保证严格递增（等于不满足条件）；B 处在 dp[j] 的基础上加1，表示把 a[i] 接在以 a[j] 结尾的子序列后面。',
          fb_err:  'A 处：题目要求严格递增，必须用 < 而不是 <=（含等号会把相等的元素也算进去）。B 处：接上 a[i] 后长度加1，是 dp[j]+1 而不是 dp[j-1]+1（j-1 和当前枚举的 j 没有必然关系）。',
          hint:    'A 想想"严格"递增和"不降"递增的区别在哪个符号上；B 想想在 dp[j] 后面再接一个元素，长度变化是多少。',
        },
      ],
    },

    // ─────────────────────────────────────────────
    // 锐士：陷阱与边界
    // ─────────────────────────────────────────────
    pro: {
      rank: '锐士',
      coins: 80,
      xp: 200,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 LIS的常见陷阱',
          body:
`陷阱一：返回 dp[n-1] 而非 max(dp)

  a = [5, 4, 3, 2, 1]（严格递减序列）
  dp = [1, 1, 1, 1, 1]
  dp[n-1] = 1，正确。

  a = [1, 3, 2, 4]
  dp = [1, 2, 2, 3]
  dp[n-1] = 3，正确，但这是巧合——
  如果序列是 [1, 3, 4, 2]，dp = [1,2,3,2]，dp[n-1]=2 而答案是3！

陷阱二：用 <= 代替 <（把不降子序列当成递增子序列）

  a = [1, 1, 2]，用 <=：dp = [1, 2, 3]，答案3（错）
  正确用 < ：dp = [1, 1, 2]，答案2（对）

陷阱三：忘记 dp 初始化为1
  dp[i] 最小为1（自身单独成序列），若初始化为0会导致结果偏小。`,
          concept:
`三个陷阱的根源：

  陷阱一：LIS 不一定以最后元素结尾，需要对整个 dp 取 max。
  陷阱二：严格递增要求 a[j] < a[i]，不是 a[j] <= a[i]。
  陷阱三：dp[i] 初始值 = 1，代表"只选自己"这一种合法方案。

记忆口诀：
  初始化为1，条件用严格小于，答案取全局最大。`,
          table: [
            ['序列', 'dp数组', '错误写法 dp[n-1]', '正确答案 max(dp)'],
            ['[1,3,4,2]', '[1,2,3,2]', '2', '3'],
            ['[3,1,2,4]', '[1,1,2,3]', '3', '3（这次恰好相同）'],
            ['[4,3,2,1]', '[1,1,1,1]', '1', '1（这次也相同）'],
          ],
          tip: '📌 "答案取 max(dp) 而非 dp[n-1]"这一点在比赛中丢分率极高，每次写完都要检查返回值。',
        },

        {
          type:  'choice',
          lbl:   '陷阱',
          title: '❓ 选择：找出代码中的错误',
          q:
`下面的LIS代码有一个bug，请找出来：

  for (int i = 1; i < n; i++) {
      for (int j = 0; j < i; j++) {
          if (a[j] <= a[i])
              dp[i] = max(dp[i], dp[j] + 1);
      }
  }
  return dp[n - 1];

这段代码有哪些问题？`,
          opts: [
            '只有一个问题：条件应为 a[j] < a[i]（严格递增）',
            '只有一个问题：返回值应为 max(dp) 而非 dp[n-1]',
            '有两个问题：条件应为 a[j] < a[i]，且返回值应为 max(dp)',
            '代码没有问题，逻辑完全正确',
          ],
          ans:    2,
          fb_ok:  '正确。两个 bug 同时存在：<=  会把相等的元素纳入递增序列；dp[n-1] 不一定是全局最优，需要对整个 dp 数组取 max。',
          fb_err: '这段代码有两处错误：① a[j] <= a[i] 应改为 a[j] < a[i]（严格递增不允许相等）；② return dp[n-1] 应改为 return *max_element(dp.begin(), dp.end())，因为最优子序列不一定以最后元素结尾。',
          hint:   '回忆新兵关的两个核心点：严格递增的符号，以及返回值的选取。',
        },

        {
          type:  'choice',
          lbl:   '分析',
          title: '❓ 选择：序列 [2, 2, 2, 2] 的LIS长度',
          q:
`序列 a = [2, 2, 2, 2]，求最长严格递增子序列的长度。
（严格递增：每个元素必须严格大于前一个）`,
          opts: [
            '4（四个2全选）',
            '2（选两个2）',
            '1（任意选一个2）',
            '0（无法构成递增子序列）',
          ],
          ans:    2,
          fb_ok:  '正确。所有元素相等，严格递增要求 a[j] < a[i]，相等的元素无法拼接，所以每个 dp[i]=1，最终答案为1。',
          fb_err: '严格递增不允许相等。a[j] < a[i] 中，2 < 2 为假，任何两个位置都无法拼接。所有 dp[i] 都等于1，答案为1，不是4也不是2。',
          hint:   '严格递增和"不降"的区别就在相等时——两个相等的元素能接在一起吗？',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：修正后的完整LIS代码',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`int lis(vector<int>& a, int n) {
    vector<int> dp(n, 1);
    for (int i = 1; i < n; i++) {
        for (int j = 0; j < i; j++) {
            if (a[j] < a[i])
                dp[i] = max(dp[i], dp[j] + 1);
        }
    }
    return [A]; // 正确的返回方式：对全部dp取最大值
}

// 也可以在循环中维护答案：
int lis2(vector<int>& a, int n) {
    vector<int> dp(n, 1);
    int ans = [B]; // ans初始值：单个元素就是长度1的LIS
    for (int i = 1; i < n; i++) {
        for (int j = 0; j < i; j++) {
            if (a[j] < a[i])
                dp[i] = max(dp[i], dp[j] + 1);
        }
        ans = max(ans, dp[i]);
    }
    return ans;
}`,
          blanks:  ['A', 'B'],
          opts: [
            '*max_element(dp.begin(), dp.end())', 'dp[n - 1]',
            '1',                                  '0',
          ],
          answers: ['*max_element(dp.begin(), dp.end())', '1'],
          fb_ok:   '正确。A 处用 max_element 对整个 dp 数组求最大值，避免漏掉中间最优解；B 处初始为1，因为空序列时至少存在长度为1的子序列（任意单个元素）。',
          fb_err:  'A 处：dp[n-1] 只看最后位置，不是全局最优，应用 max_element 扫全数组。B 处：ans 初始值为1（单个元素即为长度1的LIS），初始化为0会在 n=1 时给出错误答案。',
          hint:    'A 的两个候选一个只看最后，一个看全部；B 想想只有一个元素时LIS长度是多少。',
        },
      ],
    },

    // ─────────────────────────────────────────────
    // 英雄：O(n²) vs 贪心+二分 O(nlogn)
    // ─────────────────────────────────────────────
    hero: {
      rank: '英雄',
      coins: 120,
      xp: 280,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 贪心+二分：O(nlogn) 求LIS长度',
          body:
`O(n²) 解法在 n=10000 时约 10^8 次操作，对竞赛来说偏慢。
O(nlogn) 解法：维护一个辅助数组 tails，表示长度为 k 的递增子序列的最小末尾元素。

对 a = [3, 1, 4, 1, 5, 9, 2, 6]，逐步处理：

  处理 3：tails = [3]
  处理 1：1 < tails[0]=3，替换 → tails = [1]
  处理 4：4 > tails[0]=1，追加 → tails = [1, 4]
  处理 1：1 <= tails[0]=1，替换 → tails = [1, 4]
  处理 5：5 > tails[1]=4，追加 → tails = [1, 4, 5]
  处理 9：9 > tails[2]=5，追加 → tails = [1, 4, 5, 9]
  处理 2：在 tails 中找第一个 >= 2 的位置（下标1），替换 → tails = [1, 2, 5, 9]
  处理 6：在 tails 中找第一个 >= 6 的位置（下标3），替换 → tails = [1, 2, 5, 6]

tails 长度 = 4，即LIS长度。`,
          concept:
`tails 数组的关键性质：

  tails[k] 存储的是：目前见到的所有长度为 k+1 的递增子序列中，末尾元素的最小值。
  tails 本身始终保持严格递增（可用二分查找）。

每次处理 a[i]：
  若 a[i] > tails 最后一个元素 → 追加，LIS 长度 +1
  否则 → 用二分找到 tails 中第一个 >= a[i] 的位置，替换为 a[i]

替换的意义：用更小的末尾元素，为后续拼接创造更多可能（贪心思想）。

注意：tails 不是某一条具体的LIS，它只保证长度正确。`,
          table: [
            ['处理元素', 'tails 变化', '操作'],
            ['3', '[3]',          '追加'],
            ['1', '[1]',          '替换下标0'],
            ['4', '[1,4]',        '追加'],
            ['1', '[1,4]',        '替换下标0（1>=1，无变化）'],
            ['5', '[1,4,5]',      '追加'],
            ['9', '[1,4,5,9]',    '追加'],
            ['2', '[1,2,5,9]',    '替换下标1'],
            ['6', '[1,2,5,6]',    '替换下标3'],
          ],
          tip: '📌 tails 不代表真实的LIS序列，只用来计算长度。若要还原具体方案，仍需回到 O(n²) DP 或额外记录父节点。',
        },

        {
          type:  'choice',
          lbl:   '对比',
          title: '❓ 选择：两种写法的核心区别',
          q:
`O(n²) DP 和 O(nlogn) 贪心+二分都能求LIS长度，
下列说法中哪一条是正确的？`,
          opts: [
            'O(nlogn) 写法的 tails 数组存储的是一条具体的最长递增子序列',
            'O(n²) 写法的 dp[i] 表示以 a[i] 结尾的LIS长度，O(nlogn) 写法的 tails 长度表示全局LIS长度',
            'O(nlogn) 写法只能处理严格递增，O(n²) 写法可以处理不降序列',
            '两种写法的空间复杂度都是 O(n²)',
          ],
          ans:    1,
          fb_ok:  '正确。dp[i] 记录"以 a[i] 结尾"的局部信息，最终需取 max；tails 的长度直接就是全局LIS长度，但 tails 本身不是一条真实的LIS序列。',
          fb_err: 'tails 不是真实的LIS序列，只是每种长度的最优末尾集合。两种写法都可以处理严格递增或不降（调整比较符号即可）。两种写法空间都是 O(n)。正确区别是：dp[i] 是局部（以 a[i] 结尾），tails 长度是全局LIS长度。',
          hint:   '想想 tails 经过替换操作后，它里面的元素还是原来序列里连续的吗？',
        },

        {
          type:  'choice',
          lbl:   '二分',
          title: '❓ 选择：贪心写法中二分的目标',
          q:
`处理元素 a[i] 时，在 tails 数组中进行二分查找，
查找的目标是什么位置？`,
          opts: [
            '第一个严格大于 a[i] 的位置',
            '最后一个小于 a[i] 的位置',
            '第一个大于等于 a[i] 的位置（lower_bound）',
            '第一个等于 a[i] 的位置',
          ],
          ans:    2,
          fb_ok:  '正确。用 lower_bound 找第一个 >= a[i] 的位置并替换，这样保证严格递增（等于的情况不追加，而是替换，防止把相等元素当作递增）。',
          fb_err: '需要找第一个 >= a[i] 的位置（lower_bound）：若 tails 中存在等于 a[i] 的值，替换它可以保持严格递增；若找 upper_bound（第一个 > a[i]）则会把相等的元素也视为可追加，破坏严格性。',
          hint:   '严格递增不允许相等，所以遇到等于 a[i] 的 tails 元素应该替换还是跳过？',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：O(nlogn) 贪心+二分代码',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`int lis_nlogn(vector<int>& a, int n) {
    vector<int> tails; // tails[k]：长度k+1的递增子序列的最小末尾
    for (int i = 0; i < n; i++) {
        // 在tails中找第一个 >= a[i] 的位置
        auto it = [A];
        if (it == tails.end()) {
            tails.push_back(a[i]); // a[i]最大，追加，LIS长度+1
        } else {
            [B]; // 替换为更小的末尾，贪心保留更优状态
        }
    }
    return tails.size(); // tails长度即为LIS长度
}`,
          blanks:  ['A', 'B'],
          opts: [
            'lower_bound(tails.begin(), tails.end(), a[i])', 'upper_bound(tails.begin(), tails.end(), a[i])',
            '*it = a[i]',                                    'tails.push_back(a[i])',
          ],
          answers: [
            'lower_bound(tails.begin(), tails.end(), a[i])',
            '*it = a[i]',
          ],
          fb_ok:   '正确。A 处用 lower_bound 找第一个 >= a[i] 的位置，处理严格递增（等于也替换，不追加）；B 处用 *it = a[i] 就地替换该位置的值，缩小该长度的末尾元素，为后续拼接留更大空间。',
          fb_err:  'A 处：upper_bound 找第一个 > a[i] 的位置，会把相等元素视为可追加，破坏严格递增；应用 lower_bound（第一个 >= a[i]）。B 处：找到替换位置后，用 *it = a[i] 原地替换，而不是 push_back（那会错误地延长 tails）。',
          hint:    'A 想想 lower_bound 和 upper_bound 分别找 >= 还是 >；B 找到位置 it 之后，是追加还是在原位替换？',
        },
      ],
    },
  },
};
