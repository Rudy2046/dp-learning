// data/28_prob_dp.js
// 章节：28.概率DP · 加权后继状态转移

export const LESSON = {
  id:    'prob_dp',
  title: '28.概率DP',
  sub:   '加权后继状态转移',

  battles: {
    new: {
      rank: '新兵', coins: 50, xp: 120,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 什么是概率DP',
          body:
`问题类型：在随机过程中，求某个事件发生的概率。

例题：一枚硬币正面朝上的概率为 p，反面为 1-p。
连续投 3 次，恰好出现 2 次正面的概率是多少？

这类问题可以直接用组合数学算，但当状态复杂时（如有多种随机操作、状态有约束），
就需要用 DP 来系统地计算各状态的概率。

概率DP 的核心思想：
  设 dp[state] = 处于某个状态 state 的概率。
  从初始状态出发，按照随机转移规则，
  把初始概率"分流"到各个后继状态，
  最终汇总到目标状态，得到答案。`,
          concept:
`概率DP 的框架：

状态定义：dp[state] = 到达该状态的概率

初始化：dp[初始状态] = 1.0（确定性地从初始状态出发）

转移（正向推进）：
  当前状态 s 以概率 p_i 转移到后继状态 t_i，则：
  dp[t_i] += dp[s] * p_i

含义：当前状态 s 的概率，按各个转移的概率"分流"给后继状态。

关键性质：
  所有状态的概率之和 = 1（概率守恒）
  每个状态转移出去的概率之和 = 1（概率分流不损失）

与普通DP的区别：
  普通DP：dp[s] = max 或 min（后继状态的最优值）
  概率DP：dp[t] += dp[s] * p（把概率乘以转移权重累加给后继）`,
          tip: '📌 概率DP 的转移是"乘法"（概率相乘）再"加法"（多条路径汇总），普通DP 的转移是"比大小"（取最优）。',
        },
        {
          type:  'choice',
          lbl:   '概念',
          title: '❓ 选择：概率转移的含义',
          q:
`掷一枚骰子（六面，点数 1~6 等概率），设 dp[k] 为"当前点数之和恰好为 k"的概率。
从状态 dp[3] = 0.2（当前和为 3，概率 0.2）出发，掷一次骰子后，
对 dp[4] 的贡献是多少？`,
          opts: [
            '0.2 * (1/6)，因为从 3 掷出 1 点到达 4 的概率是 1/6，乘上当前状态概率',
            '0.2 + (1/6)，因为概率要相加',
            '1/6，与 dp[3] 无关',
            '0.2 / 6，因为要把概率均分给 6 个后继状态',
          ],
          ans:     0,
          fb_ok:  '✓ 从状态 3 出发，掷出 1 点（概率 1/6）到达状态 4，这条路径的概率是 dp[3] * (1/6) = 0.2 * (1/6)。dp[4] 可能还有其他路径（如从状态 2 掷出 2 点），全部累加。',
          fb_err: '✗ 概率转移是相乘（路径的联合概率），不是相加；dp[3] 的值影响从 3 出发的这条路径的概率权重，必须乘进来；除以 6 是错误的——我们不是把 dp[3] 分摊给 6 个后继，而是每个后继都以 dp[3]*(1/6) 的概率被到达。',
          hint:   '"掷出 1 点到达 4"这条路径发生，需要同时满足"当前和为 3"AND"掷出 1"，两个独立事件的概率用乘法。',
        },
        {
          type:  'choice',
          lbl:   '推进',
          title: '❓ 选择：概率守恒',
          q:
`概率DP 正向推进时，某一轮"所有状态的概率之和"应该等于多少？`,
          opts: [
            '始终等于 1，概率不会无故增加或减少',
            '每轮都增加，因为状态数越来越多',
            '每轮都减少，因为概率在分流',
            '不一定，取决于转移的概率之和',
          ],
          ans:     0,
          fb_ok:  '✓ 概率总和守恒：无论经过多少次转移，所有可能状态的概率之和始终等于 1。这是概率的基本性质，也是验证代码正确性的好方法——每一步都可以加总验证是否还是 1.0。',
          fb_err: '✗ 概率是守恒的，不会凭空增加或减少。"分流"只是把概率从一个状态分配到多个后继，总量不变。如果某一步总和不等于 1，说明转移概率写错了。',
          hint:   '类比水流：一杯水从一个容器分流到多个容器，水的总量不变。概率就是这杯水。',
        },
        {
          type:  'fill',
          lbl:   '代码',
          title: '✏️ 填空：掷骰子恰好到达 n 的概率',
          q:     '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 从 0 出发，每次掷一枚六面骰子（1~6 等概率），求恰好到达位置 n 的概率
// 若超过 n 则本次无效（不累计）
int n;
double dp[1005]; // dp[i] = 恰好到达位置 i 的概率

void solve() {
    memset(dp, 0, sizeof(dp));
    // [A] 初始状态：在位置 0，概率为 1
    dp[0] = 1.0;

    for (int i = 0; i < n; i++) {
        if (dp[i] < 1e-12) continue; // 概率极小，跳过
        for (int d = 1; d <= 6; d++) {
            if (i + d > n) continue; // 超过 n，无效
            // [B] 从位置 i 以概率 1/6 转移到位置 i+d
            dp[i + d] += dp[i] * (1.0 / 6);
        }
    }
    // [C] 答案：恰好到达 n 的概率
    printf("%.6f\n", dp[n]);
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'dp[0] = 1.0;',
            'dp[0] = 0.0;',
            'dp[i + d] += dp[i] * (1.0 / 6);',
            'dp[i + d] = max(dp[i + d], dp[i] * (1.0 / 6));',
            'printf("%.6f\\n", dp[n]);',
            'printf("%.6f\\n", dp[n] * 6);',
          ],
          answers: [
            'dp[0] = 1.0;',
            'dp[i + d] += dp[i] * (1.0 / 6);',
            'printf("%.6f\\n", dp[n]);',
          ],
          fb_ok:  '✓ A：从位置 0 出发是确定的，概率为 1.0。B：概率转移用 +=（多条路径汇总），乘以转移概率 1/6；用 max 是普通最优DP的写法，概率DP不用 max。C：dp[n] 就是恰好到达 n 的概率，直接输出。',
          fb_err: '✗ A：初始概率是 1，不是 0；B：概率转移必须用 += 累加所有路径，用 max 只保留了最大值，含义完全错误；C：dp[n] 已经是概率值，乘以 6 没有意义。',
          hint:   '概率DP 的两个关键：初始状态概率为 1，转移用 += 累加（不是 max/min）。',
        },
      ],
    },

    pro: {
      rank: '锐士', coins: 80, xp: 200,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 逆向概率DP：从后往前推',
          body:
`新兵关学的是"正向"概率DP：从初始状态出发，把概率分流给后继状态。
但有些问题更适合"逆向"推：从目标状态出发，往前推每个状态到达目标的概率。

例题：从位置 0 出发，每步等概率向右走 1 步或 2 步，
求恰好经过位置 k（不能跳过）的概率。

正向需要追踪"是否经过 k"，状态会变复杂。
逆向定义：dp[i] = 从位置 i 出发，最终恰好经过 k 的概率，
初始化 dp[k]=1，dp[k+1]~dp[终点]=0（已过 k 无效），
往前递推 dp[i] = 0.5*dp[i+1] + 0.5*dp[i+2]。`,
          concept:
`正向 vs 逆向概率DP：

正向（顺推）：
  dp[state] = 从初始出发，到达该状态的概率
  初始化：dp[start] = 1
  转移：dp[next] += dp[cur] * p
  适用：关心"最终在哪个状态"的概率分布

逆向（逆推）：
  dp[state] = 从该状态出发，最终到达目标的概率
  初始化：dp[target] = 1，其余通常为 0
  转移：dp[cur] = sum(p_i * dp[next_i])（加权平均后继）
  适用：关心"从某个特定起点出发达到目标"的概率

选择技巧：
  如果初始状态唯一、目标状态多样 → 正向
  如果初始状态多样、目标状态唯一 → 逆向（或正向然后汇总）
  两种方式在数学上等价，选更自然的那种。`,
          tip: '📌 逆向概率DP 的转移是"加权平均"：dp[cur] = p1*dp[next1] + p2*dp[next2]。这和期望DP（第29章）非常像——区别在于 dp 的含义是概率还是期望步数。',
        },
        {
          type:  'choice',
          lbl:   '逆向',
          title: '❓ 选择：逆向概率DP的转移方向',
          q:
`逆向概率DP 中，dp[i] 表示"从位置 i 出发到达目标的概率"。
已知从位置 i 可以以概率 0.4 走到位置 i+1，以概率 0.6 走到位置 i+2。
dp[i] 的转移方程是什么？`,
          opts: [
            'dp[i] = 0.4 * dp[i+1] + 0.6 * dp[i+2]',
            'dp[i+1] += dp[i] * 0.4，dp[i+2] += dp[i] * 0.6',
            'dp[i] = max(dp[i+1], dp[i+2])',
            'dp[i] = dp[i+1] * dp[i+2]',
          ],
          ans:     0,
          fb_ok:  '✓ 逆向DP：dp[i] 由后继状态推出，等于各后继状态"到达目标概率"的加权平均。走到 i+1（概率 0.4）和走到 i+2（概率 0.6），分别对应 dp[i+1] 和 dp[i+2]，加权求和。',
          fb_err: '✗ 第二个选项是正向DP的写法（把概率分流给后继），方向反了；取 max 是最优DP的写法；概率相乘是"两件事同时发生"，这里是"两条互斥路径之一"，应用加权求和。',
          hint:   '逆向DP：当前状态的值 = 各后继状态值的加权平均（权重是转移概率）。想象站在 i，有 0.4 的可能性经历 dp[i+1] 的命运，有 0.6 的可能性经历 dp[i+2] 的命运。',
        },
        {
          type:  'choice',
          lbl:   '陷阱',
          title: '❓ 选择：概率DP的浮点精度',
          q:
`概率DP 通常用 double 存储概率值。以下哪种做法更合适？`,
          opts: [
            '用 1e-9 作为浮点数"等于 0"的判断阈值，而不是直接用 == 0.0',
            '把所有概率乘以 10^9 转成整数，用整数DP',
            '只保留小数点后两位，四舍五入到 0.01',
            '使用 long double 彻底避免精度问题',
          ],
          ans:     0,
          fb_ok:  '✓ 浮点数不能用 == 精确比较，应该用一个极小的阈值（如 1e-9 或 1e-12）来判断"是否约等于 0"。这是概率DP中常见的精度处理技巧。',
          fb_err: '✗ 把概率转成整数会丢失大量精度（概率可以非常小）；保留两位小数精度太低，题目通常要求更高；long double 精度更高但并不"彻底避免"精度问题，且与浮点比较的问题无关。',
          hint:   '任何浮点数的比较，都不要用 ==，要用 abs(a-b) < eps 这类阈值判断。概率DP 中跳过极小概率状态用 dp[i] < 1e-12 来判断。',
        },
        {
          type:  'fill',
          lbl:   '代码',
          title: '✏️ 填空：逆向概率DP——恰好踩到位置 k',
          q:     '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 从位置 0 出发，每步等概率走 1 或 2 格，求恰好踩到位置 k 的概率
// 逆向DP：dp[i] = 从位置 i 出发，最终恰好踩到 k 的概率
int k, n; // 目标位置 k，终点 n（n >= k）
double dp[1005];

void solve() {
    memset(dp, 0, sizeof(dp));
    // [A] 目标状态：在 k 就是已经踩到，概率为 1
    dp[k] = 1.0;
    // k+1 ~ n：已经越过 k，无法回头，dp 保持 0

    // 从 k-1 往前逆推
    for (int i = k - 1; i >= 0; i--) {
        // [B] 从 i 出发，等概率走 1 格或 2 格
        dp[i] = 0.5 * dp[i + 1] + 0.5 * dp[i + 2];
    }
    // [C] 答案：从位置 0 出发恰好踩到 k 的概率
    printf("%.6f\n", dp[0]);
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'dp[k] = 1.0;',
            'dp[k] = 0.5;',
            'dp[i] = 0.5 * dp[i + 1] + 0.5 * dp[i + 2];',
            'dp[i] += 0.5 * dp[i + 1]; dp[i] += 0.5 * dp[i + 2];',
            'printf("%.6f\\n", dp[0]);',
            'printf("%.6f\\n", dp[k]);',
          ],
          answers: [
            'dp[k] = 1.0;',
            'dp[i] = 0.5 * dp[i + 1] + 0.5 * dp[i + 2];',
            'printf("%.6f\\n", dp[0]);',
          ],
          fb_ok:  '✓ A：已经在 k，一定踩到，概率为 1.0，不是 0.5。B：逆向DP 的转移是加权平均，两条路各 0.5，写成 = 赋值（逆推时 dp[i] 从 0 开始，没有别的路径贡献）。C：答案是从出发点 0 出发的概率，是 dp[0] 而非 dp[k]。',
          fb_err: '✗ A：dp[k]=1 表示"在目标状态一定能到达目标"，是边界定义，不是 0.5；B：逆向DP 每个状态只有唯一的转移方程，用 = 赋值，不需要 +=（正向DP 才用 +=，因为多条路径汇聚到同一个后继）；C：问的是从 0 出发的概率，dp[0] 才是答案。',
          hint:   '逆向DP：边界在目标（dp[k]=1），推向起点；每个状态用 = 赋值（加权平均后继），答案在 dp[0]。',
        },
      ],
    },

    hero: {
      rank: '英雄', coins: 120, xp: 280,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 概率DP与普通DP的本质区别',
          body:
`英雄关从"转移方程的本质"来对比概率DP和普通DP。
表面上看，它们都在枚举后继状态并做某种计算，
但背后的数学含义完全不同。`,
          concept:
`普通DP（以最短路为例）：
  dp[state] = 到达该状态的最优代价
  转移：dp[cur] = min(dp[prev] + cost)
  含义：选一条最优路径，代价取最小值。
  只有一条路径最终"起作用"，其他路径被丢弃。

概率DP：
  dp[state] = 到达该状态的概率（或从该状态出发到达目标的概率）
  转移：dp[next] += dp[cur] * p（正向）
        dp[cur] = sum(p_i * dp[next_i])（逆向）
  含义：所有路径同时存在，每条路径有自己的概率权重，
        最终是所有路径的概率之和（正向）或加权平均（逆向）。
  每条路径都有贡献，没有路径被丢弃。

关键对比：
  普通DP：竞争关系（选最优，其余丢弃），操作是 min/max
  概率DP：叠加关系（所有路径同时存在），操作是加权求和

这也解释了为什么：
  普通DP 的 dp 值是确定的最优解，
  概率DP 的 dp 值是 [0, 1] 之间的实数（概率），
  所有状态的 dp 值之和 = 1（概率守恒）。`,
          tip: '📌 看到"min/max"就是普通DP；看到"加权求和 / += * 概率"就是概率DP。两者的核心操作不同，背后数学含义也截然不同。',
        },
        {
          type:  'choice',
          lbl:   '对比',
          title: '❓ 选择：转移操作的本质区别',
          q:
`普通最短路DP 和概率DP 都有"枚举后继状态"的步骤。
以下哪句话最准确地描述了二者的核心区别？`,
          opts: [
            '普通DP 只保留最优路径（竞争/取舍），概率DP 保留所有路径（叠加/加权）',
            '普通DP 用整数，概率DP 用浮点数',
            '普通DP 是正向推进，概率DP 是逆向推进',
            '普通DP 只有一个最优解，概率DP 有多个解',
          ],
          ans:     0,
          fb_ok:  '✓ 这是最本质的区别：普通DP 的 min/max 操作意味着"选一条最优路径，丢弃其余"；概率DP 的 += 意味着"所有路径同时存在，各自贡献自己的概率权重"。数据类型和推进方向都只是表象。',
          fb_err: '✗ 用整数还是浮点数只是实现细节，不是本质；概率DP 也可以正向推进（新兵关就是正向的）；"多个解"的说法不准确，概率DP 的答案仍然是一个确定的概率值。',
          hint:   '想想 min 操作和 += 操作分别代表什么："我只要最好的那条路"vs"所有路的贡献我都要"。',
        },
        {
          type:  'choice',
          lbl:   '验证',
          title: '❓ 选择：如何验证概率DP的代码是否写对',
          q:
`写完一段概率DP代码后，有一种快速验证正确性的方法。
以下哪种方法最有效？`,
          opts: [
            '对所有状态的 dp 值求和，检查是否等于 1（概率守恒）',
            '把 dp 数组中的最大值输出，看是否合理',
            '把转移概率全部改成 1，看输出是否变成 1',
            '对比暴力枚举的结果（只验证 n 很小时的情况）',
          ],
          ans:     0,
          fb_ok:  '✓ 概率守恒是概率DP最基本的性质：任何时刻，所有互斥状态的概率之和等于 1。若求和不等于 1（允许浮点误差），说明转移概率写错或漏了某些状态。这是快速自查的好方法。',
          fb_err: '✗ 最大值没有固定含义；把转移概率全改成 1 会破坏模型结构；对比暴力是一种有效验证手段，但需要单独写暴力代码，不如概率守恒检查简单快捷。两种方法都可以用，但在比赛中概率守恒检查最快。',
          hint:   '概率DP 有一个天然的内置"校验器"：所有状态的概率之和必须等于 1。这个性质不需要对比答案就能验证。',
        },
        {
          type:  'fill',
          lbl:   '代码',
          title: '✏️ 填空：两种写法对比——掷骰子到达 n',
          q:     '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 正向概率DP：dp[i] = 恰好到达位置 i 的概率
// 逆向概率DP：dp[i] = 从位置 i 出发恰好到达 n 的概率
// 每步掷骰子走 1~6 格，等概率

// === 正向写法 ===
double dp_fwd[1005];
void forward(int n) {
    memset(dp_fwd, 0, sizeof(dp_fwd));
    dp_fwd[0] = 1.0;
    for (int i = 0; i < n; i++) {
        if (dp_fwd[i] < 1e-12) continue;
        for (int d = 1; d <= 6 && i + d <= n; d++)
            // [A] 正向：把当前概率分流给后继状态
            dp_fwd[i + d] += dp_fwd[i] / 6.0;
    }
    printf("forward: %.6f\n", dp_fwd[n]);
}

// === 逆向写法 ===
double dp_bwd[1005];
void backward(int n) {
    memset(dp_bwd, 0, sizeof(dp_bwd));
    // [B] 逆向初始化：在目标位置 n，概率为 1
    dp_bwd[n] = 1.0;
    for (int i = n - 1; i >= 0; i--) {
        // [C] 逆向：当前状态 = 后继状态的加权平均
        for (int d = 1; d <= 6 && i + d <= n; d++)
            dp_bwd[i] += dp_bwd[i + d] / 6.0;
    }
    printf("backward: %.6f\n", dp_bwd[0]);
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'dp_fwd[i + d] += dp_fwd[i] / 6.0;',
            'dp_fwd[i + d] = max(dp_fwd[i + d], dp_fwd[i] / 6.0);',
            'dp_bwd[n] = 1.0;',
            'dp_bwd[n] = 1.0 / 6.0;',
            'dp_bwd[i] += dp_bwd[i + d] / 6.0;',
            'dp_bwd[i] = max(dp_bwd[i], dp_bwd[i + d] / 6.0);',
          ],
          answers: [
            'dp_fwd[i + d] += dp_fwd[i] / 6.0;',
            'dp_bwd[n] = 1.0;',
            'dp_bwd[i] += dp_bwd[i + d] / 6.0;',
          ],
          fb_ok:  '✓ A：正向用 +=，把当前状态的概率乘以转移概率累加给后继，体现"分流"。B：逆向的边界是目标状态概率为 1（确定在目标就能到达目标）。C：逆向也用 +=，枚举各个后继并累加加权贡献，体现"加权平均"——两种写法的 += 含义略有不同但结果等价。',
          fb_err: '✗ A：max 是最优DP的操作，概率DP永远用 +=；B：逆向边界 dp_bwd[n]=1，不是 1/6（1/6 是转移概率，不是边界值）；C：max 会只保留最大的一个后继概率，丢弃了其他路径的贡献，逆向DP必须用 += 把所有后继的加权贡献累加起来。',
          hint:   '正向和逆向都用 +=（前者分流，后者汇集）；逆向边界是"在终点=1"；概率DP 里永远不用 max/min。',
        },
      ],
    },
  },
};
