// data/13_pack_var.js
// 章节：13.背包模型变形 · 方案数·恰好凑齐·最小价值

export const LESSON = {
  id:    'pack_var',
  title: '13.背包模型变形',
  sub:   '方案数·恰好凑齐·最小价值',

  battles: {

    // ─────────────────────────────────────────────
    // 新兵：方案数背包
    // ─────────────────────────────────────────────
    new: {
      rank: '新兵',
      coins: 50,
      xp: 120,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 背包变形一：统计方案数',
          body:
`经典0/1背包问题：最多装多少价值？
变形问题：恰好装满容量 W 的背包，有多少种选法？

例：W=5，三件物品重量分别为 2、3、5。
  选法1：只选重量5的物品（5=5）
  选法2：选重量2和重量3的物品（2+3=5）
  共2种方案。

变形的本质：
  原版：dp[j] = 最大价值，转移取 max
  方案数：dp[j] = 恰好凑成重量j的方案数，转移取累加

注意"恰好"与"至多"的区别：
  至多装满：初始化 dp[0]=0，dp[1..W]=0（正常0/1背包）
  恰好凑满：初始化 dp[0]=1（空集是一种方案），dp[1..W]=0`,
          concept:
`状态定义：dp[j] 表示恰好凑成重量 j 的方案数。

初始化：dp[0] = 1，dp[1..W] = 0。
  dp[0]=1 的含义：什么都不选，恰好重量为0，这是1种方案。

转移方程（0/1背包，逆序枚举容量）：
  for i in 0..n:
    for j = W downto w[i]:
      dp[j] += dp[j - w[i]]

含义：凑成重量 j 的方案数 = 不选第i件（dp[j]原值）+ 选第i件（dp[j-w[i]]）。

与最大价值版的对比：
  最大价值：dp[j] = max(dp[j], dp[j-w[i]] + v[i])
  方案数：  dp[j] += dp[j-w[i]]
  只改了转移符号，循环骨架完全相同。`,
          table: [
            ['j（目标重量）', '0', '1', '2', '3', '4', '5'],
            ['初始', '1', '0', '0', '0', '0', '0'],
            ['加入w=2后', '1', '0', '1', '0', '0', '0'],
            ['加入w=3后', '1', '0', '1', '1', '0', '1'],
            ['加入w=5后', '1', '0', '1', '1', '0', '2'],
          ],
          tip: '📌 方案数背包初始化 dp[0]=1 而不是0——如果写成0，所有状态都会传播0，最终全部为0。这是方案数问题最常见的初始化错误。',
        },

        {
          type:  'choice',
          lbl:   '初始化',
          title: '❓ 选择：方案数背包的初始化',
          q:     '统计"恰好凑成重量W"的方案数时，dp 数组应该如何初始化？',
          opts: [
            'dp[0..W] 全部初始化为 0',
            'dp[0] = 0，dp[1..W] = 1',
            'dp[0] = 1，dp[1..W] = 0',
            'dp[0..W] 全部初始化为 1',
          ],
          ans:    2,
          fb_ok:  '正确。dp[0]=1 表示"什么都不选、重量恰好为0"是一种合法方案（空集）。dp[1..W]=0 表示初始时还没有任何物品，凑不出非零重量。这是方案数 DP 的标准初始化。',
          fb_err: '选项A全零会导致所有状态传播0，最终答案也是0。选项B反了，dp[0] 是基础方案数必须为1。选项D全1没有意义，会把不合法的凑法也计入。正确做法：dp[0]=1（空集方案），其余为0。',
          hint:   '思考"什么都不选"时，重量是多少？这算不算一种方案？',
        },

        {
          type:  'choice',
          lbl:   '推导',
          title: '❓ 选择：方案数手动推导',
          q:
`W=4，两件物品重量为 w1=1，w2=3。
统计恰好凑成重量4的方案数。
初始 dp = [1,0,0,0,0]（下标0~4）。
加入 w1=1 后，dp[4] 的值是多少？`,
          opts: [
            '0（重量1装不到容量4的位置）',
            '1（选一件w=1，dp[4] += dp[3] = 0+0 = 0，所以还是0）',
            '0（dp[4] += dp[3]，而此时 dp[3]=0，所以 dp[4]=0）',
            '1（dp[4] 直接被置为1）',
          ],
          ans:    2,
          fb_ok:  '正确。加入 w1=1 时，对 j=4 执行 dp[4] += dp[4-1] = dp[3]。此时 dp[3] 仍为0（还没有凑成重量3的方案），所以 dp[4] 依然为0。这说明只有一件重量1的物品，无法凑出重量4。',
          fb_err: '选项A说法有误，重量1的物品可以影响各容量，但影响是通过 dp[j] += dp[j-1] 传播的。选项D错误，方案数是累加而非置1。正确过程：dp[4] += dp[3]，而 dp[3]=0，所以结果是0。',
          hint:   '用转移方程 dp[j] += dp[j-w[i]] 代入 j=4，w1=1，看看 dp[3] 当时是多少。',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：方案数背包核心代码',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 统计恰好凑成重量W的方案数（0/1背包）
int countWays(int n, int W, vector<int>& w) {
    vector<long long> dp(W + 1, [A]); // 初始化：空集是一种方案
    dp[0] = 1;
    for (int i = 0; i < n; i++) {
        for (int j = W; j >= w[i]; j--) {
            [B]; // 累加选第i件物品的方案数
        }
    }
    return dp[W];
}`,
          blanks:  ['A', 'B'],
          opts: [
            '0',              '1',
            'dp[j] += dp[j - w[i]]', 'dp[j] = max(dp[j], dp[j - w[i]])',
          ],
          answers: ['0', 'dp[j] += dp[j - w[i]]'],
          fb_ok:   '正确。A 处初始化为0（代码下一行再单独设 dp[0]=1），表示初始时只有空集这一种方案。B 处转移用累加 +=，把"选第i件"的方案数叠加到当前状态，这是方案数问题与最大价值问题的核心区别。',
          fb_err:  'A 处：初始化为1会让 dp[1..W] 都从1开始，计入不合法方案，应初始化为0（dp[0] 单独设为1）。B 处：max 是求最大价值的写法，方案数要用 += 累加，不是取最大值。',
          hint:    'A 想想 dp[1..W] 初始应该是"没有方案"还是"有一种方案"；B 方案数是叠加还是取最大？',
        },
      ],
    },

    // ─────────────────────────────────────────────
    // 锐士：恰好凑齐 vs 至多装满，最小价值变形
    // ─────────────────────────────────────────────
    pro: {
      rank: '锐士',
      coins: 80,
      xp: 200,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 恰好凑齐 vs 至多装满，以及最小价值',
          body:
`背包变形汇总：同一套循环骨架，改两处就能变出不同问题。

变形一：至多装满（经典0/1背包）
  目标：容量不超过W时的最大价值
  初始化：dp[0..W] = 0
  转移：dp[j] = max(dp[j], dp[j-w[i]] + v[i])

变形二：恰好凑满（恰好等于W）
  目标：恰好用完W容量时的最大价值
  初始化：dp[0] = 0，dp[1..W] = -INF（负无穷，表示不可达）
  转移：dp[j] = max(dp[j], dp[j-w[i]] + v[i])
  答案：若 dp[W] == -INF 则无解，否则输出 dp[W]

变形三：最小价值（恰好凑满，最小化代价）
  目标：恰好用完W容量时的最小价值（如最少硬币数）
  初始化：dp[0] = 0，dp[1..W] = INF（正无穷，表示不可达）
  转移：dp[j] = min(dp[j], dp[j-w[i]] + v[i])
  答案：若 dp[W] == INF 则无解，否则输出 dp[W]`,
          concept:
`三种变形的对比表（转移方程 + 初始化）：

问题类型       初始化              转移          无解判断
至多最大价值   dp[*] = 0           取 max        无（总有不选的选项）
恰好最大价值   dp[0]=0,其余=-INF   取 max        dp[W] < 0
恰好最小价值   dp[0]=0,其余=INF    取 min        dp[W] == INF

关键规律：
  1. "恰好"问题需要用 INF / -INF 标记不可达状态
  2. 求最大用 -INF 初始化（不可达状态不能参与 max 传播）
  3. 求最小用 +INF 初始化（不可达状态不能参与 min 传播）
  4. 循环骨架（逆序或正序）由0/1背包还是完全背包决定，与变形无关

记忆口诀：
  最大 → 不可达=-INF（防止假的大值传播）
  最小 → 不可达=+INF（防止假的小值传播）`,
          table: [
            ['变形类型', '初始化 dp[1..W]', '转移', '答案无解条件'],
            ['至多最大', '0', 'max', '不会无解'],
            ['恰好最大', '-INF', 'max', 'dp[W] 仍为 -INF'],
            ['恰好最小', '+INF', 'min', 'dp[W] 仍为 +INF'],
            ['方案计数', '0（dp[0]=1）', '+=', 'dp[W]=0 即无方案'],
          ],
          tip: '📌 "恰好"问题最容易忘记把 dp[1..W] 初始化为 INF 或 -INF。如果全部初始化为0，不可达的状态会被当成"代价为0"的合法状态参与转移，导致结果偏小或偏大。',
        },

        {
          type:  'choice',
          lbl:   '初始化',
          title: '❓ 选择：恰好凑满最小价值的初始化',
          q:
`求"恰好使用重量W，使得总价值最小"时，dp 数组应如何初始化？
（物品有重量 w[i] 和价值 v[i]，每件只能选一次）`,
          opts: [
            'dp[0..W] 全部初始化为 0',
            'dp[0] = INF，dp[1..W] = 0',
            'dp[0..W] 全部初始化为 INF',
            'dp[0] = 0，dp[1..W] = INF',
          ],
          ans:    3,
          fb_ok:  '正确。dp[0]=0 表示重量为0时最小价值是0（不选任何物品）。dp[1..W]=INF 表示初始时凑不出任何非零重量，标记为不可达。转移时 min 操作不会从 INF 状态传播出有效值，只有真正可达的状态才会被更新。',
          fb_err: '选项A全零会把不可达状态当成"价值为0"参与转移，最终答案可能偏小。选项B dp[0]=INF 错误，不选任何物品时重量和价值都是0，dp[0] 必须为0。选项C全INF连 dp[0] 也是INF，转移无法从有效初始值出发。正确：dp[0]=0，其余INF。',
          hint:   '想想 dp[0] 代表什么情况——什么都不选，重量是0，这时最小价值是多少？',
        },

        {
          type:  'choice',
          lbl:   '对比',
          title: '❓ 选择：至多装满 vs 恰好凑满的初始化差异',
          q:
`以下两段代码：

代码A：dp[0..W] = 0，转移 dp[j] = max(dp[j], dp[j-w[i]]+v[i])
代码B：dp[0]=0，dp[1..W]=-INF，转移 dp[j] = max(dp[j], dp[j-w[i]]+v[i])

W=5，只有一件物品 w=3，v=10。
两段代码执行后，dp[5] 分别是多少？`,
          opts: [
            '代码A：10，代码B：10',
            '代码A：0，代码B：-INF',
            '代码A：0，代码B：0',
            '代码A：10，代码B：-INF',
          ],
          ans:    1,
          fb_ok:  '正确。代码A：dp[5] = max(dp[5], dp[5-3]+v) = max(0, dp[2]+10) = max(0, 0+10) = 10。代码B：dp[2]=-INF，所以 dp[5] = max(-INF, -INF+10) 仍为 -INF，表示无法恰好凑出重量5。两段代码对"容量5，只有重量3的物品"给出了不同语义：A允许装不满，B要求恰好凑满。',
          fb_err: '代码A初始化全0，dp[2]=0，所以 dp[5] = max(0, 0+10) = 10，可以选重量3不满5（至多装满语义）。代码B中 dp[2]=-INF，-INF+10 仍是-INF，dp[5] 不会被更新，最终是-INF，表示不可达（恰好凑满语义）。',
          hint:   '代码B中 dp[2] 是多少？用 -INF 加上10还是-INF吗？',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：恰好凑满最小价值',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 恰好使用重量W，最小化总价值（0/1背包）
int minCostExact(int n, int W, vector<int>& w, vector<int>& v) {
    const int INF = 1e9;
    vector<int> dp(W + 1, [A]); // 不可达状态初始化为INF
    dp[0] = 0;                   // 重量为0时代价为0
    for (int i = 0; i < n; i++) {
        for (int j = W; j >= w[i]; j--) {
            if (dp[j - w[i]] < INF)  // 前驱状态可达才转移
                dp[j] = [B];          // 选第i件，取更小的价值
        }
    }
    return dp[W] == INF ? -1 : dp[W]; // -1表示无解
}`,
          blanks:  ['A', 'B'],
          opts: [
            'INF',  '0',
            'min(dp[j], dp[j - w[i]] + v[i])', 'max(dp[j], dp[j - w[i]] + v[i])',
          ],
          answers: ['INF', 'min(dp[j], dp[j - w[i]] + v[i])'],
          fb_ok:   '正确。A 处初始化为 INF 标记所有容量不可达（dp[0] 单独设为0）。B 处用 min 取更小的价值，这是最小化问题的转移方向，与最大价值问题的 max 方向相反。',
          fb_err:  'A 处：初始化为0会把不可达状态当作"价值为0"参与转移，结果偏小；应为 INF。B 处：max 是求最大价值的写法，最小化问题要用 min。',
          hint:    'A 想想不可达状态应该用什么值标记，才能在 min 转移中不被误选；B 最小化问题用哪个比较函数？',
        },
      ],
    },

    // ─────────────────────────────────────────────
    // 英雄：同一套骨架切换三种问题性质
    // ─────────────────────────────────────────────
    hero: {
      rank: '英雄',
      coins: 120,
      xp: 280,
      steps: [
        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 一套骨架，三种变形：只改两处',
          body:
`背包变形的本质：循环骨架不变，只改"初始化"和"转移符号"。

通用骨架（0/1背包，逆序）：
  for i in 0..n:
    for j = W downto w[i]:
      dp[j] = ???(dp[j], dp[j-w[i]] ??? v[i])

三种问题只需改两处：

  变形一：最大价值（至多装满）
    初始化：dp[*] = 0
    转移：  dp[j] = max(dp[j], dp[j-w[i]] + v[i])

  变形二：最大价值（恰好凑满）
    初始化：dp[0]=0，dp[1..W]=-INF
    转移：  dp[j] = max(dp[j], dp[j-w[i]] + v[i])

  变形三：最小价值（恰好凑满）
    初始化：dp[0]=0，dp[1..W]=INF
    转移：  dp[j] = min(dp[j], dp[j-w[i]] + v[i])

  变形四：方案计数（恰好凑满）
    初始化：dp[0]=1，dp[1..W]=0
    转移：  dp[j] += dp[j-w[i]]`,
          concept:
`为什么循环骨架可以不变？

  背包DP的骨架只决定"每件物品用几次"和"枚举顺序"，
  与"问题问的是什么"无关。

  问题性质由两个要素决定：
    1. 初始化：决定"基础状态的合法值"和"不可达状态的标记值"
    2. 转移操作符：决定"从多个候选中怎么选"

  INF / -INF 的作用：
    不可达状态不应参与传播。
    用 INF 初始化 + min 转移：INF + 任何值 >= INF，不会被选
    用 -INF 初始化 + max 转移：-INF + 任何值 <= -INF，不会被选
    这样不可达状态自然被隔离，不会污染可达状态。

扩展：完全背包同理，只需把逆序改正序，其余变形规则完全相同。`,
          table: [
            ['变形', '初始化', '转移', '答案提取'],
            ['至多最大', 'dp[*]=0', 'max(dp[j], dp[j-w]+v)', 'dp[W]'],
            ['恰好最大', 'dp[0]=0,其余-INF', 'max(dp[j], dp[j-w]+v)', 'dp[W]（-INF则无解）'],
            ['恰好最小', 'dp[0]=0,其余+INF', 'min(dp[j], dp[j-w]+v)', 'dp[W]（+INF则无解）'],
            ['方案计数', 'dp[0]=1,其余0', 'dp[j]+=dp[j-w]', 'dp[W]（0则无方案）'],
          ],
          tip: '📌 记住这张表，背包变形题90%都是这四种之一的组合。遇到新题时先判断问题性质，再套对应的初始化和转移符号，骨架不用动。',
        },

        {
          type:  'choice',
          lbl:   '识别',
          title: '❓ 选择：判断问题性质选正确初始化',
          q:
`题目：有 n 枚硬币，面值分别为 c[1..n]，每枚只能用一次。
问：凑出恰好金额 W，最少需要几枚硬币？
（无解输出 -1）

这道题对应哪种变形？初始化应该怎么写？`,
          opts: [
            '至多最大价值变形，dp[*]=0',
            '恰好最大价值变形，dp[0]=0，dp[1..W]=-INF',
            '恰好最小价值变形，dp[0]=0，dp[1..W]=INF',
            '方案计数变形，dp[0]=1，dp[1..W]=0',
          ],
          ans:    2,
          fb_ok:  '正确。"恰好金额W"说明是恰好凑满，"最少几枚"说明是最小化，对应恰好最小价值变形。初始化 dp[0]=0（0枚硬币凑出0），dp[1..W]=INF（初始不可达）。转移用 min，每枚硬币价值视为1。',
          fb_err: '关键词"恰好"排除了至多装满（选项A）。"最少"是最小化方向，排除了最大价值（选项B）。"最少几枚"是求最优值而非计数，排除了方案计数（选项D）。正确：恰好+最小化 = 选项C，初始化 dp[0]=0，其余INF。',
          hint:   '两个关键词："恰好"决定是否用INF/-INF标记不可达；"最少"决定是min还是max。',
        },

        {
          type:  'choice',
          lbl:   '传播',
          title: '❓ 选择：INF 为什么能隔离不可达状态',
          q:
`恰好最小价值背包中，初始化 dp[1..W]=INF。
假设某个状态 dp[j] 在处理完所有物品后仍为 INF。

以下哪个说法正确解释了 INF 初始化的作用？`,
          opts: [
            'INF 只是一个大数，不影响 min 的结果，只是方便判断无解',
            'INF + v[i] 通常仍然很大，在 min 转移中不会比合法状态更优，所以不可达状态不会污染其他状态',
            'INF 初始化后，代码中的 if 检查会自动跳过 INF 状态，所以它不会被传播',
            '只要某个状态是 INF，整个背包就输出无解，不继续计算',
          ],
          ans:    1,
          fb_ok:  '正确。INF + v[i] 仍然是一个极大的值，在 min(dp[j], INF + v[i]) 中，合法状态的实际代价一定更小，所以 INF 状态不会被选为更优结果，也就不会把"不可达"污染给其他状态。',
          fb_err: '选项A说法不完整，INF 不只是为了判断无解，更重要的是防止传播。选项C错误，示例代码中用 if 检查是一种实现方式，但即使不加 if，INF+v 在 min 中也不会胜出。选项D错误，INF 状态只影响自身，不影响整体计算流程。',
          hint:   '假设 dp[j-w[i]] = INF，那么 min(dp[j], INF + v[i]) 的结果是什么？',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：一套骨架切换两种变形',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 同一骨架，通过初始化和转移符号切换问题性质
// 当前：恰好凑满W，统计方案数（完全背包，正序）
int countWaysComplete(int n, int W, vector<int>& w) {
    vector<long long> dp(W + 1, [A]); // 恰好凑满方案数的初始化
    dp[0] = 1;
    for (int i = 0; i < n; i++) {
        for (int j = w[i]; j <= W; j++) { // 正序：完全背包（可重复选）
            [B];                            // 累加选第i件的方案数
        }
    }
    return (int)dp[W];
}`,
          blanks:  ['A', 'B'],
          opts: [
            '0',                      '1',
            'dp[j] += dp[j - w[i]]',  'dp[j] = max(dp[j], dp[j - w[i]])',
          ],
          answers: ['0', 'dp[j] += dp[j - w[i]]'],
          fb_ok:   '正确。A 处初始化为0（dp[0] 单独设为1，表示空集是一种方案，其余不可达）。B 处用 += 累加方案数，与最大价值问题的 max 不同。注意这里是完全背包（正序），骨架改了方向，但初始化和转移符号的规则与0/1背包版相同。',
          fb_err:  'A 处：初始化为1会让 dp[1..W] 初始就有1种"假方案"，结果偏大；应为0。B 处：max 是求最大值而非计数，方案数用 += 累加。切换到完全背包只改了容量枚举方向（正序），初始化和转移符号的选择规则不变。',
          hint:    'A 想想方案计数的初始化规律（参考新兵关）；B 方案数是累加还是取最大？',
        },
      ],
    },
  },
};
