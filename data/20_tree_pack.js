// data/20_tree_pack.js
// 章节：20.树上背包 · 有依赖的背包

export const LESSON = {
  id:    'tree_pack',
  title: '20.树上背包',
  sub:   '有依赖的背包',

  battles: {

    new: {
      rank: '新兵',
      coins: 50,
      xp: 120,
      steps: [

        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 有依赖的背包——选儿子必须先选父亲',
          body:
`题目：有 n 件物品排成一棵树，每件物品有重量 w[i] 和价值 v[i]。
规则：若要选物品 i，则必须先选 i 的父节点（依赖关系沿树边传递）。
背包容量为 m，求最大总价值。

建模：树形DP + 背包
  · 选 i 就必须选 i 的整条祖先链（一直到根）
  · 等价于：选某子树的节点，必须包含该子树的根节点

状态设计：
  dp[u][j] = 以 u 为根的子树，u 被选中，且子树内（含u）共选了 j 个容量的物品时，最大价值

  注意：这里"u 被选中"是前提，dp[u][j] 只在 u 被选的情况下定义。
  不选 u 时，其子树内的任何节点都不能被选（依赖链断掉），所以不选 u 时贡献为 0。

转移（v 是 u 的子节点）：
  合并子树 v 之前，dp[u] 只考虑了 u 自身和之前合并的子树。
  合并 v 时，枚举分给 v 子树的容量 j'，v 子树贡献 max(0, dp[v][j'])：
    dp[u][j] = max{ dp[u][j-j'] + dp[v][j'] }（0 <= j' <= j）

  这是"树上背包"的核心：把子树看成一组物品，逐棵合并。`,
          concept:
`完整DP框架：

  初始化：dp[u][w[u]] = v[u]（只选u自身，容量w[u]，价值v[u]）
           dp[u][j] = -INF 或 0（j < w[u] 时：容量不够选u本身，视情况处理）

  合并子节点 v：
    // 从大到小枚举容量（避免重复选）
    for j = m downto 0:
        for j' = 0 to j:
            dp[u][j] = max(dp[u][j], dp[u][j-j'] + dp[v][j'])

  答案：max(0, max{ dp[root][j] }) 或者以不选根节点为答案0。

时间复杂度：
  朴素实现：O(n * m^2)（每条边合并时两层容量枚举）
  优化：每个节点 u 合并时，j 的上界是"当前已处理子树的大小"，总复杂度 O(n*m)`,
          tip: '📌 树上背包的初始化：dp[u][j] 初始只放 u 自身（dp[u][w[u]]=v[u]），不能初始化 dp[u][j]=v[u] for all j>=w[u]，那是无限背包的写法。dp 表示恰好选了 j 容量，不是"至多"。',
        },

        {
          type:  'choice',
          lbl:   '状态',
          title: '❓ 选择：dp[u][j] 的含义',
          q:
`树上背包中，dp[u][j] 的正确含义是？（假设 u 一定被选中）`,
          opts: [
            '以 u 为根的子树中，选了重量恰好为 j 的物品集合（含u）的最大价值',
            '以 u 为根的子树中，选了不超过 j 容量的物品集合（含u）的最大价值',
            '从根到 u 的路径上，选了重量为 j 的物品的最大价值',
            '以 u 为根的子树中，共选了 j 件物品的最大价值',
          ],
          ans:    0,
          fb_ok:  '✓ 正确。dp[u][j] = 以 u 为根的子树中，u 被选、子树内物品总重量恰好为 j 时的最大价值。"恰好"而非"不超过"是因为合并时需要精确枚举各子树分配的容量，用"不超过"会导致重复计算。',
          fb_err: '✗ 选项B（不超过j）：合并子树时，枚举分给每棵子树的容量，需要精确知道各分配量，"不超过"会导致多次计数同一物品集合。选项C：路径上的物品无法享受子树的灵活选择，与题意不符。选项D（j件物品）：背包通常以容量为维度，不是件数；件数维度会改变问题结构。',
          hint:   '想象把背包容量 m 分配给每棵子树，枚举分给子树 v 的容量 j\'，剩余 j-j\' 给之前的子树，这需要"恰好"语义才能正确相加。',
        },

        {
          type:  'choice',
          lbl:   '合并',
          title: '❓ 选择：合并子树 v 时的容量枚举方向',
          q:
`树上背包合并子树 v 时，外层枚举当前容量 j（从 m 到 0 或从 0 到 m），内层枚举分给 v 的容量 j\'（从 0 到 j）。外层应该从大到小还是从小到大枚举？`,
          opts: [
            '从小到大：j 从 0 到 m，因为小容量状态先确定，大容量可以依赖小容量',
            '从大到小：j 从 m 到 0，防止同一子树 v 的物品被重复选取',
            '方向无所谓，因为内层 j\' 从 0 到 j，两个方向结果相同',
            '从大到小：j 从 m 到 w[u]，小于 w[u] 的容量无效',
          ],
          ans:    1,
          fb_ok:  '✓ 正确。外层 j 从大到小枚举，是 0/1 背包的经典技巧：确保每棵子树 v 对每个容量 j 只贡献一次。若从小到大，较小的 j 先被更新，之后枚举较大的 j 时会用到已经加入了 v 的 dp[u][j-j\']，导致 v 的物品被重复计入。',
          fb_err: '✗ 选项A（从小到大）：dp[u][j] 更新后，之后更大的 j 访问 dp[u][j-j\'] 时可能用到已经加入 v 的值，等同于 v 的物品被选了多次（完全背包的效果）。选项C（无所谓）：方向会影响正确性，不是无所谓。选项D：从m到w[u]方向对，但理由说的是"小于w[u]无效"——这是关于 j 范围的说法，而外层枚举方向的原因是防止重复。',
          hint:   '类比 0/1 背包：为什么 0/1 背包要从大到小枚举容量？同样的原因，树上背包合并子树时外层也要从大到小。',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：树上背包核心DFS',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`#include <bits/stdc++.h>
using namespace std;
int w[305], v[305], dp[305][305];
int m; // 背包容量
vector<int> children[305];

void dfs(int u) {
    // [A] 初始化：只选u自身
    for (int j = 0; j <= m; j++) dp[u][j] = 0;
    if (w[u] <= m) dp[u][w[u]] = v[u];

    for (int child : children[u]) {
        dfs(child);
        // 合并子树child到u，从大到小枚举容量
        // [B] 外层从大到小枚举当前容量j
        for (int j = m; j >= 0; j--) {
            // [C] 内层枚举分给child子树的容量j'
            for (int jj = 0; jj <= j; jj++) {
                if (dp[child][jj] > 0) // child子树选了jj容量
                    dp[u][j] = max(dp[u][j], dp[u][j-jj] + dp[child][jj]);
            }
        }
    }
}

int main() {
    int n; cin >> n >> m;
    for (int i = 1; i <= n; i++) cin >> w[i] >> v[i];
    // 读树结构并建树（略）
    dfs(1);
    int ans = 0;
    for (int j = 0; j <= m; j++) ans = max(ans, dp[1][j]);
    cout << ans << endl;
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'for (int j = 0; j <= m; j++) dp[u][j] = 0; if (w[u] <= m) dp[u][w[u]] = v[u]',
            'for (int j = w[u]; j <= m; j++) dp[u][j] = v[u]',
            'for (int j = m; j >= 0; j--)',
            'for (int j = 0; j <= m; j++)',
            'for (int jj = 0; jj <= j; jj++)',
            'for (int jj = 0; jj <= m; jj++)',
          ],
          answers: [
            'for (int j = 0; j <= m; j++) dp[u][j] = 0; if (w[u] <= m) dp[u][w[u]] = v[u]',
            'for (int j = m; j >= 0; j--)',
            'for (int jj = 0; jj <= j; jj++)',
          ],
          fb_ok:  '✓ A：先全部清零，再只在 dp[u][w[u]] 处设 v[u]（恰好只选u自身）。B：外层从大到小，防止child的物品被重复选取（0/1背包原则）。C：内层 jj 从0到j，枚举分配给child的容量，且不超过当前总容量j。',
          fb_err: '✗ A 若写for j=w[u]..m，dp[u][j]=v[u]：这是"不超过"语义，把u当成可以任意选的物品，变成完全背包，错误。B 若从0到m：child物品可被重复计入，变成完全背包。C 若jj到m：jj可能超过j，导致dp[u][j-jj]下标为负（数组越界），或者容量超出当前j的限制。',
          hint:   'A 的关键：恰好 dp[u][w[u]]=v[u]，其他位置为0。B 的关键：从大到小（0/1背包）。C 的关键：jj<=j，不超过当前总容量。',
        },
      ]
    },

    pro: {
      rank: '锐士',
      coins: 80,
      xp: 200,
      steps: [

        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 树上背包的时间复杂度优化',
          body:
`朴素树上背包的复杂度问题：

对每条边 (u, v)，合并 v 到 u 时有两层容量循环（j 和 j\'），各最多 O(m)，合并一条边 O(m^2)。
n 条边（n 个节点有 n-1 条边），总复杂度 O(n * m^2)。
当 n=300, m=300 时，约 2700 万次运算，可以接受。
但 n=1000, m=1000 时，约 10^9 次，超时。

优化思路：限制 j 的上界为"当前子树大小"

  关键观察：以 u 为根的子树里，物品总数（或总重量）是有限的。
  合并前，u 子树（不含 v）有 sz[u] 个物品（或总重量 W[u]），
  v 子树有 sz[v] 个物品（或总重量 W[v]）。
  合并时，j 不超过 min(m, W[u]+W[v])，j\' 不超过 min(j, W[v])。
  
  理论分析：每对（u子树节点, v子树节点）在合并时只被枚举一次，
  总复杂度降为 O(n * m)（当物品重量都为1时，即选节点数不超过背包容量）。`,
          concept:
`优化后的合并代码（以节点数为容量的版本）：

  int sz[N]; // sz[u] = 以u为根的子树节点数

  void dfs(int u, int par) {
      sz[u] = 1;
      dp[u][1] = v[u]; // 只选u，容量1（每个节点重量为1）

      for (int child : adj[u]) {
          if (child == par) continue;
          dfs(child, u);
          // 合并时限制上界
          for (int j = min(m, sz[u] + sz[child]); j >= 1; j--) {
              for (int jj = 1; jj <= min(j-1, sz[child]); jj++) {
                  dp[u][j] = max(dp[u][j], dp[u][j-jj] + dp[child][jj]);
              }
          }
          sz[u] += sz[child]; // 更新子树大小
      }
  }

复杂度分析：
  任意两节点 x（属于 u 子树）和 y（属于 v 子树）的合并被计入 O(1) 次，
  共 O(n^2) 对节点，容量维度被 min(m, n) 限制，总 O(n * min(n,m))。`,
          tip: '📌 优化的核心：j 的上界从 m 缩小到"当前已合并子树大小+即将合并的子树大小"。这个界限是物理上合理的——不可能选超过子树大小的物品。优化前后结果相同，只是跳过了必然为0的状态。',
        },

        {
          type:  'choice',
          lbl:   '复杂度',
          title: '❓ 选择：朴素树上背包的时间复杂度',
          q:
`n 个节点，背包容量 m，朴素树上背包（每条边合并时j和j\'各枚举0到m）的时间复杂度是？`,
          opts: [
            'O(n * m)',
            'O(n^2 * m)',
            'O(n * m^2)',
            'O(n^2 * m^2)',
          ],
          ans:    2,
          fb_ok:  '✓ 正确。有 n-1 条边（近似 n 条），每条边合并时两层循环各 O(m)，合并一条边 O(m^2)，总复杂度 O(n * m^2)。',
          fb_err: '✗ 选项A（O(nm)）：是优化后的复杂度，朴素版本还有 m^2 的代价。选项B（O(n^2*m)）：多了一个 n 因子，枚举边是 O(n)，不是 O(n^2)。选项D（O(n^2*m^2)）：过高估计，枚举边是 O(n)，不是 O(n^2)。',
          hint:   '枚举多少条边（O(n)）× 每条边合并代价（两层容量循环 O(m^2)）= 总复杂度。',
        },

        {
          type:  'choice',
          lbl:   '优化',
          title: '❓ 选择：为什么限制 j\' 的上界为 sz[v] 能降低复杂度',
          q:
`优化后合并子树 v 时，内层 j\' 的上界从 j（最大m）改为 min(j, sz[v])（子树大小）。为什么这能正确地降低复杂度？`,
          opts: [
            '因为 sz[v] 通常远小于 m，少枚举了很多无效状态',
            '因为 dp[v][j\'] 在 j\' > sz[v] 时一定为 0（无法选超过子树大小的物品），枚举这些状态没有意义',
            '因为 j\' 越大，dp[v][j\'] 越小，从大到小枚举更高效',
            '因为限制上界会改变最优解，得到近似结果而非精确结果',
          ],
          ans:    1,
          fb_ok:  '✓ 正确。子树 v 中只有 sz[v] 个节点（物品），每件物品重量为1，无论如何不能选超过 sz[v] 个，dp[v][j\'] 在 j\' > sz[v] 时为 0。把 0 加入 dp[u][j] 不改变结果，跳过这些状态只是避免无效计算，不影响正确性。',
          fb_err: '✗ 选项A：说"通常远小于m"不够精确，正确原因是"一定为0"，不是"通常"。选项C：dp值与枚举方向无关，且值的大小不决定枚举顺序的效率。选项D：结果完全一样，是精确算法，不是近似。',
          hint:   '子树 v 有 sz[v] 个物品，背包不可能装超过 sz[v] 个——装不了就是装不了，dp[v][j\']=0。枚举这些0值没意义，跳过即可。',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：优化版树上背包（子树大小限界）',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`#include <bits/stdc++.h>
using namespace std;
int v[305], dp[305][305], sz[305];
int m, n;
vector<int> adj[305]; // 无向树

void dfs(int u, int par) {
    // [A] 初始化：只选u，容量1，价值v[u]（每个节点重量为1）
    sz[u] = 1;
    dp[u][1] = v[u];

    for (int child : adj[u]) {
        if (child == par) continue;
        dfs(child, u);
        // 限制j上界为已处理子树大小+child子树大小
        // [B] 外层j从min(m, sz[u]+sz[child])到1
        for (int j = min(m, sz[u] + sz[child]); j >= 1; j--) {
            // [C] 内层j'从1到min(j-1, sz[child])
            for (int jj = 1; jj <= min(j-1, sz[child]); jj++) {
                dp[u][j] = max(dp[u][j], dp[u][j-jj] + dp[child][jj]);
            }
        }
        // [D] 更新子树大小
        sz[u] += sz[child];
    }
}

int main() {
    cin >> n >> m;
    for (int i = 1; i <= n; i++) cin >> v[i];
    for (int i = 0; i < n-1; i++) {
        int a, b; cin >> a >> b;
        adj[a].push_back(b); adj[b].push_back(a);
    }
    dfs(1, 0);
    int ans = 0;
    for (int j = 1; j <= m; j++) ans = max(ans, dp[1][j]);
    cout << ans << endl;
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'sz[u] = 1; dp[u][1] = v[u]',
            'sz[u] = 0; dp[u][0] = v[u]',
            'for (int j = min(m, sz[u] + sz[child]); j >= 1; j--)',
            'for (int j = m; j >= 0; j--)',
            'for (int jj = 1; jj <= min(j-1, sz[child]); jj++)',
            'for (int jj = 0; jj <= j; jj++)',
          ],
          answers: [
            'sz[u] = 1; dp[u][1] = v[u]',
            'for (int j = min(m, sz[u] + sz[child]); j >= 1; j--)',
            'for (int jj = 1; jj <= min(j-1, sz[child]); jj++)',
          ],
          fb_ok:  '✓ A：sz[u]=1（u自身是1个节点），dp[u][1]=v[u]（选u，容量1，价值v[u]）。B：上界 min(m,sz[u]+sz[child]) 是当前合并后子树的最大有效容量，下界1是最少需要1个物品（选u本身）。C：jj从1开始（至少分1个给child），上界 min(j-1,sz[child]) 保证剩余j-jj>=1给u自身，且不超过child子树大小。',
          fb_err: '✗ A若sz[u]=0：u自身没被计入，子树大小统计错误。B若用m代替上界：失去优化效果，退化为朴素O(nm^2)。C若jj从0开始：jj=0表示child不贡献，dp[u][j]与dp[u][j]+0=dp[u][j]相同，是无效迭代，更重要的是若写jj<=j（而不是j-1）会导致j-jj=0，dp[u][0]=0（u未被选），语义错误。',
          hint:   'C中jj上界 min(j-1,sz[child])：j-1保证给u本身留至少1个容量；sz[child]保证不超过child子树能提供的最大量。两个约束取min。',
        },
      ]
    },

    hero: {
      rank: '英雄',
      coins: 120,
      xp: 280,
      steps: [

        {
          type:  'concept',
          lbl:   '读题',
          title: '📖 为什么容量上界是子树大小——复杂度的严格分析',
          body:
`英雄关把"限制上界后复杂度为 O(nm)"做一个严格分析。

分析方法：计算所有合并操作中（j, j\'）的枚举总次数。

对树上任意两个节点 x 和 y（x 在 u 已处理的子树中，y 在 child 子树中），
在合并 child 到 u 的那一步，(j, j\') = (sz[u处理前]+sz[child], 对应分配) 的组合中，
(x, y) 这一对节点被计入一次（对应 j\'=某值包含y，j-j\'=某值包含x）。

每对不同子树的节点 (x, y) 恰好被枚举一次（在它们的 LCA 那一步合并中）。
树上共有 O(n^2) 对节点，但每对只计一次，所以总枚举次数是 O(n^2)。
加上容量维度 m 的约束，最终复杂度 O(n * min(n, m))。

更直觉的理解：
  把整棵树的合并过程看成"把所有节点的信息汇聚到根"，
  每个节点的信息在传播路径上恰好被处理一次，
  每次处理消耗 O(m) 时间，n 个节点共 O(nm) 时间。`,
          concept:
`容量上界为子树大小的三层含义：

1. 正确性保证：dp[v][j\'] 在 j\'>sz[v] 时一定为0，
   这些0值对 max 操作没有贡献，跳过不影响正确性。

2. 复杂度优化：把每条边的合并代价从 O(m^2) 降低到 O(sz[u]*sz[v])，
   对整棵树求和恰好是 O(n^2)（类似合并排序的分析）。

3. 代码体现：
   for j = min(m, sz[u]+sz[v]) downto 1:
     for jj = 1 to min(j-1, sz[v]):
   两个 min 一个控制有效范围，一个防止越界。`,
          tip: '📌 O(nm)优化的关键操作：合并child后立刻 sz[u]+=sz[child]，保证下一个子节点合并时 sz[u] 已包含了child。若忘记更新 sz[u]，每次合并的上界都是原始的 sz[u]=1，退化成不同的错误。',
        },

        {
          type:  'choice',
          lbl:   '复杂度',
          title: '❓ 选择：优化后树上背包的时间复杂度',
          q:
`用"限制 j\' 上界为子树大小"优化后，树上背包（每个节点重量为1，背包容量m）的时间复杂度是？`,
          opts: [
            'O(n * m)',
            'O(n^2)',
            'O(n * m^2)',
            'O(n * min(n, m))',
          ],
          ans:    3,
          fb_ok:  '✓ 正确。总枚举次数是 O(n^2) 对节点（每对在其LCA合并时被计入一次），容量维度被 min(n,m) 限制（不可能选超过n个节点，也不超过背包容量m），总复杂度 O(n * min(n,m))。当 m >> n 时简化为 O(n^2)，当 m << n 时简化为 O(nm)。',
          fb_err: '✗ 选项A（O(nm)）：只有在 m<=n 时才成立，通常写成 O(n*min(n,m)) 更精确。选项B（O(n^2)）：只有在 m>=n 时成立。选项C（O(nm^2)）：这是朴素版本的复杂度，优化后不再是这个。',
          hint:   '分两种情况：m<=n 时背包容量是瓶颈，复杂度 O(nm)；m>n 时节点数是瓶颈，复杂度 O(n^2)。合并写成 O(n*min(n,m))。',
        },

        {
          type:  'choice',
          lbl:   '陷阱',
          title: '❓ 选择：sz[u] 更新时机的重要性',
          q:
`优化版树上背包中，sz[u] += sz[child] 这行代码应该放在什么位置？`,
          opts: [
            '在 dfs(child) 之前更新，确保合并时 sz[u] 已经正确',
            '在合并 child 的两层循环之后更新，确保本次合并的上界正确',
            '在 dfs(1) 调用之后统一计算所有节点的 sz，再重新跑 DP',
            '不需要更新，sz[u] 在初始化时已经包含了所有子节点',
          ],
          ans:    1,
          fb_ok:  '✓ 正确。sz[u]+=sz[child] 必须在合并 child 的循环之后、处理下一个子节点之前执行。这样在处理第 k 个子节点时，sz[u] 是"前 k-1 个子节点合并后的子树大小"，与当前子节点 sz[child] 之和才是正确的 j 上界。',
          fb_err: '✗ 选项A（合并前更新）：若在合并前就更新 sz[u]+=sz[child]，则 j 的上界变成"包含child的子树大小"，但合并还没开始，dp[u][j] 此时只包含之前的子树，上界偏大，枚举了无效状态（虽然不影响正确性，但破坏了复杂度分析的紧界）。选项C：统一计算 sz 再跑 DP 是另一种实现方式，但若预先算好 sz 后需要在合并时额外查询"合并前的sz[u]"，代码复杂。选项D：初始化时 sz[u]=1 只包含 u 自身，不包含子节点。',
          hint:   '合并第 i 个子节点时，j 的上界 = 前 i-1 个子节点合并后的子树大小 + 第 i 个子节点大小。所以 sz[u] 在处理完第 i-1 个子节点后、处理第 i 个之前更新是正确时机。',
        },

        {
          type:    'fill',
          lbl:     '代码',
          title:   '✏️ 填空：树上背包——验证更新顺序',
          q:       '点击代码中的 [?] 方框选中，再点候选项填入：',
          code:
`// 以三节点树为例验证：根1(v=3)，子2(v=5)，子3(v=4)，m=2
// 预期答案：选节点2和3，价值=9（不选根）
// dp[2]={0,5,0,...}, dp[3]={0,4,0,...}
// 合并2到1：dp[1][1]=3, dp[1][2]=max(dp[1][1]+dp[2][1])=3+5=8
//           等等，要先合并2再合并3

#include <bits/stdc++.h>
using namespace std;
int v[] = {0,3,5,4}; // 1-indexed
int dp[4][3], sz[4]; // m=2
vector<int> adj[] = {{},{2,3},{},{}}; // 根1，子2，子3

void dfs(int u, int par, int m) {
    sz[u] = 1;
    // [A] 只选u自身初始化
    if (1 <= m) dp[u][1] = v[u];

    for (int c : adj[u]) {
        if (c == par) continue;
        dfs(c, u, m);
        for (int j = min(m, sz[u]+sz[c]); j >= 1; j--)
            for (int jj = 1; jj <= min(j-1, sz[c]); jj++)
                dp[u][j] = max(dp[u][j], dp[u][j-jj] + dp[c][jj]);
        // [B] 合并后更新子树大小
        sz[u] += sz[c];
    }
}

int main() {
    dfs(1, 0, 2);
    // [C] 输出dp[1][0..2]中的最大值
    int ans = 0;
    for (int j = 0; j <= 2; j++) ans = max(ans, dp[1][j]);
    cout << ans << endl; // 期望输出9
}`,
          blanks:  ['A', 'B', 'C'],
          opts: [
            'if (1 <= m) dp[u][1] = v[u]',
            'for (int j = 0; j <= m; j++) dp[u][j] = v[u]',
            'sz[u] += sz[c]',
            'sz[c] += sz[u]',
            'for (int j = 0; j <= 2; j++) ans = max(ans, dp[1][j])',
            'for (int j = 1; j <= 2; j++) ans = max(ans, dp[1][j])',
          ],
          answers: [
            'if (1 <= m) dp[u][1] = v[u]',
            'sz[u] += sz[c]',
            'for (int j = 0; j <= 2; j++) ans = max(ans, dp[1][j])',
          ],
          fb_ok:  '✓ A：恰好选u一个，容量1，价值v[u]。B：sz[u]+=sz[c]，合并child后更新父节点子树大小，供下一个子节点合并时使用。C：从j=0开始（包含dp[1][0]=0，即不选任何节点），取全部最大值。本题答案为9（选2和3，不选1）。',
          fb_err: '✗ A 若设所有j>=1的dp为v[u]：变成"任意容量都只取v[u]"，丢失了精确容量信息。B 若sz[c]+=sz[u]：方向反了，应该是父节点吸收子节点大小。C 若从j=1开始：可能漏掉j=0的情况，但本题j=0时dp=0，不影响答案；保险起见从0开始。',
          hint:   '手动追踪：dp[2]={0,5,0}，dp[3]={0,4,0}；合并2到1后dp[1]={0,3,8}（j=2: dp[1][1]+dp[2][1]=3+5=8）；再合并3，j=2,jj=1: dp[1][2]=max(8, dp[1][1]+dp[3][1])=max(8,3+4)=max(8,7)=8... 等等，不对，sz[1]=1合并2后sz[1]=2，合并3时j_max=min(2,2+1)=2，j=2,jj=1: dp[1][2-1]+dp[3][1]=dp[1][1]+4=3+4=7<8，j=2,jj=1只有这个选项所以dp[1][2]=8。实际最优是选2和3不选1，需要dp[1][0]存"选了j=0容量对应u不选"——哦，初始dp[1][0]=0，合并2时j=1,jj=1:dp[1][0]+dp[2][1]=0+5=5，dp[1][1]=max(3,5)=5，合并3时j=2,jj=1:dp[1][1]+dp[3][1]=5+4=9，dp[1][2]=max(8,9)=9。答案9。这验证了从j=0开始枚举的重要性。',
        },
      ]
    },
  }
};
